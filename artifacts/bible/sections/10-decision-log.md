---
title: Decision Log
icon: "⚖"
status: stable
updated: 2026-07-22
---

Architectural decisions with reasoning, newest last. Agents: append, never rewrite history; superseded decisions get a `superseded-by` note rather than deletion.

| ID | Date | Decision | Choice | Why | Rejected |
|----|------|----------|--------|-----|----------|
| D-001 | 2026-06-10 | System architecture | Monolith core with B-seams (EventBus, ModelRouter) | Fastest path to a working system for one operator; seams make worker split / alt models a transport swap, not a rewrite | Microservices day one (overhead); pure monolith without seams (paints into corner) |
| D-002 | 2026-06-10 | Bible source of truth | **Compiled bible** — structured md sections + live DB data → rendered HTML | Agents edit small diffable sections, never raw HTML; visual quality is owned by the compiler so it never degrades; live data keeps docs honest | Hand-authored HTML (agent edits erode it); flat markdown (doesn't scale, no live data) |
| D-003 | 2026-06-10 | GitHub connection | **`gh` CLI + polling**, SQLite cache, GitHubProvider seam | Zero new infrastructure, already authenticated, works behind any network; 15–60s lag is acceptable for a single operator | GitHub App + webhooks (public endpoint + app management is heavy for self-host) |
| D-004 | 2026-06-10 | CI/CD verification | **Two-layer**: GitHub Actions deterministic CI + agent-team verification skill | Machines gate merges deterministically and run while the operator's machine is off; agents add judgment (coverage gaps, reviews, doc freshness) and repair the CI itself | Local-only verification (invisible on GitHub); agents-as-CI (slow, token cost per push) |
| D-005 | 2026-06-10 | Project model | **Registry, both onboarding paths** (register local path / clone GitHub URL) | Existing work registers in place; new work clones into the managed workspace; registry row is identical either way | Managed-workspace-only (forces moving repos); path-only (harness should own cloning) |
| D-006 | 2026-06-10 | Dashboard IA *(IA superseded-by D-024)* | **Command Deck**: icon sidebar + ⌘K bar + swappable stage + activity strip; per-project workspace with 7 tabs incl. its own knowledge graph | One consistent frame at every scope; graph is first-class at both fleet and project level without being the only navigation; mockups approved in visual review | Card-grid home (graph buried); graph-first constellation (everything else one level down) |
| D-007 | 2026-06-10 | Design language | **Precision minimal** — near-black, hairline borders, single indigo accent, mono numerals, 150ms motion | Daily-driver tool: ages well, renders fast, makes data the hero; density without noise | Aurora glass (GPU cost, fights density); sci-fi HUD (gimmick fatigue) |
| D-008 | 2026-06-18 | Phase-1 deferral re-homing | **Re-home the Phase-1 "deferred to Phase 3" items into Phase 3 proper** — web terminal → 3-6, structured task/goal records + Issues sync → 3-7; auth hardening → Phase 4 | Each phase's checklist should reflect only its own delivered scope; "deferred to a later phase" lines made Phase 1 look permanently incomplete and the Phase 3 breakdown ambiguous | Leaving the items as unchecked Phase-1 lines (perpetually incomplete phase); dropping them silently (loses the commitment) |
| D-009 | 2026-06-20 | Design language layer | **Hybrid glass extends D-007** — keep precision-minimal for density/data, add a thin translucent backdrop-filter layer to hero surfaces only (command bar, modals, node inspector, activity strip), with an `@supports` opaque fallback and a `MotionConfig` reduced-motion default | Adds depth/polish so the tool reads as finished without sacrificing the density and legibility D-007 buys; glass confined to non-data surfaces so it never fights data; `@supports` fallback prevents transparent-over-text on browsers lacking backdrop-filter | Full aurora glass everywhere (the GPU-cost / density fight D-007 originally rejected); staying flat (interface read as unfinished); per-component ad-hoc motion (drift from the §06 motion tokens) |
| D-010 | 2026-06-20 | Knowledge graph engine | **GitNexus-orchestrated, server-owned graph engine** — `project_graphs` state table; `buildGraph()` shells `npx gitnexus analyze` behind an injected seam (in-flight guard, EventBus/WS emit); live node enrichment, node-scoped dispatch, and debounced auto-reindex on run completion | One operator-triggerable, observable build pipeline with status/stale tracking; the analyze seam keeps GitNexus out of CI and unit tests deterministic; enrichment/dispatch/auto-reindex make the graph an actionable diagnostic surface, not a static picture | Bundling a graph parser into core (re-implements GitNexus, drifts); building synchronously in the request (long analyze blocks the handler); auto-reindex with no debounce/guard (rebuild storms on busy projects) |
| D-011 | 2026-06-20 | UI demo as a first-class artifact | **Compile the interactive UI demo to a stored artifact + ship a `create-web-ui-artifact` generator skill** — rich self-contained HTML written to disk verbatim (bypassing the artifact sanitizer), served in a sandboxed iframe (`allow-scripts`, no `allow-same-origin`); the compile route accepts only `{ projectId }`; a skill (modeled on `onboarding`) authors per-project demos | The bible self-demonstrates the design language with a real, interactive surface; verbatim-on-disk keeps inline CSS/JS intact while the no-same-origin sandbox + projectId-only route keep untrusted HTML out of the verbatim-write path; the skill makes per-project demos repeatable | Re-sanitizing the demo (strips the inline JS/CSS that make it interactive); accepting caller HTML at the route (opens a verbatim-write XSS path); a static screenshot (not interactive, drifts from the live design) |
| D-012 | 2026-06-24 | Todo delegation-workflow execution model | **Idea 1 — one supervised orchestrator run per selection** (`workflows.ts` + `workflow_runs`): a multi-select of todos dispatches a single controller run that runs the delegation loop (implementer → spec-review → quality-review → controller) in its own worktree and opens one PR; the `workflows.ts`/`workflow_runs` boundary preserves the Idea-2 option without paying for it | The delegation loop is a **prose** methodology, not code, and `startRun` is strictly one-agent/one-worktree — so a selection naturally maps to one combined run that the agent decomposes internally; the seam + table keep the staged-engine option open (add a `workflow_stages` table, spawn one run per stage on `eventBus`) without building it now; the route, api client, and UI are unchanged when it grows | Idea 2 — staged multi-run engine (requires threading one shared worktree/branch across independent runs, which the worktree-per-run model doesn't support: a large, risky subsystem); Idea 3 — workflow-as-skill (the loop is methodology/prose, not a discrete skill artifact, and would still bottom out on a single `startRun`) |
| D-013 | 2026-06-24 | Vivid midnight-glass redesign + IA cleanup *(IA superseded-by D-024)* | **Re-skin to a vivid midnight-purple / blush-pink / sky-blue liquid-glass language and fix the navigation IA** — extends D-009 with bolder glass, roundedness (18/14/10px radii) and generous spacing; the accent splits in two — blush `#ff8fc0` for fills/active pills/badges, sky `#38bdf8` for hover/active/focus transitions; a labeled collapsible sidebar replaces the 52px icon rail (Docs dropped as a top-level destination, Help/Settings → footer); the project-workspace "Bible" tab becomes "Artifacts" (a project-scoped artifact gallery); graph nodes gain glow + labels and edges become visible; hard-delete lands for projects (transactional, FK-safe) and tasks | The precision-minimal indigo tool (D-007) read as an internal utility — dense, tiny icon-rail, invisible graph edges, no delete, a misnamed Bible tab. Product-quality polish (Apple-landing-page roundedness/depth), an intuitive labeled nav, a legible diagnostic graph, and delete affordances make it read as a product without sacrificing the density D-007 buys: status colours still carry meaning, glass stays hero-only, mono numerals stay. Supersedes the *palette/IA* of D-007/D-009; retains their density discipline + hero-only-glass principles | Staying precision-minimal indigo (read as unfinished/internal); aurora-glass-everywhere (GPU cost + density fight D-007/D-009 reject); white text on the light blush accent (fails WCAG AA — accent FILLS use dark `--bg` text, accent-as-text stays on dark surfaces) |
| D-014 | 2026-06-27 | Interactive multi-turn HITL | **Persistent-stdin interactive runs** (A3.0 smoke-confirmed) — keep the child's stdin open; each operator turn is one `{type:"user",message:{role,content}}` line; `{type:"result"}` marks the turn boundary → park the run at the non-terminal `awaiting_input`; `POST /api/runs/:id/input` uses an atomic conditional UPDATE to guard the double-send race; `/end` closes stdin → `done` | A real multi-turn conversation needs the same process + context + cost state across turns; the `result` line is the only reliable turn boundary on the stream-json wire; the conditional UPDATE makes the awaiting→running flip race-safe under double submits | Re-spawn the CLI per turn (loses context/cost); kill on first `result` (no multi-turn); a non-atomic optimistic transition (double-send race) |
| D-015 | 2026-06-27 | Event-data enrichment foundation | **Persist structured tool fields** on each event (`toolUseId/toolKind/toolInput/toolResult/toolResultIsError/subagentType/childLabel`) parsed in `parseClaudeLine`; pair `tool_use` (assistant) ↔ `tool_result` (user) by `tool_use_id` across SEPARATE events; race-tolerant `ALTER TABLE ADD COLUMN` migration; `classifyTool` table-maps a tool name → command/file/delegate/other | The rich console + workflow tree need structured data, not raw re-parsed on every render; tool_use and its result arrive as separate stream-json events so a stored join key is required; a tolerant migration keeps multi-process/CI boots safe | Parse `raw` JSON on the client each render (slow, fragile); collapse tool_use + result into one event (the wire delivers them separately) |
| D-016 | 2026-06-27 | Workflow visualization | **Purpose-built CSS/SVG static defined-loop diagram** from a shared `DELEGATION_WORKFLOW` constant + a **live runtime sub-agent tree** derived from `delegate` tool calls (delegated agents run IN-PROCESS via the Agent/Task tool, so a run's sub-agents ARE its delegate tool calls) | The data is a small fixed hierarchy plus a per-run shallow tree, so a hand-laid diagram is simpler and crash-free; one shared constant keeps client + server in agreement without a new endpoint; the live tree reuses the D3/D4 pairing helpers | A force-graph (wrong tool for a small fixed hierarchy + WebGL/AFRAME crash risk); a new GET endpoint for static data (a shared import suffices) |
| D-017 | 2026-06-27 | Editable prompts & config | **Editable skill prompts** via PATCH (partial update, name-collision → 409, clear→NULL) + a **Settings page** with provider/auth STATUS cards (claude/ollama/github/auth posture, NO secrets) and a **guarded global CLAUDE.md editor** (fixed path, gitnexus block preserved, atomic temp+rename, rolling backups, confirm-before-save, schema-locked body) | The prompts + config that drive agents should be observable and editable in-app; status reveals provider/auth posture without leaking tokens; the editor is guarded so it can't traverse, drop the gitnexus block, or half-write a file | A free-form path param for the editor (path traversal); per-project prompt editing now (global-only is the smaller, safer first step); returning the token value in status (leak) |
| D-018 | 2026-06-27 | Context indicator + smoke-gated compaction | **Context-pressure indicator** (`ctx X / Y · Z%`, warn ≥70 / danger ≥90) counting the FULL input context (fresh `input_tokens` + `cache_creation_input_tokens` + `cache_read_input_tokens`, persisted as `context_tokens` so reloaded runs stay accurate) + **real CLI `/compact`** (a manual "Compact context" button AND a guarded/debounced hysteresis auto-compact). The D6.0 live smoke OVERTURNED the planned premise — `/compact` IS honored over the stream-json input wire (the CLI runs its compaction machinery), so on-demand compaction CAN be forced via `sendInput('/compact')` | Fresh `input_tokens` alone underreports true context on cached runs; the smoke proved compaction is forceable, so a real Compact button + hysteresis auto-trigger beats a soft self-summary; persisting `context_tokens` keeps the meter honest after a reload | The soft "summarize & continue" fallback (unnecessary once the smoke proved real `/compact`); counting fresh `input_tokens` only (underreports). Deferred: compact-and-CONTINUE success on a long non-empty context (a Wave V live item) |
| D-019 | 2026-06-27 | Desktop app deferred | **Defer the Tauri desktop app** (bundled-core sidecar, tray, native notifications) to a later phase; the web Command Deck stays the surface | The observability + agent-UX priorities of this phase deliver more operator value per unit effort than re-packaging the existing web app; the full desktop spec is retained for later (`~/.claude/plans/read-through-and-analyze-rippling-hanrahan.md`) | Building Tauri now (cost vs. this phase's observability / UX priorities) |
| D-020 | 2026-06-27 | Agent-organization model | **Re-frame to a directed agent ORG with 3 authority tiers as ONE profile entity** — K (secretary, the friendly face, no code), Chief (manager, delegates, no code), orchestrator leads (staff engineers, full coding within a charter); all are one `AgentProfile` differentiated by `tier: secretary\|chief\|orchestrator`; a lead's discipline (Frontend/Backend/Systems/Security/Network) is a capability bundle + charter, NOT a directory | The product value is "you direct an org," not "you operate a dashboard"; one entity + a tier field keeps the model small while the tier gates capability; discipline-as-charter lets the roster grow by adding profiles, not code paths; matches how a real eng org delegates down and reports up | Three separate agent codebases (triples the surface); discipline-by-directory (false constraint — a lead isn't bound to a folder); a flat single-agent assistant (no delegation, no specialization, no authority gating) |
| D-021 | 2026-06-27 | Authority control plane | **Tier-scoped MCP servers + the `--allowedTools` allowlist enforce authority** — `logistics-mcp` (K), `mgmt-mcp` (Chief), a status-write MCP (leads); coding tools (Bash/Write/Edit/Agent) only at the lead tier; reuse the Google Calendar/Gmail/Drive connectors (K) and GitNexus MCP read-only (Chief). It is a gating LAYER, not a fourth seam — events/models/GitHub still flow over the existing B-seams | Authority must be enforced, not advisory — a tier physically cannot mount a server or call a tool it isn't granted, even if prompted; MCP scoping + the allowlist compose to do that; reusing existing connectors avoids rebuilding calendar/mail/code-intel; keeping it a gating layer preserves the B-seam architecture | Prompt-only "please don't" guardrails (bypassable); one mega-MCP for all tiers (no isolation); a new transport seam for authority (over-engineered — gating composes onto the existing seams) |
| D-022 | 2026-06-27 | Memory model | **Layered memory, START AT A** — per-profile markdown lessons + a gated end-of-run reflection (the run proposes a *pending* lesson; the operator approves before it enters memory), composing with the project's `lessons.md`; storage shaped to grow into B (structured store + relevance retrieval + outcome-weighting, the ModelRouter posture) and C (verification/eval-derived lessons) | Start simple and human-gated so nothing self-modifies its brain unreviewed; file lessons already exist, so A is mostly reuse; shaping the store + approval gate + outcome metadata now makes B/C a swap, not a rewrite (same grow-into-it posture the ModelRouter took for routing) | A heavyweight vector store on day one (premature, unproven retrieval value); ungated self-editing memory (a bad lesson poisons the profile); no memory at all (the org never gets smarter) |
| D-023 | 2026-06-27 | K runtime *(K's warm-session hybrid superseded-by D-062)* | **Persistent identity, ephemeral execution** — every tier is durable (charter + memory + thread) but only ever RUNS as a bounded `startAgentRun(profileId, { trigger, … })` activation; triggers = user-message (HITL), schedule/event (Phase-3 scheduler, reused), delegation (tier→tier). K specifically is HYBRID: the durable thread + memory is source of truth, a warm interactive session is kept while actively chatting, and a fresh seeded run is spun on restart/idle-timeout/scheduled wake | A durable secretary you "talk to" can't be one never-ending process (cost/leak/restart-fragility), but re-seeding every message loses the feel of continuity; making identity durable and execution bounded gives both — continuity from stored thread+memory, robustness from short activations; reuses the supervisor/EventBus and the Phase-3 scheduler unchanged | A single always-on K process (leaks, can't survive restart, unbounded cost); stateless re-seed per message (loses conversational continuity); a bespoke runtime outside the supervisor (re-implements what `startRun` already does) |
| D-024 | 2026-06-27 | Redesign — evolve-visual + agent-first IA | **Evolve (not replace) vivid midnight-glass + reorganize the IA into Direct / Observe** — keep `#1a0f2e` / `#ff8fc0` / hero-only glass / mono numerals, warmed/friendlier on K-home; Direct group (K-home landing · Chief · Orchestrators + detail · Workflows + detail · Projects) + Observe group (Runs · Graph · Metrics · Routing · Terminal) + Settings/Help footer; the MCP/authority-tier panel is a persistent first-class panel on Orchestrator-detail (read-only mirror on Chief); the abstract Workflows diagram folds into Orchestrator/Workflow detail; confirm-card + toast-with-link is universal. Marks the *IA* of D-006 and D-013 `superseded-by: D-024`, retaining their density/glass/token discipline | The org re-frame (D-020) makes an operator-centric tool IA wrong — the home should be the conversation with K and the nav should mirror the org; evolving the proven visual language (vs. a fresh re-skin) keeps the token discipline that already works; making the authority panel first-class makes the control plane legible; universal confirm + toast-with-link fixes the "hunt for your result" / silent-action gaps | A from-scratch visual redesign (throws away working D-013 tokens); keeping the flat operator IA (contradicts the org model); a separate standalone Workflows diagram (floats without context — better embedded where a lead runs it) |
| D-025 | 2026-06-27 | Roadmap re-baseline | **Phase 5 = Agentic Org** (5.1 Foundation+K · 5.2 Chief · 5.3 Roster+Workflows · 5.4 Voice) becomes the headline next program; the old "Phase 5 — Intelligence & Scale" stub renumbers to **Phase 6** (and absorbs memory layers B/C). Phase 4 (Agent-UX + Observability) is merged | The agent org is now the central thesis (D-020), so it earns the next-phase headline; Intelligence & Scale (NATS/Redis, learned routing, graph-driven context) is real but optional and naturally follows the org, so it moves to Phase 6; memory B/C map cleanly onto it | Slotting the org work as a sub-phase (under-sells the central re-frame); leaving Intelligence & Scale as Phase 5 (collides with the new headline); dropping the Intelligence stub (loses the committed scale work) |
| D-026 | 2026-06-27 | IA simplification (Phase-5 UX) | **One front door · one org-status home · compose-is-confirm + undo · unified work-items · de-duplicated views** — K (⌘K) is the only dispatch surface and every per-screen `⚡` is a SCOPED PREFILL of the K composer (to this lead/project/symbol), not an independent dispatch; K routes (logistics it handles, the Chief, or a specific lead) and shows the route INLINE before send. Chief is the SINGLE org-status home (objectives + one delegation tree + a thin health line, no full health strip). Send fires with a 5 s UNDO toast; a full confirm-card appears ONLY on escalation (T3 authority / cross-project / destructive kill·delete·reassign). ONE DelegationTree component reused at scopes (Chief whole-org · Orchestrator-detail one-lead); Graph 3D stays the structural code/fleet graph. UNIFIED `work_items` (`scope: personal/org/project`) replaces the `agent_tasks`/`project_tasks` split; every metric printed in exactly one place. **Refines D-024.** | Many co-equal dispatch surfaces and duplicated org views read as cosmetic hierarchy and force a hunt-for-result; collapsing to one front door, one status home, compose-is-confirm + undo, and de-duplicated metrics/trees keeps the agent-first IA legible and low-friction | Many co-equal dispatch surfaces + duplicated org views (read as cosmetic hierarchy, hunt-for-result) |
| D-027 | 2026-06-27 | Claude Code ↔ K boundary | **K synthesizes a per-run config dir** — three ownership domains (D1 host `~/.claude` invoked-never-depended-on · D2 the K repo incl. versioned `agent-config/` · D3 the per-run synthesized dir); `synthesizeConfigDir` relocates `CLAUDE_CONFIG_DIR` per claude run (settings, per-tier `--allowedTools`, `--mcp-config`/`--strict-mcp-config`, vendored gitnexus skills/hook, injected **L0 base operating prompt + L1 tier charter**); auth token-first (`ANTHROPIC_API_KEY`/`CLAUDE_CODE_OAUTH_TOKEN`) with a guarded host-credential fallback | The agent engine is the host CLI, but a managed run must never inherit the host's skills/plugins/MCP/credentials — relocating the config dir per run makes the boundary product-clean and enforced (the host can't load), while injection keeps K's operating prompt + tier charter authoritative over any project `CLAUDE.md` | Pointing runs at the host `~/.claude` (leaks personal skills/plugins/MCP, non-reproducible); a static shared config dir (no per-tier allowlist/MCP scoping); prompt-only "ignore your host config" (unenforceable) |
| D-028 | 2026-06-27 | artifacts/ + tasks/ are hidden, K-owned, never committed | **Gitignore the K-system dirs in every project** — `artifacts/` and `tasks/` use the same hidden names everywhere (`onboard.ts ensureGitignore`); bibles scaffold to `artifacts/bible/`; K's **own** bible leaves version control and lives on-disk as a living spec | These are K's working memory, not project source — committing them pollutes managed repos and couples K-internal state to project history; one consistent hidden location means any agent knows where to look without per-project config | Versioning the bible/tasks into each repo (pollutes history, merge noise); a per-project config for the location (drift); deleting them (loses the living spec / tracker). **Deferred to Phase 5:** seeding + the `work_items` / `agent_memory` migration |
| D-029 | 2026-06-27 | Host `~/.claude` conservative cleanup | **Vendor what K needs, then offer a reversible host cleanup** — gitnexus skills+hook are vendored into `agent-config/`; `scripts/host-cleanup.mjs` reconciles a dev host toward "invoked-never-depended-on" (dry-run default; `--apply` backs up first; removes only what K now owns) | Once K vendors its dependencies the host copies are redundant, but a developer's host is personal — cleanup must be conservative and reversible, never surprising | Auto-cleaning on boot (surprising, destructive); leaving everything (the host stays a hidden dependency); removing personal plugins too (superpowers/ecc/ui-ux are the operator's, left intact) |
| D-030 | 2026-06-27 | Runtime Ollama model management *(PLANNED 5.5)* | **A persisted `app_config` store makes the active local model operator-selectable & hot-swappable** — `route()` reads the active model + `ENABLE_OLLAMA` through getters (seeded from env), so a Settings selection applies to the next run with **no restart**; a model surface over the Ollama HTTP API (`/api/tags` list · `/api/pull` NDJSON progress on the EventBus→WS wire · `/api/delete`) plus a curated catalog and an `fs.statfs` disk-fit check; generation stays on the `ollama run` CLI | The model was a boot-time env constant invisible to the operator — you couldn't see installed models, pull new ones, or switch without editing `.env` and restarting; a tiny config store + the Ollama HTTP API make it a live, observable surface while leaving the generation path and the ModelRouter contract untouched (the same grow-into-it posture routing took) | Env-var-only (restart to change, invisible); shelling `ollama pull` with no progress (opaque multi-GB downloads); free-form pull with no disk guard (fills the disk mid-download); a vector/registry service (over-built for a local model list) |
| D-031 | 2026-06-27 | Voice transcription — push-to-talk *(PLANNED 5.4)* | **A new `TranscriptionProvider` B-seam + local Whisper, push-to-talk** — `transcribe(audio)→{text}` with a default impl POSTing to a local OpenAI-compatible Whisper server (`faster-whisper`, `WHISPER_BASE_URL`); browser `MediaRecorder` captures hold-to-talk audio → `POST /api/transcribe` (core proxies, browser holds no key) → transcript drops into the K composer / HITL reply as ordinary text; gated by `ENABLE_VOICE`, degrades to keyboard when unreachable | Talking to agents needs STT, and Ollama can't run speech models — so transcription is its own swappable seam, kept **local-first** so audio never leaves the box; proxying through core keeps any key server-side; reusing the existing composer/HITL input means nothing downstream changes; the seam lets a cloud STT swap in later without touching callers | Browser Web Speech API (streams audio to Google/Microsoft, no Firefox, quality varies); cloud STT day one (API key + per-minute cost + audio off-box); routing STT through Ollama (no speech models); always-listening/wake-word (false-trigger risk, larger build — deferred) |
| D-032 | 2026-06-28 | Subagent-spawn tool-id is `Task`, not `Agent` *(corrects D-021)* | **Keep "agent" as the prose concept everywhere; the only machine token is `Task`** — the `--allowedTools` allowlist + any tool enumeration use the literal `Task` (the CLI's subagent-spawn tool-id, verified against the binary); charters/skills/docs keep saying "agent / subagent". D-021's "Bash/Write/Edit/Agent" is corrected to `…/Task` | `--allowedTools` matches by exact string — `"Agent"` grants nothing, so an orchestrator with `Agent` on its allowlist could spawn no one; but renaming the concept would be churn, so only the one machine-matched string changes | Renaming the concept to "Task" everywhere (needless churn, worse vocabulary); leaving `Agent` in the allowlist (silently un-spawnable orchestrator) |
| D-033 | 2026-06-28 | Worker agents = tool-scoped subagent definitions + per-tier bundles | **Real subagent DEFINITIONS (`agent-config/agents/*.md`) an orchestrator spawns, mounted per tier by a bundle** — six defs (implementer · spec/quality/security reviewers · debugger · planner), each with its own `tools:` frontmatter scoped to authority (only the implementer writes; the rest read-only), every def's tools ⊆ the spawning tier's allowlist; `agent-config/bundles/<tier>.json` declares which skills + agents the synthesizer mounts (orchestrator = full roster; chief/secretary = none). Realizes D-020's "role subagents" | Authority must be bounded per role, not just per tier — a read-only reviewer shouldn't hold Write/Edit/Task; encoding each role as a def with its own tool scope + a per-tier bundle makes the roster data, mounts only what a tier needs, and lets an asset test enforce `tools ⊆ allowlist` and the bundle↔roster match | Ad-hoc inline `Task` prompts (no durable, reviewable role contract); one shared agent set for all tiers (chief/secretary would gain coding agents); granting every worker the full tool set (over-broad authority) |
| D-034 | 2026-06-28 | kstore — storage is tools, not files *(realizes D-022 layer A; refines D-026)* | **A K-owned stdio MCP server over `work_items` / `agent_memory` / `workflow_steps`** exposing work-item CRUD, lesson propose/list (gated reflection — proposed lessons land `pending` in `agent_memory`), and **workflow status-write** (`workflow_step_set` / `workflow_status_set`, resolving the `workflow_runs` row from the injected `K_RUN_ID`). The store layer is **SDK-free** (unit-testable; reusable by a CLI fallback) under a thin transport glue; tools are **run-scoped** (a run reads/mutates only its own tickets/lessons); the synthesizer rewrites the server's command/env per run and **`mcp__kstore` must be allowlisted** or the mounted tools are denied in headless `-p`. Memory layer A is thus a TOOL, not `tasks/lessons.md` (home-dev only); the shipped `work_items` is run-scoped (unifying with `project_tasks` under D-026's `scope` model is deferred) | Managed agents needed real storage, not home-dev `tasks/*.md` files; an MCP tool over SQLite is the enforced, observable seam; SDK-free store logic keeps it testable and gives a Bash-CLI fallback if MCP-in-`-p` is flaky on Windows; run-scoping prevents one run reading/clobbering another's tickets/lessons; the K_RUN_ID→workflow resolution keeps the agent from handling ids | File-based tasks/lessons for managed runs (couples K state to project trees, not enforced); a store layer that imports the MCP SDK (untestable without a transport); a globally-shared store (cross-run read/write escalation); trusting an arg-supplied run/workflow id (cross-run forgery) |
| D-035 | 2026-06-30 | Eval, integrated into the engine (F3) | **LIFT the proven out-of-band `testing/eval/` harness INTO core as a DB-backed, dashboard-surfaced, operator-triggerable subsystem** — port `testing/eval/harness/*.mjs` → typed `core/src/eval/*` (direct-await dispatch, NOT on the F2.W1 supervised-run seam); generalize `skill_evals` → `eval_systems`/`eval_cases`/`eval_runs`/`eval_results`/`eval_baselines` seeded from the existing files; `POST /api/evals/run` + report/baseline GETs; an **Evals** dashboard. **Token-gated:** `startEvalRun`, the route, AND the runner all DEFAULT `dry:true` — a real (spend) run requires an explicit `dry:false` | The rigorous behavioral harness (6 systems × 8 cases, deterministic CHECKS + fixed-judge + a degraded-anti-prompt discrimination control) was the real eval methodology but lived outside the engine (not in `pnpm test`, not surfaced); lifting it verbatim preserves the degraded-discrimination control (the synth-config `startRun` path always injects real L0+L1, which would fight that control) while making evals a first-class operator surface; dry-by-default at EVERY layer keeps it spend-safe | Generalizing `startRun` for eval dispatch (fights the degraded control + high blast radius on CRITICAL `startRun`); a separate `@k/eval` package (a subdir suffices); leaving the harness out-of-band (invisible, never run); rewiring skills `/test` now (deferred — stood the subsystem up alongside) |
| D-036 | 2026-06-30 | Live coverage-trend signal (F4) | **Derive the `verify.ts` coverage-trend factor from a real per-project signal** — `readCoveragePct` reads `coverage/coverage-summary.json` (`total.lines.pct`); `classifyCoverageTrend` compares it to the `coverage_pct` persisted on the project's previous report (improving/stable/declining/unknown, 0.1 pp dead-band); each report persists its measured `coveragePct`. Kills the hardcoded `coverageTrend='unknown'` stub so the 20-pt factor is LIVE; uninstrumented projects (no coverage file) stay neutral (unknown → full marks) | The 20-point coverage factor was a no-op stub (always `unknown` → neutral), so health was effectively weighted only on CI + bible + findings; reading the project's own coverage summary + comparing to persisted history makes the factor real and deterministic (single-shot, no test run) while never penalizing a project that simply doesn't emit coverage; the neutral fallback preserves every existing bare-project score | Running coverage during verify (non-deterministic, slow, compute cost, breaks the single-shot snapshot); penalizing `unknown` (punishes uninstrumented projects); keeping the hardcoded stub (the 20 pts stay permanently inert) |
| D-037 | 2026-07-01 | Agent-org foundation realized (P5.0) | **Stand up the org seam the whole agent-org rides** — a DB-backed `agent_profiles` registry (`core/src/profiles.ts`: get/list/create/update + `seedProfiles`) seeded with the eight durable profiles; `core/src/authority.ts` resolving `tier → {allowedTools, mcpServers, skills}` from the SAME `agent-config/{allowlists,mcp,bundles}` assets the synthesizer reads; `startAgentRun` (`core/src/agent-runs.ts`) generalizing `startRun` over the `run-lifecycle` seam with dispatch-failure rollback; `@k/shared` `AgentProfileSchema` as the canonical type | The org needed a durable, inspectable identity + an enforced authority record before any Chief/K wake loop could ride it; sourcing authority from the same assets the synthesizer mounts (fail-closed grant guard, D-034) makes a seeded profile's grant match its run exactly; riding the existing run-lifecycle seam (not a bespoke path) keeps `startRun` a one-line additive change on a CRITICAL symbol | (a) storing the charter prompt TEXT in the DB column — rejected: duplicates + drifts from the `.md` the synthesizer reads, so `charter` holds the charter-asset BASENAME (=== tier) and the old `charterTier` field was renamed to `charter` for db/shared consistency; (b) a fresh `agent_memory` lessons table — rejected (collides with the shipped kstore table), so `profile_id` is added via guarded ALTER reusing `run_id` as the source-run; (c) no tracking table — rejected (nothing for the seam to patch/finalize + no way to assert the rollback), so an `agent_runs` table mirrors `skill_runs`/`workflow_runs` |
| D-038 | 2026-07-01 | Runtime Claude default model *(5.5)* | **Move the global Claude default model off the env-frozen `const` onto the `app_config` runtime store** — `router.ts` reads `claudeDefaultModel()` (config-store, seeded from `CLAUDE_MODEL`) at `route()` **call time**, not module load, so a Settings change applies to the next run with **no restart**; `GET/PUT /api/claude/model` validates against the shared `KNOWN_MODELS` registry (400 on unknown). Mirrors the D-030 Ollama/voice config pattern; the per-run ⌘K picker still overrides per dispatch. The per-profile `default_model` (P5.0) reconciles with this global default at integration | The Claude default was `const CLAUDE_DEFAULT_MODEL = process.env.CLAUDE_MODEL …` read once at load — invisible and unchangeable without an env edit + restart (a recon finding); a getter over the existing config store makes it operator-selectable and hot-swappable while leaving the ModelRouter contract and every `route()` caller untouched (route() stays pure via an injectable dep) | Env-var-only (restart to change, invisible); a whole new settings table (the `app_config` store already exists); letting any string be the default (a typo silently breaks dispatch — hence the registry gate) |
| D-039 | 2026-07-01 | logistics-mcp — K's logistics working store *(realizes D-021's `logistics-mcp`; sibling to D-034 kstore)* | **A K-owned, run-scoped stdio MCP server (`logistics`) over three tables — `logistics_notes` / `logistics_events` / `logistics_reminders`** — exposing `note_add|list|update`, `event_add|list|update`, `reminder_add|list|update` (nine tools). Same shape as kstore: an **SDK-free** store layer (`core/src/mcp/logistics.ts`, unit-tested) under a thin transport glue (`logistics-server.ts`); tools are **run-scoped** (a run reads/mutates only its own rows; a bogus/absent `K_RUN_ID` degrades to a null owner, no FK error); the synthesizer resolves the server's command/env per run (generalized to a `runScopedServers` loop, behaviour-preserving for kstore); and **`mcp__logistics` is allowlisted** in `allowlists/secretary.json` alongside the `mcp/secretary.json` mount (D-034 — mounting ≠ granting). It is **STORAGE, not execution** — a stored event is never scheduled on a real calendar. **`tasks` deliberately stay the kstore `work_item_*` tools** — logistics holds only the non-ticket logistics data (notes/calendar/reminders) K-home shows, so no third task store is introduced ahead of the D-026 unification | K's secretary tier needed a real store for the logistics data the K-home surface renders (notes, schedule, reminders), and the proven kstore pattern already gives an enforced, observable, testable seam; run-scoping keeps one run from reading/clobbering another's logistics; keeping the store SDK-free preserves the unit-test + Bash-CLI-fallback posture; scoping logistics to non-ticket data avoids duplicating the work_items store and pre-empting D-026 | A general "task list" table in logistics (rejected — collides with kstore `work_items` + prejudges the D-026 `scope` unification, so tasks stay kstore); wiring the Google Calendar/Gmail/Drive connectors now (deferred to Phase 5 — a local store is testable + deterministic and satisfies K-home today); a store layer importing the MCP SDK (untestable without a transport); a separate synthesizer block per server (duplicates the kstore wiring — folded into one `runScopedServers` loop instead) |
| D-040 | 2026-07-01 | mgmt-mcp + Chief org tree — the management store & its derived view (P5.2a) *(sibling to D-034 kstore / D-039 logistics)* | **A Chief-owned, run-scoped stdio MCP server (`mgmt`) over two tables — `mgmt_assignments` / `mgmt_reports`** — exposing `assign_lead` · `pick_workflow` · `scope_projects` · `report` (four tools). Identical shape to kstore/logistics: an **SDK-free** store layer (`core/src/mcp/mgmt.ts`, unit-tested) under a thin stdio glue (`mgmt-server.ts`); **run-scoped** writes/ownership reads (a run mutates only its own assignments; a bogus/absent `K_RUN_ID` degrades to a null owner) plus ONE deliberately **cross-run** operator read (`listRecentAssignments`, the Objectives panel — not a tool); appended to the synthesizer's `runScopedServers` loop and mounted+**granted** on the chief tier (`mcp/chief.json` + `mcp__mgmt` in `allowlists/chief.json`, D-034). It is **STORAGE, not execution** — assigning a lead does not dispatch it. The **Chief org tree** is a pure DERIVATION (no new table): `GET /api/chief/org` returns one batched `ChiefOrgPayload`; a generic recursive `DelegationTree` component + a pure builder (`web/src/lib/delegation.ts`) assemble Chief → leads → sub-agents by **reusing `eventsToWorkflowTree` unchanged** for the sub-agent level | The Chief tier needed the same enforced/observable/testable store the other tiers have, and the proven kstore/logistics pattern drops in verbatim; the org tree is exactly the per-run delegation tree rooted one tier higher, so deriving it from run events (not persisting a delegation table) keeps it a view over the single-wire EventBus; making `DelegationTree` generic + its builder pure lets P5.3 reuse both for a single-lead root, and keeps the page a ONE-query read (no per-lead fan-out) | Persisting a delegation/tree table (rejected — the tree is derivable from events; a table would drift and duplicate the run log); dispatching the lead from `assign_lead` (rejected — that couples storage to execution and pulls the P5.2b wake/delegation scope forward; storage stays inert); a per-lead `useQuery` fan-out on the page (rejected — N+1 over the wire; one batched `ChiefOrgPayload` instead); recomputing a full health strip in the route (rejected per D-026 — the payload carries only a THIN leads-active count) |
| D-041 | 2026-07-01 | Memory layer A — operator approve/reject gate *(surfaces D-022 layer A; over the D-034 kstore `agent_memory` table)* | **A thin HTTP gate + a "Memory review" web surface make the pending-lesson queue operator-usable** — `core/src/routes/memory.ts` exposes `GET /api/memory/lessons?status=&profileId=` (defaults `pending`, LEFT-joined to `agent_profiles` for the proposing profile's name in ONE batched query) and `POST /api/memory/lessons/:id/approve|reject`, performing **status transitions only** (pending→accepted\|rejected) with **validate-before-mutate** (unknown id → 404; already-reviewed → 409; bogus `?status` → 400). The mutation flips the **same `agent_memory` row** the kstore `lesson_propose` tool wrote (`agentMemoryDb.getLesson` + additive `updateLessonStatus`, keyed by PK — no shadow copy), stamping `reviewed_at`. A `MemoryPage` renders the queue as Approve/Reject cards (Reject via the shared `ConfirmDialog`) with a lighter accepted/rejected history view; additive `api.memory` namespace; new `memory` sidebar destination (+ chord, required by the destination↔chord invariant test). Retrieval of accepted lessons back into a run stays **layer B (deferred)** | Layer A's approval gate existed only as a table + producer tool — a proposed lesson could be written but never reviewed except by hand-editing SQLite; a small route mirroring `routes/evals.ts` + a review surface makes the gate real while touching only status/`reviewed_at`, so a bad lesson can't self-approve and memory still can't self-modify without a human; flipping the producer's own row (not a copy) keeps `lesson_list` and any future layer-B retrieval reading exactly what the operator approved | Editing lessons through the gate (rejected — the gate is transitions only, not a lesson editor; text is the agent's, the verdict is the operator's); an idempotent no-op on a non-pending id (rejected in favor of a 409 so a stale UI double-action is a clear signal, matching the skills-route conflict pattern); a per-profile retrieval read here (rejected — that's layer B; A is gate-only); extending `@k/shared` with a new response type (rejected — a web-local `MemoryLesson` mirrors the backend shape, following the `lib/evals.ts` precedent, keeping the shared surface minimal) |
| D-042 | 2026-07-01 | K front door + hybrid runtime (P5.1c) *(realizes D-023; warm/fresh hybrid superseded-by D-062)* | `POST /api/k/ask` activates K via `startAgentRun('k-secretary', {trigger:'user-message', thread})` over the existing supervisor/EventBus/WS stream; a durable K thread (`k_threads`/`k_thread_turns`) is the source of truth (persists across reload) while execution is ephemeral — a warm interactive session (reusing the D-014 persistent-stdin loop) continues the live run while chatting, and a fresh run is seeded from the durable thread when cold/idle; K's answers are captured back to the thread at each turn boundary so a reseed stays coherent; the surfaced route is a deterministic `routeForMessage` preview (client+server agree via `@k/shared`) while K's runtime tool/hand-up choice is authoritative | Talking to K needed a real front door reusing the run-streaming wire (no bespoke channel) that honors D-023 "persistent identity, ephemeral execution"; the thread outlives any run so reload keeps continuity, and warm-vs-fresh reuses the proven interactive HITL loop instead of a new session manager; a shared heuristic lets the composer preview the route before send without pretending to know K's runtime decision | A bespoke K chat channel (duplicates EventBus/WS); persisting only user turns (K forgets its own answers on reseed — so answers are captured on turn boundary); always fresh-seeding (loses warm continuity mid-chat); route computed only server-side (composer couldn't preview before send) |
| D-043 | 2026-07-01 | Orchestrators control plane — roster + detail + per-lead authority, over the reused DelegationTree (P5.3a) *(splits the large P5.3 wave)* | **Surface the five discipline leads as a first-class operator control plane, and SPLIT P5.3.** A new `routes/orchestrators.ts` exposes a slim roster (`GET /api/orchestrators`), one-lead detail (`GET /api/orchestrators/:id`), and a per-lead authority `PATCH /api/orchestrators/:id`. The Chief's per-lead assembly is lifted VERBATIM into `routes/org-shared.ts` (`isLead` · `assembleLead` · row-mappers · bounds) — behaviour-preserving for `GET /api/chief/org` — and a **slim `rosterVitals`** variant skips the per-lead delegate-events fetch the roster never renders. Every mutation delegates to `profiles.ts::updateProfile`, so the **fail-closed mcp↔allowlist grant guard is on the write path**: an ungranted MCP mount is rejected `400` with the row UNCHANGED; only the guard message maps to 400, any other throw re-throws (→ 500). The PATCH schema is `.strict()` and deliberately **excludes `tier`/`charter`** — a tier move could flip a lead off `isLead` and out of its own management surface. The UI is `OrchestratorsPage` (one batched query, slim cards) + `OrchestratorDetailPage` (tabbed skills/tools/MCP·Authority editors + read-only charter + approved-lessons memory) reusing the **one-lead `DelegationTree` via `leadNode`** (no re-derivation); nav adds an `orchestrators` destination + `g o` chord (destination↔chord invariant) with the detail param-routed like `project`/`verify`. **P5.3a ships this; P5.3b (deferred)** is the `workflow_definitions` table + generalized `buildDelegationPrompt` + Workflows list/detail UI + the Settings org-default authority/MCP panel | P5.3 was too large for one reviewable commit, so the highest-value coherent brick shipped first — the leads were already seeded (P5.0) and the reusable tree already existed (P5.2a), so the roster/detail/authority-editor is pure assembly over existing seams; routing every edit through `updateProfile` reuses the proven fail-closed guard instead of a second write path; lifting the Chief's assembly into a shared module (not copy-paste) means both org surfaces can never drift; excluding tier/charter from the patch closes a self-lockout foot-gun the reviewers caught | (a) a bespoke authority write path on the route — rejected (bypasses `updateProfile`'s grant guard; the whole point is fail-closed reuse); (b) mapping ANY `updateProfile` throw to 400 — rejected (mislabels a missing-charter-asset or DB fault as a client error; only the guard message is a 400, the rest re-throw → 500); (c) allowing `tier`/`charter` patches — rejected (a `PATCH {tier:'chief'}` makes the lead fail `isLead` and vanish from its own roster, unmanageable via itself); (d) the roster calling full `assembleLead` — rejected (fetches + maps delegate events it discards; `rosterVitals` skips that query); (e) building P5.3 as one commit — rejected (exceeds one reviewable brick; split into P5.3a roster/detail/authority + P5.3b workflow-defs/Workflows-UI/Settings-org-default); (f) reusing the `@k/shared` `WorkflowDefinition` name for the P5.3b DB entity — rejected (collides with the existing roles+edges diagram type; P5.3b introduces a distinct name) |
| D-044 | 2026-07-01 | Chief autonomous wake — reuse `agent_runs` as the wake ledger (P5.2b) *(realizes D-023 schedule/event triggers, Chief-side)* | **Wire the reused Phase-3 node-cron scheduler + the EventBus straight into `startAgentRun('chief', { trigger })`** (`core/src/chief-wake.ts`): a `*/15 * * * *` tick fires a `schedule` wake, a subscribed terminal run-completion fires an `event` wake; bounded by **two guards** (min-interval debounce, already-running/idempotent) + a **self-wake guard** (the Chief's own run finishing does not re-wake it) with a **failure-degrade** (a dispatch throw is recorded `failed` via the `startAgentRun` rollback, then swallowed so the loop never crashes). `startChiefWake()` is wired at boot (`index.ts`, stop-fn on `onClose`; default ON, `CHIEF_WAKE=0` opts out). A "Chief wake" is **not a new table** — it reuses `agent_runs` (`profile_id='chief'`, `trigger ∈ {schedule,event}`), whose columns already carry all four wake facts (kind=`trigger`, time=`created_at`, run=`run_id`, outcome=`status`) | The Chief needed to run continuously (D-020/D-023) but had no wake source — the mgmt store was STORAGE-only; the scheduler + EventBus + `startAgentRun` all already existed, so the wake is pure wiring, not new subsystems. Reusing `agent_runs` means the wake ledger is the SAME row the chief route already reads as `chiefWakes` and the ChiefPage `WakeRow` already renders (trigger/goal/run-link/status/time) — so the observability surface was wired before the wakes existed; P5.2b just makes them exist. The guards keep the loop bounded and the self-wake guard prevents a wake→run→complete→wake storm; the failure path leans on the existing rollback contract so no new degrade logic is added. A crash mid-wake would leave a `running` activation that permanently blocks Guard B, so boot reconciliation (`reconcileStaleActivations`) sweeps stale activations to `failed` alongside the existing runs sweep | A dedicated `chief_wakes` table (rejected — redundant: `agent_runs` already carries trigger/time/run_id/status and is already read by the chief route; a second table would drift + duplicate); rebuilding the scheduler or `startAgentRun` for the Chief (rejected — the Phase-3 scheduler + the activation primitive already do exactly this, so wiring beats rebuilding); waking on the Chief's own run completing (rejected — self-wake loop, hence the self-wake guard); pulling K→Chief delegation **dispatch** into this slice (rejected — it touches K's routing, a separate later slice; this wave is Chief-side wake only) |
| D-045 | 2026-07-01 | Task-model unification — the `scope` down-payment, split from the collapse (P5.1d1) *(advances D-026; over the D-034 kstore `work_items` table)* | **Land the D-026 `scope` discriminator on the canonical `work_items` table as a PURELY ADDITIVE column, and SPLIT the unification.** A guarded, `hasTable`-gated `ALTER TABLE work_items ADD COLUMN scope TEXT NOT NULL DEFAULT 'personal' CHECK(scope IN ('personal','org','project'))` in `migrate()` (ALTER-only, **not** in the CREATE-TABLE DDL — migrate() runs at boot so fresh + existing DBs share one path, exactly like `score_breakdown` / `project_tasks`-issue; race-safe through the S1-018 `addColumn` helper) gives every existing ticket `scope='personal'` (== today's run-scoped semantics). `scope` is surfaced on the `@k/shared` `WorkItem` type (`WorkItemScopeSchema`), mapped in `rowToWorkItem`, and accepted as an OPTIONAL `work_item_create` arg (defaulting `personal`); `org`/`project` are reserved. The **COLLAPSE is DEFERRED to P5.1d2**: adding `work_items.project_id`, migrating `project_tasks` rows into `work_items` (`scope:'project'`), and rewiring `routes/projects.ts` + `github.ts::syncIssues` + `workflows.ts` + the Tasks UI onto the one store (plus row *promotion* via `work_item_update`). Because D-026 is the flagged **RISKIEST** change, the wave was driven **characterization-first**: `core/test/p5-1d-characterization.test.ts` pins the CURRENT invariant contract of BOTH stores (kstore `work_item_*` lifecycle + run-scope isolation, the `project_tasks` route, and `syncIssues` insert/close/reopen/degrade via an injected fetcher), proven GREEN on unmodified code BEFORE the column and kept green through it; the new `scope` behavior lives in a separate RED→GREEN feature file. `project_tasks`, its routes, `syncIssues`, `workflows.ts`, and the web UI are UNCHANGED | The two task stores (`work_items` run-scoped; `project_tasks` project-scoped + issue-synced) diverge on schema, ownership, status enum, and API, so a one-shot collapse exceeds a single reviewable commit AND risks a forced behavior change on the highest-traffic task surfaces. Splitting lets the low-risk, behavior-preserving half — the `scope` column the whole model keys on — land first behind the unchanged public APIs, with characterization tests as the regression net proving nothing changed; the collapse then builds on a schema that already carries the discriminator. Defaulting to `personal` makes the migration a no-op for every existing ticket, and the additive-only shape means all pre-existing kstore/work-item/project-task/campaign tests stay green | (a) collapsing both stores in one wave — rejected (exceeds one reviewable brick; forces route/sync/UI rewiring + a data migration in the same commit as the schema change, on the riskiest surface); (b) adding `project_id` + folding `project_tasks` in now — rejected (that IS the deferred P5.1d2 collapse; premature here); (c) putting `scope` in the CREATE-TABLE DDL — rejected (breaks the established ALTER-only migrate() convention that keeps fresh + existing DBs on one path and the old-schema fixtures green); (d) making `work_item_update` mutate `scope` (row promotion) now — rejected (no consumer until the collapse; §04's "promotable along the same row" wiring is P5.1d2); (e) loosening a characterization test when two pre-existing fixture tests went red — rejected (they were red only because a now-required column must be bound / appears in the item's key-set; the fixtures were updated preserving their exact intent — s1's rollback still fires on the `status:'NOPE'` CHECK, s3 still asserts bogus keys are stripped — so the STOP-and-report rule for a genuine behavior change never fired); (f) rewriting the D-034 row — rejected (the decision log is append-only; D-045 records the advance instead) |
| D-046 | 2026-07-01 | K→Chief delegation + report-back — close the up/down chain (realizes D-023 delegation trigger, K-side) *(the automatic routing hop retired-by D-126; report-backs ride the mailbox since D-124 — the traceability links stand)* | **Wire the K→Chief delegation hop in `askK` (`core/src/k-thread.ts`).** When K's deterministic route escalates (the Chief, or a named discipline lead), K **delegates instead of running the message itself** — `startAgentRun('chief', { trigger:'delegation', goal })`, checked BEFORE the warm/cold branch so K's pure logistics/Q&A path is unchanged. The goal is the operator's ask verbatim + (for a named-lead route) the discipline hint so the Chief can `assign_lead`. **Report-back rides the run-lifecycle seam**: `reportDelegationBack` (`trackSupervisedRun`) appends a `k` turn to the durable K thread on the Chief run's terminal, summarizing the Chief's latest **mgmt `report`** (read run-scoped via the new read-only `mgmtDb.listReportsByRun`), falling back to the run's assistant text, then a status line. **Traceability needs no new table/column**: the delegation IS the `agent_runs.trigger='delegation'` row, and the parent→child link is recorded on the EXISTING `k_thread_turns.run_id` FK (K links the user turn + an acknowledgment turn to the Chief run), so the hop is derivable both directions. Rollback-on-throw holds (a dispatch failure propagates; the durable user turn stays). Scope is the K→Chief hop + report-back ONLY — Chief→lead `assign_lead` (P5.2a) and lead→PR stay downstream | This is exit-criterion #3: an engineering request must actually FLOW K→Chief and report back, not just preview a route. Everything needed already existed — `startAgentRun` with a `delegation` trigger (P5.0/D-023), the mgmt `report` tool (P5.2a), and the run-lifecycle seam (F2) — so the hop is pure wiring over proven seams, no new subsystem. Recording the link on `k_thread_turns.run_id` (already an FK to `runs`) instead of a new `parent_run_id` column keeps the schema flat and honors "derive from the event/store stream, no new table"; the child's `trigger='delegation'` marker + the turn's `run_id` make the chain derivable without one. Landing the outcome on the SAME durable thread the operator asked on — over the existing thread + event stream — means no bespoke report channel, and the self-wake guard already prevents the delegated Chief run's terminal from re-waking the Chief | (a) a dedicated `delegations`/`parent_run_id` table or column (rejected — the `agent_runs.trigger='delegation'` row + `k_thread_turns.run_id` FK already make the link derivable; a new table/column would duplicate + drift); (b) K running the message itself AND delegating (rejected — double dispatch + interleaved report turns; delegate-instead is simpler and matches "K, the friendly face, does no engineering"); (c) a bespoke report-back channel (rejected — reuse the thread + EventBus + mgmt `report`, no new seam); (d) reading K's own assistant events for the outcome as the primary source (rejected — the mgmt `report` is the deliberate "status up the chain"; assistant text is only the fallback); (e) wiring the full K→Chief→lead→PR loop in one commit (rejected — exceeds one reviewable brick; the Chief→lead dispatch + lead PR stay downstream follow-ups) |
| D-047 | 2026-07-01 | Named workflow definitions + Workflows UI + Settings org-default panel (P5.3b) *(completes the D-043 split; realizes the §04 planned `WorkflowDefinition` runtime record)* | **Land the persisted, operator-editable workflow TEMPLATE under a DISTINCT name and generalize the prompt builder behavior-preservingly.** A new `workflow_definitions` table + `NamedWorkflow` repo/CRUD (`core/src/db.ts` `workflowDefsDb` + `rowToNamedWorkflow`; registry/seed in `core/src/workflow-defs.ts`; `routes/workflows.ts` list/detail/PATCH) — named **`NamedWorkflow`**, NOT `WorkflowDefinition`, because the `@k/shared` `WorkflowDefinition` (roles+edges) is the existing read-only DIAGRAM type (D-043 item (f)); `NamedWorkflow` REUSES the existing `WorkflowRole` type. `core/src/workflows.ts::buildDelegationPrompt` is generalized into `renderWorkflowPrompt(scaffold, tasks)` (replaces a `{{CHECKLIST}}` token via a **function replacer** so `$`-bearing titles insert literally; `replaceAll` for multi-token operator edits) rendering an exported `CODE_WAVE_SCAFFOLD` that is the pre-P5.3b inline prompt **verbatim** — so `buildDelegationPrompt` output is **byte-identical** and the pre-existing `workflows.test.ts` / dispatch / delegation-determinism tests stay green (asserted directly with `.toBe`). Three seeds: `code-wave` (the lifted first def, roles = `DELEGATION_WORKFLOW.roles`), `investigate`, `refactor` (idempotent by unique name, mirroring `seedProfiles`). The **Workflows UI** turns `WorkflowsPage`'s Defined tab into a Definitions list + preview and adds `WorkflowDetailPage` (name/scaffold/cross-project editable, roles read-only; a duplicate-name rename maps the `UNIQUE` throw to `400`, never a 500). The **Settings org-default panel** reads/edits the `default-orchestrator` profile (skills/tools/MCP) via `GET`/`PATCH /api/org-default`, reusing `updateProfile`'s fail-closed grant guard EXACTLY like `routes/orchestrators.ts` (guard message → 400 row-unchanged; other throw → 500) — the org-default the per-lead overrides inherit from | The §04 model always planned a generalized named-definition mechanism; P5.3a shipped the control plane and explicitly deferred this half. Lifting the current loop verbatim into a seeded `code-wave` scaffold + a token-render function means the generalization is provably behavior-preserving (byte-identical prompt, existing tests untouched) rather than a risky rewrite. A distinct DB-entity name avoids the D-043 name clash while reusing `WorkflowRole` keeps the shape honest. The org-default panel reuses the SAME `updateProfile` grant guard the per-lead editor uses, so there is one fail-closed authority write path, and the "inherits org default unless overridden" language in the orchestrator detail finally has a real, editable source | (a) reusing the `WorkflowDefinition` name for the DB entity — rejected (collides with the diagram type, D-043 (f)); (b) a bespoke prompt rewrite instead of token-rendering a verbatim scaffold — rejected (can't prove byte-identical; risks the dispatch/determinism tests); (c) a second authority write path for the org default — rejected (bypasses the fail-closed grant guard; PATCH /api/org-default routes through `updateProfile` like the leads do); (d) mapping a duplicate-name rename to a raw 500 — rejected (a plausible operator action via the Rename form; the `UNIQUE` throw maps to a 400 with the row unchanged); (e) editing roles in the detail now — rejected (roles read-only this wave; scaffold/name/cross-project cover the operator need, role-graph editing is a later increment); (f) executing cross-project runs — rejected (the flag lands for an honest data model, execution stays one-agent/one-worktree per D-012/D-026) |
| D-048 | 2026-07-01 | Task-model unification — the storage COLLAPSE behind unchanged routes (P5.1d2a) *(realizes D-045's deferred collapse; advances D-026; over the D-034 kstore `work_items` table)* | **Fold the deprecated `project_tasks` store into the unified `work_items` table (`scope='project'`) — the storage half of the collapse, behind UNCHANGED public APIs.** `migrate()` adds `work_items.project_id` (a PLAIN FK, **no** `ON DELETE` action — matching the `runs.project_id` precedent) plus the issue-sync columns `completed_at` / `issue_number` / `issue_url` / `issue_state`, all via the S1-018 duplicate-tolerant `addColumn` helper (ALTER-only, exactly like `scope`), then an **idempotent, `hasTable`-guarded** `INSERT … SELECT` **backfills** every `project_tasks` row into `work_items` (`scope='project'`, `project_id` set, `run_id` NULL, status/`completed_at`/issue-linkage preserved; `NOT EXISTS` on the reused id so a re-boot never duplicates; `project_tasks` rows are COPIED not moved — a frozen safety snapshot). The `projectTasksDb` prepared statements are **RE-POINTED onto `work_items … scope='project'`** with IDENTICAL param names/positions and the exact old snake_case column projection, so `routes/projects.ts` + `github.ts::syncIssues` + `workflows.ts` operate on the ONE store **behind their unchanged public APIs** (their source is untouched). `deleteProject` deletes project-scoped `work_items` **before** the project row (the NO-ACTION FK would otherwise block it); personal items (`project_id` NULL) are untouched. Driven **characterization-first**: `core/test/p5-1d-characterization.test.ts` (the pinned contract of BOTH stores + `syncIssues`) stayed **GREEN and UNCHANGED** through the collapse; a new `p5-1d2-collapse.test.ts` pins the storage behavior (columns, backfill idempotency + metadata preservation, both `hasTable` guards, re-pointed helpers, delete-cleanup); and **7 white-box fixture tests** that coupled to the PHYSICAL `project_tasks` table via raw SQL were mechanically re-pointed to `work_items` **preserving their exact intent** — of which THREE (`campaign-s4-dispatch-lock-hygiene` S4-017 validate-before-mutate, `delete-routes` cascade, `campaign-s1-atomicity` cascade) were **FALSE-GREENS** under the collapse (still passing but no longer verifying their target) and were **STRENGTHENED** to seed via the helper / additionally assert `work_items` count 0. The collapse is **SPLIT**: **d2a (this)** = repo unification behind unchanged routes; **d2b (deferred)** = the direct route/sync reroute (retire the compat helpers), the scope-aware Tasks UI collapse, row **promotion** via `work_item_update`, and DROPPING `project_tasks` | The full collapse (schema + data migration + three call-site reroutes + a new scope-aware Tasks UI + row promotion) far exceeds one reviewable commit on the flagged **RISKIEST** surface. Splitting lands the behavior-preserving STORAGE unification first, with the characterization suite proving the observable route/sync contract is unchanged, so the surface reroute (d2b) then builds on a single physical store. Re-pointing the helpers (rather than a view+triggers, or an immediate code reroute) is the most transparent, reversible collapse that keeps every caller AND every characterization test green **without touching route/sync/UI code**; a plain NO-ACTION FK + explicit `deleteProject` cleanup avoids the ON-DELETE-in-ALTER uncertainty and matches the codebase's explicit-cleanup pattern; and copying (not moving) `project_tasks` leaves a reversible safety net. The 7 fixture re-points are the D-045 precedent (fixtures updated preserving exact intent when a store moves — NOT a genuine behavior change), so the STOP-and-report rule for a broken characterization contract never fired | (a) one-shot full collapse — rejected (exceeds one reviewable brick on the riskiest surface; forces route/sync/UI rewrite + migration in the same commit); (b) `project_tasks` as a VIEW over `work_items` with `INSTEAD OF` triggers — rejected (trigger subtlety + a destructive `DROP` in the riskiest wave, and it would break the `campaign-s1-migration-idempotency` fixtures that create+inspect a `project_tasks` table; re-pointing prepared statements is more transparent/reviewable); (c) rerouting `routes`/`syncIssues`/`workflows` to a work-item API directly now — rejected (that IS d2b; the `syncIssues` characterization reads `projectTasksDb` directly, so re-pointing keeps it green without touching sync code); (d) `ON DELETE CASCADE` on `work_items.project_id` via `ADD COLUMN` — rejected (ALTER-ON-DELETE uncertainty; explicit `deleteProject` cleanup is proven by `delete-routes`/`s1-atomicity`); (e) DELETE-ing `project_tasks` rows in the backfill — rejected (keep a frozen copy as a reversible safety net; the drop is a d2b step); (f) loosening any characterization or white-box test to absorb the collapse — rejected (characterization stayed green + unchanged; the 7 fixtures were re-pointed preserving intent and 3 false-greens were strengthened) |
| D-049 | 2026-07-01 | Autonomous execution loop — Chief→lead dispatch + report-back (loop-a, P5.4) *(realizes §03 D-023 downward hop; exit-criterion #3)* | **A dedicated `dispatch_lead` mgmt EXECUTION tool (`assign_lead`/`pick_workflow`/`scope_projects`/`report` stay STORAGE-only) makes the Chief autonomously hand an objective DOWN to an orchestrator lead.** `dispatch_lead` resolves the assignment's free-text `lead` to a lead profile (`chief-dispatch.ts::resolveLeadProfileId` — id/name/discipline-slug, gated by `isLeadProfile` so K/Chief/default-orchestrator can never be dispatched), resolves the assignment's chosen NamedWorkflow (default `code-wave`; `resolveLeadWorkflow`), **seeds the lead run prompt from that workflow's `promptScaffold` via `renderWorkflowPrompt` (the objective rendered as the single checklist item) + a lead charter line that mandates opening a PR**, and activates it with `startAgentRun(leadProfileId, { trigger:'delegation', goal, workflowId })`. The **parent→child link is a single nullable column, NO new table**: `mgmt_assignments.lead_run_id` (the lead run id; parent = `assignment.run_id`) + the lead activation's `agent_runs.trigger='delegation'` — the exact mirror of K→Chief's `k_thread_turns.run_id` + trigger (D-046). **Report-back rides the run-lifecycle seam downward**: `reportLeadOutcomeToChief` (`trackSupervisedRun`) files a mgmt `report` scoped to the Chief's run on the lead run's terminal, summarizing the lead's assistant text (capped) or a bare status line — the downward twin of `reportDelegationBack`. Guards double-dispatch (`lead_run_id` already set) and cross-run ownership; a dispatch throw leaves `lead_run_id` NULL (retryable) via the `startAgentRun` rollback. The mgmt store logic stays SDK-free; `mgmt-server.ts` now `await`s the handler (async-tool support). Scope is **loop-a** (the Chief→lead hop + report-back); **loop-b** (Chief→K report continuation/re-wake + the multi-tier org-tree DERIVATION render) is deferred to P5.6 | Everything the hop needs already existed — `startAgentRun` with a `delegation` trigger (D-037/D-023), the NamedWorkflow scaffolds + `renderWorkflowPrompt` (D-047), the mgmt store (D-040), and the run-lifecycle seam (F2) — so the loop is pure wiring over proven seams, mirroring the K→Chief precedent (D-046) one tier down. A dedicated execution tool keeps the D-040 storage/execution separation intact (the four storage tools and their run-scoping tests are untouched). Recording the link on one nullable `lead_run_id` column (guarded ALTER, like `agent_memory.profile_id`) keeps the delegation tree DB-derivable without a table that would drift/duplicate the run log. Filing the outcome into the Chief's OWN mgmt store (not a bespoke channel) means the Chief's next activation reads it where it already looks. Splitting loop-b off keeps this a single reviewable brick | (a) `assign_lead` itself dispatching — rejected (couples storage to execution, breaks D-040's inert-storage contract and its run-scoping tests); (b) a new `delegations`/`parent_run_id` edge table — rejected (the link is derivable from `assignment.lead_run_id` + `agent_runs.trigger='delegation'`; a table would duplicate + drift, same call as D-044/D-046); (c) dispatching from core on a poller/scheduler — rejected (the Chief must decide and invoke the hop via a tool, not have core auto-dispatch its assignments); (d) landing loop-b (Chief→K continuation + multi-tier tree render) in this commit — rejected (exceeds one reviewable brick; deferred to P5.6); (e) a bespoke lead→Chief report channel — rejected (reuse the mgmt `report` store + the run-lifecycle seam, no new seam) |
| D-050 | 2026-07-02 | Autonomous execution loop — main-process dispatch relay + boot reconcile (loop-b b1) *(closes D-049's deferred live-path gap; realizes §03 D-023 "the loop runs for real")* | **Decouple the Chief→lead `dispatch_lead` tool-call (RECORD) from EXECUTION via a DB-backed `lead_dispatches` intent queue drained by a MAIN-process relay.** loop-a's `dispatch_lead` ran inside the ephemeral per-Chief-run **stdio mgmt-server CHILD**, calling `startAgentRun` there — so a real lead run's `agent_runs` row stayed `running` forever (its `trackSupervisedRun` finalizer died with the child) and the lead→Chief report-back subscriber died too. Now `dispatch_lead` (still SDK-free, still in the child) only **records** a `pending` row into a new `lead_dispatches` queue (CREATE-only table, no ALTER; `chief_run_id`/`lead_run_id` FK→runs `ON DELETE SET NULL`, `assignment_id` FK `ON DELETE CASCADE`), resolving lead profile + workflow + seed prompt exactly as before and guarding double-dispatch **two ways** (assignment `lead_run_id` already set OR a live pending/dispatched intent) + cross-run ownership + no-lead-match. A new relay (`core/src/lead-dispatch-relay.ts`, wired at boot in `index.ts` with a stop-fn on `onClose`, mirroring `startChiefWake`; default ON, `LEAD_DISPATCH_RELAY=0` opts out; unref'd interval) polls the queue, **atomically claims** each row (`pending→dispatched` conditional UPDATE — an overlapping drain sees `changes===0` and skips, so exactly-once) + a `draining` re-entrancy latch, then performs `startAgentRun(leadProfileId,{trigger:'delegation',goal,workflowId})` **in the main process** so the tracking-row lifecycle AND `reportLeadOutcomeToChief` (re-subscribed on the **main** EventBus) finalize in a process that stays up. `drainLeadDispatches()` is the directly unit-testable seam (mirrors `scheduledChiefWake`). **Carry-forwards fixed:** the post-`startAgentRun` wiring (`setAssignmentLeadRun` + report-back) is an inner try/catch (partial-failure window — a LIVE lead run is never lost to a wiring throw); a `startAgentRun` failure marks the intent `failed`, leaves the assignment link NULL (retryable), degrades without crashing; a null `chief_run_id` logs a skip; `concatLeadAssistantText` reads a **bounded** `listAssistantEvents(runId, 50)` (`WHERE type='assistant' … LIMIT`) not the whole event log. **Boot safety-net:** additive `supervisor.ts::reconcileOrphanedActivations` finalizes `agent_runs` stuck `running` whose linked run is already terminal (done→`completed`, else `failed`), wired into `reconcileOnBoot` **before** the blanket `reconcileStaleActivations` so a mid-dispatch-COMPLETED lead becomes `completed`, not clobbered to `failed`; `reconcileStaleActivations`/`reconcileStaleRuns`/`startRun` are UNCHANGED (CRITICAL file, additive only). The Chief→lead **link derivation is unchanged** (assignment `run_id`+`lead_run_id`+`trigger='delegation'`, no new edge table — `lead_dispatches` is a transient execution queue). Verified with **mocked dispatch only** (no real tokens); the live Chief→lead→PR→report smoke is the conductor's gated milestone. **SPLIT:** b1 (this) = relay + boot reconcile + carry-forwards; **b2 (deferred)** = Chief→K report **continuation** (lead report flows UP through the Chief to the durable K thread) + the multi-tier org-tree DERIVATION render | D-049 explicitly flagged that binding the dispatch to the mgmt-server child was the one thing standing between the seams and a loop that actually runs; everything needed already existed (`startAgentRun`, the run-lifecycle seam, `reportLeadOutcomeToChief`, the boot-reconcile pattern), so the fix is pure wiring over proven seams. The child and main are **separate OS processes sharing one SQLite file**, so the hand-off MUST be DB-backed — an in-process EventBus can't cross the boundary; a durable queue also survives a child that dies mid-dispatch (the boot sweep then reconciles it). Recording intent in the child + executing in the main process keeps `dispatch_lead` SDK-free and its guards/UX (immediate no-lead-match rejection) intact while moving supervision to where it lives; the atomic claim + re-entrancy latch make exactly-once robust without a lock table; a brand-new CREATE-only table needs no migration; and keeping the reconcile additive (a new precise function ordered before the blanket one) means no existing run/reconcile behavior changes on the CRITICAL supervisor path | (a) executing the dispatch in the child (rejected — the whole bug: the child dies, orphaning the run + report-back); (b) an in-process EventBus "dispatch" signal (rejected — doesn't cross the child→main process boundary; only a shared-DB queue or a watcher does); (c) reusing `mgmt_assignments.lead_run_id` as the queue flag instead of a table (rejected — that column is the durable parent→child LINK; overloading it as an execution-state flag would conflate derivation with a transient queue and lose the resolved profile/workflow/goal the relay needs); (d) making the blanket `reconcileStaleActivations` also join `runs` (rejected — changes existing behavior on the CRITICAL supervisor path; a new additive precise function ordered before it is safer and more correct — done→completed not clobbered); (e) landing the Chief→K continuation + multi-tier render in this commit (rejected — exceeds one reviewable brick; b2); (f) a per-tick lock table for exactly-once (rejected — the atomic `pending→dispatched` CAS + a `draining` latch already guarantee it for the single main-process relay) |
| D-051 | 2026-07-02 | Autonomous execution loop — Chief→K report continuation + multi-tier org tree (loop-b b2) *(closes D-050's deferred b2; completes exit-criterion #3)* | **Complete the up-chain to K and RENDER the whole-org tree — both pure derivations over existing links, no new table.** Two gaps remained after loop-b b1. **(1) Chief→K report continuation.** `reportDelegationBack` (D-046) surfaces the Chief's outcome on the **Chief** run's terminal — but a Chief's bounded activation can end BEFORE the lead it dispatched finishes, so K could read a PRE-lead status. Fix: `k-thread.ts::continueLeadOutcomeToK(chiefRunId, leadRunId, lead)`, wired in `lead-dispatch-relay.ts` **right next to** `reportLeadOutcomeToChief` (the same lead-terminal signal on the **main** EventBus), rides the run-lifecycle seam (`trackSupervisedRun`, once-latched) and — IF the parent Chief run was itself a K delegation — appends the lead's outcome one hop UP onto the durable K thread ("Chief (via <lead>) completed: <capped assistant text>"), linked to the lead run. The K→Chief link is resolved with **no new table**: `kThreadsDb.getThreadIdByTurnRunId` reads the `k_thread_turns` row whose `run_id` = the Chief run (the exact link `delegateToChief` recorded on the operator's user + ack turns). A **no-op when the Chief woke autonomously** (chief-wake never touches `k_thread_turns` → no thread resolves). Deliberately independent of the lead→Chief report-back — each rides its own once-latched subscriber on the same terminal, mirroring `reportDelegationBack`'s shape; the K turn reuses the module's own capped `concatAssistantText`. **(2) Multi-tier org tree.** `web/src/lib/delegation.ts::fullOrgToDelegationTree` wraps the existing Chief subtree (`orgToDelegationTree`, UNCHANGED) under two ancestor tiers — **user → K → Chief → lead → sub-agent** — reusing the same generic `DelegationTree` component (arbitrary depth); ChiefPage renders it. The K→Chief edge is derived from the links, no new table: additive optional `ChiefOrgPayload.kDelegations` = `agentRunsDb.countAgentRunsByProfileAndTrigger('chief','delegation')` (every such chief activation is one K hand-up — `delegateToChief` is the only path that sets `trigger='delegation'`; autonomous wakes use `schedule`/`event`). K's node rides the chain (running while the Chief subtree is active); the user root is the operator anchor. Verified with **mocked dispatch** (no real tokens); gate green (core 1317 / web 407 / typecheck / build) + a headless screenshot of the whole-org tree on the Chief page | This is the last piece of exit-criterion #3 ("the result reports back up the chain — all visible in the org tree"): b1 made the loop RUN for real, but the lead's outcome still stopped at the Chief's mgmt store and the whole-org render was deferred. Everything needed already existed — the lead-terminal signal on the main EventBus (D-050), the run-lifecycle once-latch (F2), the `k_thread_turns.run_id` K→Chief link (D-046), the generic `DelegationTree` + pure builder (D-040) — so both halves are pure wiring/derivation, mirroring the precedents one tier up. Continuing the SAME lead terminal (a second once-latched subscriber next to the lead→Chief report) means the lead outcome reaches K without a bespoke channel or a Chief re-wake; the thread link already present makes the autonomous-vs-delegation discrimination free. Wrapping (not rewriting) `orgToDelegationTree` keeps the Chief subtree byte-for-byte and its tests green, and deriving the K tier from a COUNT of existing `trigger='delegation'` rows keeps the tree a view (no delegation table to drift) | (a) extending `reportLeadOutcomeToChief` in `chief-dispatch.ts` to also write the K turn — rejected (couples the pure downward-mirror module to the K thread + risks an import cycle; the K continuation belongs in `k-thread.ts`, wired at the relay's single observation point); (b) re-surfacing the lead report by RE-WAKING the Chief so `reportDelegationBack` re-reads its store — rejected (a wake storm + latency; the lead terminal is already observed on the main EventBus, so continue from THERE); (c) a new `parent_run_id`/`delegations` edge table or a `kDelegations` counter column — rejected (the `k_thread_turns.run_id` link + `trigger='delegation'` rows already make both the continuation target and the edge count derivable; a table/column would duplicate + drift, same call as D-044/046/049/050); (d) changing `orgToDelegationTree` to root at `user` — rejected (breaks the P5.2a Chief-root tests + the single-lead reuse; `fullOrgToDelegationTree` WRAPS it instead); (e) a required `kDelegations` field — rejected (an older payload would fail to build a tree; optional, defaulting to 0); (f) landing b2 as two commits (b2a continuation / b2b render) — considered per the split rule but the two halves are one cohesive derivation (~one reviewable brick: 3 core fns + 1 relay line + 1 db helper + 1 count + payload field + 1 web builder + ChiefPage), so kept as one commit | 
| D-052 | 2026-07-02 | Phase-5 (Agentic Org) close-out + merge | Merge `feat/k-org` → `main` **`--no-ff`** (one integration branch, 18 reviewable waves) after the whole-org SEAMS review (7 seams PASS · 0 blocker/major · 1 minor fixed pre-merge) + a full clean-tree gate (core 1317 / web 407 · build) + 3 operator-approved live smokes; defer d2b + the live PR run + the low nits as tracked non-breaking follow-ups | The seven cross-wave seams are coherent — authority is fail-closed, run-scoped kstore isolation holds (`scope != 'project'`), and the delegation chain links by `runs.id` at every hop; the relay mechanic is smoke-proven; `--no-ff` keeps the whole org program one revertable unit with per-wave history intact | Squash-merge (loses the per-wave reviewable history + the revert boundary); fixing d2b / the live PR before merge (non-breaking, token-gated — a follow-up, not a merge blocker); merging without the whole-org review (per-wave reviews ≠ a seams pass) |
| D-053 | 2026-07-02 | Durable operator-global stores (P5.7 A1) | **The `work_items.scope` enum becomes `'run' \| 'personal' \| 'org' \| 'project'` — the NEW `'run'` scope IS the old run-isolated semantics and stays the kstore default (lead working tickets unchanged); `'personal'`/`'org'` become DURABLE operator-global** (reads/updates filter by scope ONLY; `run_id` is still recorded on insert as provenance, never as an access filter) with a bearer-authed HTTP surface (`GET/POST/PATCH /api/k/work-items`, personal+org only — `run`/`project` rows unreachable there). A **one-shot, flag-guarded** migration (`mig_work_items_run_scope`; SQLite cannot alter a CHECK in place, so an old-CHECK DB is table-REBUILT copy→drop→rename with FKs toggled off, inside ONE IMMEDIATE auto-rollback transaction with an in-lock flag re-check) **re-stamps legacy `personal` AND `org` rows to `'run'`** — both were reachable via `work_item_create` pre-A1 and behaved identically run-scoped. Alongside: the **logistics store is de-run-scoped** (notes/events/reminders are operator-durable; run_id provenance only), the **mgmt store gains chief-READABLE `assignment_list`/`report_list`** (durable ACROSS Chief activations; writes keep run-scoped ownership), and **`agent_memory.profile_id` is now POPULATED** (`lesson_propose` resolves the calling run's profile via its `agent_runs` activation) **+ backfilled** one-shot (`mig_agent_memory_profile_backfill`) | A single operator's personal list, org objectives, logistics, and the Chief's own assignment history must SURVIVE the bounded run that wrote them — run-scoping was the right isolation for lead working tickets but the wrong lifetime for the secretary/manager stores; naming the old semantics `'run'` preserves every existing ticket's behavior while freeing `personal`/`org` for their D-026 meanings; the flag is exactly what makes the re-stamp a ONE-time migration (a plain UPDATE would clobber every future durable row on every boot), and re-stamping `org` too closes a silent-escalation hole (an untouched legacy `org` row would have surfaced in the durable operator view on the upgrade boot) | Profile-keyed scoping (rejected — single-operator reality: there is exactly one operator, so operator-global IS the owner; a profile key would partition data no one partitions); leaving legacy `org` rows unstamped (silent escalation into the durable view — caught in conductor review); an unflagged re-stamp (re-clobbers future durable rows every boot); a second personal-task table (the D-026 unification is the point) |
| D-054 | 2026-07-02 | Authority rows ENFORCED (P5.7 B1) *(supersedes the P5.0-era tier-assets-only synthesis)* | **`synthesizeConfigDir` now honors the `agent_profiles` authority rows — `allowed_tools`/`mcp_servers`/`skills` narrow WITHIN the tier assets, and the TIER is the CEILING.** An EMPTY column falls back to the tier asset (no behavior change for unedited profiles); a non-empty row is the operator's narrowed grant, ceiling-checked against the tier allowlist/mcp template/bundle. `authority.ts` gains `toolWithinCeiling`/`assertTierCeiling` (typed `GrantError`), enforced at `createProfile`/`updateProfile` — so a **PATCH granting above the tier ceiling is rejected 400** with the row unchanged — and validation is **fail-closed BEFORE any write** (an above-ceiling tool, an unknown MCP server, or an out-of-bundle skill throws with NO partial config dir left behind); the mounting≠granting guard (D-034) also holds at synth time over the EFFECTIVE values | The P5.0 synthesizer read tier assets only, so the P5.3a per-lead editors were **cosmetic** — a recorded narrowing was stored but never mounted, which is an authority-model lie; enforcing the row at the single synthesis choke-point (with the tier as a hard ceiling) makes the stored grant and the run's actual mount the same fact, and validate-before-mutate keeps a bad edit from bricking a run dir | Trusting the row without a ceiling (a DB write could escalate a tier); enforcing only at PATCH time (a hand-edited row would still mount); validating during the write (a rejected profile would leave a partial config dir); leaving the editors cosmetic (authority must be enforced, not advisory — D-021) |
| D-055 | 2026-07-02 | Project-scoped lead dispatch (P5.7 B1) | **The relay resolves `mgmt_assignments.projects` → the projects registry at EXECUTION time and passes `projectId` + `cwd` through `startAgentRun` into `startRun`** (`lead-dispatch-relay.ts::resolveDispatchScope`), so a scoped lead's worktree is created in THAT repo instead of K's own. Zero scoped names keeps today's K-repo behavior (`projectId` null); the FIRST name is authoritative (extras log a warning); an unresolvable name or a missing `localPath` fails the dispatch cleanly via the **status-guarded** `markLeadDispatchFailed` (assignment link stays NULL → retryable). Carry-forward hardening: any **post-claim throw now marks the intent `failed` immediately** instead of stranding it `'dispatched'` until the boot sweep | The Chief's `scope_projects` tool stored project names that the dispatch path silently ignored — every lead ran in K's own repo regardless of scope, making the scoping tool decorative; resolving at execution time (not record time) means a project registered/moved between the Chief's turn and the drain still resolves correctly, and failing closed on a missing path beats dispatching a lead into a nonexistent cwd | Resolving in the mgmt-server child at record time (stale by drain time; the child lacks the execution context); dispatching into K's repo on a bad path (a lead would "work" in the wrong tree); leaving a post-claim throw stranded until boot (the review-caught strand window — degrade-to-failed in the drain instead) |
| D-056 | 2026-07-02 | Runtime model default reconciled (P5.7 B1) *(corrigendum on D-038)* | **`agent_profiles.default_model` becomes nullable-in-meaning: the `''` sentinel = "use the runtime `claudeDefaultModel()`" resolved at DISPATCH time; seeds no longer freeze a literal.** A one-shot flag-guarded migration (`migration.agentProfileModelReset.v1`) un-freezes rows still carrying the historical `'claude-sonnet-4-6'` literal (a differing operator override survives; a later explicit re-pin is never wiped). Precedence: a **per-ask model override (`KAskBody.model`, validated against the known-model registry) wins over the profile override wins over the runtime default**; the orchestrator/org-default PATCH surfaces validate via `isKnownModel` (`''`/null clears) and detail exposes `effectiveModel {model, source}` so the UI renders honestly. **Corrigendum: D-038's claim that the per-profile `default_model` reconciled with the runtime global default at P5.5 integration was INCORRECT — the seeds kept freezing the literal and profile dispatch bypassed the runtime store until this wave** | The operator's Settings model choice silently did not apply to K/Chief/lead runs — every seeded profile pinned the frozen literal, exactly the env-frozen-const problem D-038 set out to kill, one layer up; storage keeps TEXT NOT NULL with `''` as the no-override sentinel (mapped in `rowToAgentProfile`) because a nullable rewrite would force an FK-laden table rebuild for zero semantic gain | Rebuilding the table for a NULL column (FK-laden rebuild vs. a mapped sentinel); a recurring reset (would wipe an operator's deliberate re-pin — hence one-shot); leaving the frozen seeds (the runtime default stays a lie for org runs); skipping the corrigendum (the log must not silently absorb a false prior claim) |
| D-057 | 2026-07-02 | Chief wake governor (P5.7 B2) *(the classifier-as-the-server's-delegation-decision clause superseded-by D-126 — the classifier is a Dock preview only)* | **Event wakes are code-governed, not prompt-governed.** (a) **Org-relevance filter**: only a terminal run owned by a lead (orchestrator-tier profile) or a `trigger='delegation'` activation wakes the Chief — plain runs, evals, skills, and K-chat terminals never do; (b) a **rolling-hour rate breaker** from `app_config` **`chief_wake_max_per_hour` (default 6)** — a suppressed wake creates NO ledger row (warn once per suppression streak); (c) a **kill switch `chief_wake_events_enabled`** ('1' default) gating the event path only, cron unaffected. Alongside, **K's routing gives logistics-intent messages PRECEDENCE over embedded engineering keywords** (`routeForMessage` runs the logistics rules first — "remind me to fix the fence" stays with K), with **under-escalation chosen as the cheap failure**; and the classifier is now documented as the server's actual delegation decision (`askK` delegates on `route.escalates`), not merely a preview | Every wake is a PAID Chief activation that can itself dispatch a lead — the wake→dispatch→lead-terminal→wake cycle had no hard cost bound and any terminal run (including K chat) could trigger it; filtering to org-relevant terminals + a rolling-hour cap + an operator kill switch bounds the spend by code; on routing, a false escalation spins up the paid Chief machinery for a grocery note while under-escalation is a cheap re-ask, so the precedence trade-off goes to logistics | Prompt-only wake discipline (unenforceable — the D-021 argument again); a wake ledger row per suppression (noise that dwarfs the real history); capping the cron path too (the schedule tick is the deliberate heartbeat); engineering-keyword precedence in routing (auto-escalates "remind me to fix the fence" — the expensive failure direction) |
| D-058 | 2026-07-02 | Task-store collapse COMPLETED (P5.7 A2 = the deferred d2b) | **`project_tasks` is DROPPED after a final one-shot backfill** (`mig_project_tasks_drop`: one last NOT-EXISTS `INSERT…SELECT` into `work_items(scope='project')`, then `DROP TABLE`, app_config-flagged, IMMEDIATE transaction with in-lock re-check) — **fixing the boot-resurrection bug**: d2a's recurring backfill re-created any pre-d2a task deleted via the API from the frozen copy on every boot. The compat helpers are renamed **`projectTasksDb` → `projectWorkItemsDb`** — now the FIRST-CLASS project-scoped surface of the unified store (`routes/projects.ts` + `syncIssues` ride it with **zero HTTP shape change**; `tasks-route` characterization stays byte-identical). A **partial UNIQUE index `(project_id, issue_number) WHERE scope='project' AND issue_number IS NOT NULL`** (dedupe first: newest `created_at`, rowid-DESC tiebreak) closes the syncIssues double-mirror hole, and **`updateProjectTaskFromIssue` binds `project_id` in its WHERE** (cross-project defense). Bonus degrade: the GitHub poller **skips projects whose `localPath` vanished** (one warn per boot; additive `Project.pathMissing`) instead of erroring every 60s | The d2a "frozen safety copy" turned out to be an active hazard, not a net: deletes silently un-happened at every boot and a resurrected issue row could duplicate against a re-synced one (the old index was non-unique); one final backfill + drop makes deletes durable permanently, and the unique index makes issue-mirroring idempotent by construction; keeping the helper layer (renamed, no longer a shim) preserves the proven route/sync contract with zero API churn | Keeping the frozen copy + a tombstone table for deletes (a second store to drift — the copy WAS the bug); rerouting routes/sync onto new statements in the same wave (churns the characterization-pinned surface for no behavior gain); a full-table UNIQUE (NULL-issue and non-project rows must stay exempt — hence partial); leaving the poller ENOENT spam (a vanished path is a state, not a per-minute error) |
| D-059 | 2026-07-02 | `migrate()` version gate (P5.7 B2) | **`migrate()` gains a `PRAGMA user_version` fast path**: `SCHEMA_VERSION` (1 at introduction; bumped as migrations land — see D-061) is stamped only AFTER a successful full guarded-ALTER/backfill scan, and a connection whose DB already carries the stamp returns immediately — so the **per-run stdio MCP children (up to 3 per K/Chief turn) skip the full scan**. The **bump-on-new-migration discipline is documented in `db.ts`** at the constant: add a migration to `migrateSlow()` → bump `SCHEMA_VERSION`, and an older-stamped DB re-runs the scan and is re-stamped. Per-step idempotency (hasColumn/hasTable guards, duplicate-column tolerance, one-shot flags) stays as belt-and-suspenders for the slow path and for concurrent first-boots racing before the stamp lands | The migration scan grew heavy (table rebuilds, backfills, window-function dedupes) and runs on EVERY connection open — the main boot AND each MCP child — so K/Chief turns paid the full scan repeatedly for a DB that was already current; the pragma lives in the DB file, so one successful migration stamps it for every later connection; stamping only on success means a failed migration retries on the next open instead of being masked | Stamping before the scan completes (a failed migration would never retry); a version table instead of the pragma (an extra read + the pragma is the SQLite idiom); dropping the per-step guards once gated (the slow path still races on concurrent first boots — belt-and-suspenders stays); leaving the scan ungated (per-child cost grows with every future migration) |
| D-060 | 2026-07-02 | Lead-dispatch intents retire by LIVENESS DERIVATION, not a new status (P5.7 C2 conductor blocker) | **`getActiveLeadDispatchByAssignment` derives "active" via a LEFT JOIN on `runs`**: `'pending'` is always active; `'dispatched'` is active only while genuinely in flight — the claim window (`lead_run_id` NULL, blocking fail-safe until the boot sweep) OR its lead run still non-terminal. A completed intent is **retired-by-derivation** with no status write. `dispatch_lead`'s guard 1 is liveness-derived too (blocks only a LIVE linked run via `isTerminalRunStatus`), which **unwedges Chief re-dispatch of completed assignments AND the new operator reassign** — `PATCH /api/chief/assignments/:id` (IMMEDIATE transaction; 409 while the lead run is live or a dispatch intent is in flight, 400 same-lead; clears the stale `lead_run_id`; files a durable mgmt audit report) | `lead_dispatches` has NO success-terminal status (CHECK `pending\|dispatched\|failed`) and the relay deliberately leaves a completed intent `'dispatched'` forever — but the old statement counted every `'dispatched'` row as active, so ONE successful dispatch wedged its assignment permanently (reassign 409'd forever; a follow-up `dispatch_lead` wave was impossible). Deriving liveness from the linked run matches the org-tree philosophy — the runs table is already the terminal-status truth, so the queue never needs a writer to keep a success status in sync | A `'completed'` status on the queue (rejected — SQLite CHECK change needs a table rebuild, AND it needs a run-terminal subscriber writing it forever, which can drift from the runs truth the way this bug did); clearing `lead_run_id` on terminal (destroys the durable parent→child link the org tree derives from); loosening the run-less-orphan case (it must keep blocking fail-safe — only the boot sweep can prove the run never spawned) |
| D-061 | 2026-07-02 | Hard tool ceiling at the CLI + as-built chief charter + seed-row resync (P5.7 L-fix; from the failed live chain proof) | **The tier tool ceiling is now ENFORCED at the CLI via `--disallowedTools`**: `CAPABILITY_TOOL_UNIVERSE` (Bash · PowerShell · Write · Edit · NotebookEdit · Task · Agent · WebFetch · WebSearch) is the authority-bearing built-in set; every managed run's denylist = the universe minus its granted tools (`authority.ts::computeDisallowedTools`; `Task`/`Agent` treated as ONE spawn capability under two CLI names), emitted after `--allowedTools` (empty ⇒ no flag, mirroring the dangling-flag guard). PowerShell + NotebookEdit join the orchestrator allowlist so leads lose nothing. **`chief.charter.md` rewritten from its "PLANNED" stub to the operating procedure** (`assignment_list`/`report_list` on wake; `assign_lead` → `scope_projects` → `pick_workflow` → `dispatch_lead` records an INTENT the main process executes; `report`; honesty rule: missing/failing tool → report + stop, never engineer) with content-pin tests on the procedure order + clauses. **One-shot `mig_seed_profile_authority_resync`** overwrites the 8 durable seed rows' authority arrays from `resolveAuthority(charter)` (operator-created profiles untouched; `default_model` untouched) — and forces the FIRST real **`SCHEMA_VERSION` bump (1→2)** under D-059's discipline | The live K→Chief→lead→PR proof failed at the Chief hop: `--allowedTools` is only an AUTO-APPROVAL list and `--permission-mode acceptEdits` auto-approved Write/Edit for the no-code chief tier, so the Chief — whose stale "PLANNED" charter gave it no tool procedure — improvised: spawned an inline `Agent` subagent and WROTE FILES in its worktree, with only headless permission denial stopping the git/PR steps. Separately, upgraded DBs still carried pre-B1 seed rows (the real DB's chief lacked `mcp__mgmt`), so B1's row enforcement would have stripped the Chief's management server on exactly the DBs that upgraded | Prompt-only discipline (the incident IS the counter-example — D-021 again); switching tiers to permission-mode `default` (breaks every autonomous run for one tier's ceiling); denying the whole tool namespace (Skill/TodoWrite/etc. are conveniences, not capabilities — denying them regresses leads for zero authority gain); a recurring resync (would clobber operator customizations now that the editors are real — hence one-shot); leaving stale seed rows to hand-editing (the upgrade path must be code, not operator surgery) |
| D-062 | 2026-07-04 | K secretary lifecycle — resumable one-shot runs *(supersedes the D-023/D-042 warm/fresh hybrid FOR K; the one-shot-ONLY posture superseded-by D-122 — resume stays as the session layer's cold path)* | **A K ask is a RESUMABLE ONE-SHOT run, not a parked interactive process.** Each K thread owns a stable Claude CLI session — `--session-id <uuid>` on the FIRST ask (seeded with the full `renderSeed` transcript), `--resume <id>` on every LATER ask sending **only the new message** — plus a stable per-thread config dir + cwd under `<dataDir>/k-secretary/<threadId>/` (**no worktree** — K writes no code). A cold ask no longer re-pays the ~24k system-prompt + MCP-tool-schema envelope or replays the 12-turn transcript; continuity is a cheap cache-read of the resumed CLI session, while the durable `k_threads`/`k_thread_turns` stays the **source of truth**. The run ANSWERS AND EXITS (`done`) — it never parks at `awaiting_input` holding a live process + worktree (fixes F-054 cost/park; subsumes H10 self-terminate). `cli_session_id` persists ONLY on the first ask's successful `done` (`SCHEMA_VERSION 5→6`); undo (`undoK` / `POST /api/k/undo`) removes the dangling user turn so an undone ask is never replayed (guarded against a late kill-flush resurrecting it). **GATED to `k-secretary`:** regular dispatch runs are byte-for-byte unchanged (fresh worktree, ephemeral config, interactive HITL park). `askK`'s `warm` flag is kept for API compatibility but is now always false; the `awaiting_input` branch remains only as belt-and-suspenders for any interactive reuse | The D-023/D-042 hybrid kept a warm interactive process while chatting and re-seeded a fresh run when cold — but a parked K run held a live CLI process + worktree indefinitely (the F-054 cost/leak, the H10 self-terminate), and each cold re-seed re-paid the whole prompt/tool envelope AND replayed the transcript (a cost explosion on every ask). A resumable one-shot gets continuity AND boundedness for free: the CLI's own resumed session carries context cheaply, the durable thread survives reload/reset, and the run exits instead of squatting a process. Only K's lifetime changed — regular dispatch keeps interactive parking | Holding a warm interactive K process (the D-023/D-042 model — the leak/cost bug this fixes); re-seeding a fresh run per cold ask (re-pays the envelope + replays the transcript — the cost explosion); giving K a worktree (it writes no code); dropping the durable thread in favor of the CLI session (the session is a cache, not the truth — reload/reset must recover from the thread); parking at `awaiting_input` (a one-shot has a single terminal boundary — the branch stays only for interactive reuse) |
| D-063 | 2026-07-04 | Org lead roster + fail-fast lead naming — unblock the autonomous K→Chief→lead chain | **Give the Chief the authoritative lead roster and make lead-naming fail-fast at ASSIGN time.** A new read-only `lead_list` mgmt tool returns the dispatchable leads (id · name · discipline · one-line role); the AUTO paths — the Chief's wake-goal seed, its no-hint delegation seed, and its charter — INJECT the five lead identifiers so the Chief always references a real lead and never invents a discipline like "engineering". `assign_lead` now VALIDATES the lead via `resolveLeadProfileId` — the SAME resolver `dispatch_lead` uses (by id, then name, then discipline slug, gated by `isLeadProfile`) — and REJECTS an unknown lead at assign time, so the two can never disagree. A project-scoped lead dispatch now BINDS a `workflow_runs` row + seeds its steps from the delegation workflow, so the lead's kstore status-write tools (`workflow_step_set`/`workflow_status_set`) RESOLVE against a real checklist; checklist seeding is isolated so a failure degrades to no-checklist without dropping the Chief/K report-back, and `finalizeWorkflowRun` reconciles lingering steps to `blocked` so the checklist can't contradict a completed run. (F-067/F-070/F-072/F-081) | The live K→Chief→lead chain died at the lead-naming hop: the Chief, with no roster in its seed, invented a discipline that `assign_lead` ACCEPTED (storage) but `dispatch_lead` later REJECTED, so the objective stranded as a dangling assignment. Handing the Chief the authoritative roster + making `assign_lead` fail the same way `dispatch_lead` does makes the accept and the dispatch agree; binding a `workflow_runs` checklist gives the dispatched lead a real progress surface the status-write tools resolve against instead of improvising | Leaving `assign_lead` permissive (the accepted-then-dangling assignment — the exact stall this fixes); a second, divergent resolver for assign vs. dispatch (they MUST agree — one `resolveLeadProfileId`); the lead improvising with no `workflow_runs` row (the status-write tools no-op with nothing to resolve); failing the whole dispatch when checklist seeding throws (degrade to no-checklist, keep the report-back) |
| D-064 | 2026-07-04 | Health score PRORATES over measured dimensions — no phantom 100 | **`computeHealthScore` prorates over only the dimensions it could actually measure:** `score = round( Σ earned(measured) / Σ weight(measured) · 100 )`. An UNMEASURED dimension is excluded from BOTH numerator and denominator — it neither helps nor hurts, never scored 0 nor full-credit. If NOTHING is measured the score is **null** (`insufficient signal`), NOT 100 — a freshly-onboarded project no longer reads 100/100 off scaffold files that never ran. The **bible** dimension is scored on **AUTHORED content** (the `BIBLE_SCAFFOLD_MARKER` sentinel: a bare scaffold is unmeasured; an authored bible — even uncommitted, per D-028 — is full marks), NOT git commit history, so an authored-but-uncommitted bible isn't punished. A MEASURED problem still demerits its own dimension exactly as before. `verification_reports.score` + `score_breakdown` become NULLABLE (a table rebuild, `SCHEMA_VERSION 4→5`), null-safe across the API and every web consumer. (F-032) | The old fixed-weight `40·CI + 20·coverage + 20·bible + 20·findings` formula credited a bare project for dimensions that carried no real signal (an unmeasured factor scored as neutral full marks), so a scaffold-only project read ~100/100 — a health lie that made a never-run project look healthier than a real one with findings. Prorating over measured dimensions makes the score reflect only real evidence; a zero-signal project is honestly `null` (insufficient signal) rather than a fabricated 100 | Scoring an unmeasured dimension 0 (punishes a project for not emitting a signal — e.g. no coverage instrumentation); scoring it neutral/full (the phantom-100 bug); keeping `score` NOT NULL (can't represent "insufficient signal" — hence the nullable rebuild); scoring the bible on commit freshness (punishes an authored-but-uncommitted bible, which D-028 makes the norm for K's own gitignored bible) |
| D-065 | 2026-07-04 | A bible is edited per-SECTION with source-of-truth writeback *(refines D-002)* | **A project/harness bible is edited per SECTION, and Save writes back to the section SOURCE.** `PUT /api/artifacts/:slug/sections/:sectionSlug` (`saveBibleSection`) writes the edited body back to its canonical `<bibleDir>/sections/<slug>.md` source (frontmatter preserved via a lossless `splitFrontmatter`) THEN recompiles — so the edit IS the source and survives every recompile. The old whole-markdown PUT against a bible slug is REJECTED (`400`, steering the caller to the section editor), closing the silent-data-loss path (F-029, HIGH). Path-traversal-safe (`SLUG_RE` + manifest allowlist + `isPathWithin`). A project bible save no longer writes `project-<id>-bible.*` into K's OWN `ARTIFACTS_DIR` or clobbers the row's `html_path` — everything stays in the project's own dir (F-030); a project's Artifacts tab lists only its own `project-<id>-*` artifacts, not the harness globals (F-038) | Under D-002 a bible compiles from section sources to one HTML; but editing and saving the COMBINED markdown (or the compiled HTML) was silently discarded on the next recompile — a real data-loss path. Writing the edit back to the exact section source the compiler reads makes the round-trip lossless; scoping the write to the project's own bible dir stops a project edit from leaking into K's artifacts and clobbering the row's html_path | Editing/saving the combined bible markdown or compiled HTML (lost on recompile — the data-loss bug); writing project bibles into K's `ARTIFACTS_DIR` (cross-repo leak + `html_path` clobber); a free-form section path (traversal — hence `SLUG_RE` + manifest allowlist + `isPathWithin`) |
| D-066 | 2026-07-04 | Terminal env scrub + prod-token exclusion + surfaced/opt-out credential posture | **Three defense-in-depth hardenings around the browser terminal + host credentials.** (a) The terminal's `node-pty` no longer inherits `process.env` — it runs a SCRUBBED env (denylist: vendor prefixes + `*_TOKEN`/`*_KEY`/`*_SECRET`/`*_PASSWORD` + connection-string `*_URL`/`*_URI`/`*_DSN`/`*_PASS`/`*_PWD`/`CONNECTION`; `PATH`/`HOME`/`PWD`/etc. survive), so a leaked/guessed terminal token can't `echo $ANTHROPIC_API_KEY`. (b) `VITE_TERMINAL_TOKEN` and `VITE_HARNESS_TOKEN` are emitted `undefined` in a PROD bundle (the dev literal only under `vite serve`) and `TerminalPage`'s fallback is dev-gated — prod fails CLOSED instead of shipping `dev-terminal-token`/`dev-token-change-me`. (c) A non-loopback + `ENABLE_TERMINAL` bind now REFUSES to boot without an explicit `TERMINAL_ALLOW_REMOTE=true`, even with a strong `TERMINAL_TOKEN` (was only refusing weak tokens). Plus the **credential posture** (`managed` \| `host-fallback` \| `disabled`) is SURFACED on `/api/status` + Settings + a boot summary (never the secret), so silent host-credential use is visible, and `K_DISABLE_HOST_CREDENTIAL_FALLBACK` opts a deployment OUT (fail-closed to unauthenticated). DEFAULT (no flag) is byte-identical — OOTB dogfooding unbroken. (F-084/F-064/F-090) | The terminal ran with the full process env, so a guessed/leaked terminal token was a full credential-exfil channel; a prod build baked the dev terminal/harness tokens into client JS; a non-loopback terminal bind exposed a shell on the LAN behind only a token; and the D-027 host-credential dogfooding fallback copied host creds SILENTLY. Each is closed without moving the OOTB default — the scrub is additive, the prod-token exclusion only affects release builds, the remote-terminal gate is opt-in, and the posture is observable + opt-out-able | Trusting the terminal token alone (one leak = full env exfil — hence the scrub); shipping the dev token literal in prod (fail closed instead); allowing a non-loopback terminal on a strong token alone (an accidental LAN bind exposes a shell — require `TERMINAL_ALLOW_REMOTE`); turning the host-fallback OFF by default (breaks OOTB dogfooding — make it VISIBLE + opt-out instead) |
| D-067 | 2026-07-04 | Opt-in carry-working-tree dispatch mode | **An opt-in `carryWorkingTree` flag lets a dispatched run see the source repo's uncommitted work.** Threaded `POST /api/runs` → `startRun` → `startAgentRun` (boolean-validated); when set, the run's worktree is seeded with the SOURCE repo's uncommitted **tracked + staged** changes via a non-destructive `git stash create` + apply (**untracked NOT carried** — documented; `stash push -u` avoided as it mutates the source). The DEFAULT (no flag) is BYTE-IDENTICAL — `worktree add --detach HEAD` unchanged, carry unreachable without the flag — locked by real-git tests incl. the persistent-session no-op interaction | A dispatched run always started from a clean `HEAD`, so it could not see in-progress local edits — sometimes exactly the state you want an agent to start from. An opt-in carry lets a run observe the uncommitted tracked+staged tree without touching the default clean-worktree contract or mutating the source repo | Carrying by default (changes the clean-worktree contract every run relies on); `git stash push -u` (mutates the source working tree — used the non-destructive `stash create`/apply); carrying untracked files (broader, unpredictable blast radius — tracked+staged only, documented) |
| D-068 | 2026-07-04 | Post-final-review follow-up hardenings — org-model warn, killed-run cost honesty, `/end` relay-run termination, undo CLI-session scrub, skill-source realpath | **Five hardenings landed after the whole-implementation FINAL REVIEW (3 read-only agents — correctness / security / tests+docs — verdict STANDARDS HELD · SECURE · SOUND), each its own reviewable commit with a real regression test.** (a) **WARN-ONLY org model capability:** `isOrgToolCapableModel` / `warnIfOrgRoleModelNotToolCapable` emit ONE clear warning at the Chief (`wakeChief`) and lead (`drainLeadDispatches`) dispatch choke-points when the role resolves to a haiku-tier model (which can't reliably drive the deferred MCP management tools, silently stalling the K→Chief→lead chain) — but still dispatch EXACTLY as configured (no upgrade, no refuse). (b) **KILLED-RUN COST HONESTY:** a run killed mid-turn never emits the authoritative turn-end `result` usage, so its 0s are UNMEASURED, not free — `accumulateInterimUsage` recovers per-`assistant` interim tokens (OUTPUT summed across messages, INPUT the conservative MAX so recovery can only under-count, never over-bill) via `reconcileKilledUsage` ONLY when no `result` was ever summed (never double-counts a completed turn); cost stays honestly unrecoverable (no interim cost signal); `isUnmeasuredKilledRun` keeps a genuinely-unmeasured killed run (0/0/$0) COUNTED as a run but OUT of the measured cost/token aggregates so it never reads as a $0 that drags an average. (c) **`/end` flips a relay-dispatched LEAD run** (was a no-op for non-interactive runs) by routing a non-interactive live proc through the same `kill()` path `/kill` uses → honest `killed` terminal; the interactive graceful-EOF path stays byte-identical (change scoped to `!interactive`). (d) **`undoK` nulls the owning thread's `cli_session_id`** (keyed on thread PK, resolved BEFORE the turn-delete) so an undone RESUME ask can't survive in K's live `claude --resume` session — closes the F-054/F-060 taint on the common fast-one-shot-then-undo path (active_run_id is already NULL by then). (e) **`readSkillSource` realpath gate:** a fail-safe `fs.realpathSync` check so a symlink planted inside `agent-config/skills` can't read THROUGH the F-069 confinement. | The final review confirmed the 92-finding campaign did NOT diminish standards, but surfaced these five: one MEDIUM undo-session gap (undo removed durable turns but not the resumable CLI session), the three live-smoke follow-ups (haiku-Chief stall, killed-run $0, `/end` relay no-op), and a LOW skill-confinement symlink gap. Each strengthens an existing HIGH redesign or closes an honest accounting/termination gap WITHOUT changing default behavior | (a) auto-upgrade to sonnet, or refuse the dispatch — both override operator intent; chose warn-only. (b) make `Run` cost/token fields nullable (shared-type ripple — kept honesty inside supervisor+metrics) or fabricate a cost estimate (rejected — no interim cost signal, would over-bill). (c) invent a distinct `ended` status (no graceful path exists for a non-PTY proc — `killed` is honest). (d) a new `cli_session_run_id` stamp column + keyed clear (schema 6→7 — thread-PK clear is simpler and errs safe by re-seeding). (e) also case-normalize `isPathWithin` on Windows (shared primitive, already fails safe — left a tracked follow-up rather than destabilize it pre-merge) |
| D-069 | 2026-07-05 | Guarded host discovery + K-scoped capability overlay *(amends D-027/D-029 — isolation widened deliberately, never accidentally)* | **K discovers the host Claude Code layer under guard and catalogs it behind a K-scoped enable overlay.** Four scanners (`host-discovery.ts`) read host user skills (`~/.claude/skills`), each REGISTERED project's `.claude/skills`, plugin skills via `installed_plugins.json` (each `installPath` realpath-VERIFIED under `~/.claude/plugins/cache` — a poisoned index cannot point discovery at arbitrary disk), and host MCP configs (`~/.claude.json` user+project scopes — win32 case-insensitive project matching — plus project `.mcp.json`, which takes precedence; non-stdio servers skipped v1 with a warning). Discovered skills land as rows in the EXISTING `skills` table with provenance columns (`source_kind`, `qualified_key` UNIQUE — ONE canonical grammar, wire id == key: bare k-native · `user:<name>` · `project:<projectId>:<name>` · `plugin:<plugin>@<marketplace>:<name>` — `origin_path`, `content_hash`, `est_tokens`/`est_tokens_meta`, `status ok\|missing`), ALWAYS default-DISABLED. Enable/disable is a **K-scoped overlay — host files are never mutated**; rescan (guarded boot scan + `POST /api/capabilities/rescan`) upserts by qualified key, PRESERVES the overlay, flags vanished assets `missing` (fail-closed at PATCH and synth), auto-disables an MCP server on config drift. An enabled asset is **vendor-copied into the D3 run config at synth** (`copyDirConfined`: source-side lstat symlink-skip, per-file realpath containment, 500-file/10MB hard caps) — a run never reads host dirs live; every read confines via `skill-roots.ts` (an explicit allowlisted-roots set × the two-gate string+realpath check per root, extending F-069). Mount collisions: k-native wins the bare name; discovered mounts `<name>--<sourceKind>` | D-027 made isolation the invariant, but the operator's host layer already holds real capability (plugins, skills, MCP) K couldn't see; guarded discovery makes K INCLUSIVE without surrendering the boundary — the catalog is a metadata snapshot, enablement is operator-scoped, and host content still enters a run only through the same vendoring gate as K's own assets | Mounting host dirs live into runs (re-opens the exact D-027 leak — vendor-copy instead); a separate discovered-skills table (two catalogs drift — one table, one key grammar); auto-enabling discovered assets (prompt-injection-by-default — everything lands disabled, preview-before-enable); storing enable state in host `~/.claude` (K never writes the host layer) |
| D-070 | 2026-07-05 | Discovered-MCP trust gating — default-disabled, hash pinning, synth re-verify | **A discovered host MCP server is code execution by design, so it is gated twice: operator TRUST, then ENABLE.** New `host_mcp_servers` table (command/args/env, `config_hash`, `enabled` default 0, `trusted_hash`/`trusted_at`, `probe_status`, `status`). Trust pins `trusted_hash = config_hash` after the operator reviews the command/args/env **NAMES** in the TrustDialog (env **VALUES** never leave core — no API payload carries them); **enable requires trust**; config drift on rescan **auto-disables + clears trust** + clears the probed `est_tokens`; and synthesis **re-reads + re-hashes the live host config at resolve time** — a mismatch throws a fail-closed `GrantError` (closes the TOCTOU window between trust and dispatch). The token/tool-count **probe** (a real MCP `initialize` + `tools/list` via `@modelcontextprotocol/sdk`, 15s hard timeout) runs ONLY on explicit request against enabled+trusted servers; a probe failure answers `502` + `probe_status='failed'` and NEVER disables. **K-reserved server names** (`kstore`/`logistics`/`mgmt`/`gitnexus`) are refused at admission — a host server cannot impersonate a K server | An MCP server's config is an arbitrary command K would execute inside runs; default-disabled + review-then-pin + drift auto-disable + synth re-hash means the exact bytes the operator reviewed are the only bytes that ever launch, and a host-side edit after trust silently REVOKES rather than silently escalates | Trust-on-enable in one click (skips the review — the operator must SEE the command first); hashing at trust time only (TOCTOU — the host config can change between trust and dispatch, hence the synth re-verify); auto-probing at scan time (spawns untrusted processes unprompted); disabling on probe failure (a flaky server would flap the operator's enablement — probe failure is a status, not a revocation) |
| D-071 | 2026-07-05 | Skill Creator saves exclusively to K's library | **The Skill Creator's drafts lifecycle lands skills ONLY in K's own library (`agent-config/skills/`), provenance `'k'`.** A `skill_drafts` table + `/api/skill-creator/drafts` CRUD drive: brief → **authoring run** (dispatched under the fail-closed `k-secretary` profile — a missing profile fails the draft rather than silently escalating to the orchestrator default; the authoring-guidance skill `agent-config/skills/skill-authoring` is EMBEDDED in the prompt, since runs mount skills via tier bundles only) → `ready\|failed` → manual edit / **refine** revisions (a refine on a failed FIRST draft is a fresh retry at revision 0) → **evaluate** (the SAME eval harness skills use, over a parallel `skill_draft_evals` table — cascade-deleted with its draft) → **save**: `agent-config/skills/<slug>/SKILL.md` (slug guarded incl. Windows reserved device names; `409` on collision with a library dir or registered key; crash-honest rollback removes a half-written dir) + a catalog row registered with provenance `'k'`. The UI stays honest — "agent-generated draft — not saved" until `/save` lands it | The Creator must grow K's OWN versioned library, not the host layer (which K never mutates — D-069) and not a project repo (a skill is org capability, not project code); authoring under the secretary tier keeps the draft run codeless within its charter, and reusing the eval harness judges a draft by the same bar as a live skill | Saving to host `~/.claude/skills` (violates never-mutate-host); a bespoke authoring runtime (the existing dispatch+track seam suffices); widening `skill_evals` with a nullable draft FK (its `skillId` is NOT-NULL FK'd — the parallel minimal table is the honest shape); auto-registering a draft as a skill (a draft must never masquerade as saved) |
| D-072 | 2026-07-05 | Ollama tool-loop parity — in-process engine, degrade-in-place, never silent fallback | **Local models get FULL agent parity via K's own in-process tool-calling loop over the Ollama `/api/chat` API** (`core/src/ollama-agent/`), branched inside the supervisor, which keeps owning lifecycle/kill/usage (kill = `AbortController.abort()`; the legacy `ollama run` pipe is retained behind `K_OLLAMA_AGENT_MODE=legacy`). The loop consumes the SAME `resolveRunAssets` as claude synthesis: **native tools** `Read/Write/Edit/Bash/Glob/Grep` + `read_skill` with claude-compatible names/schemas (tier allowlists + console rendering work unchanged; fs path-guarded to the run worktree — `K_OLLAMA_FS_SCOPE=any` widens; Bash runs a secret-stripped env with tree-kill on win32); **MCP servers** over `@modelcontextprotocol/sdk` (spawned at run start, reused across iterations, close-all in `finally`; tools advertised `mcp__<server>__<tool>`); **skills** delivered as an index + `read_skill` (full-body inlining only on the degrade path, char-budgeted). Capability is probed via `/api/show` (cached per model, invalidated on pull): a tool-less model **DEGRADES IN PLACE** (prompt-only + inlined skills) and NEVER silently falls back to claude; a truthful run-start system event + the RunConsole badge ("local · tools" / "local · prompt-only") declare the engine. **Real token capture** (`prompt_eval_count`/`eval_count`, `costUsd: 0`) rides the same usage events as claude runs; `num_ctx` is explicit (config `ollama.numCtx`, seeded from `K_OLLAMA_NUM_CTX`, default 16384). Limits: 25 iterations · 10-min run · 60s MCP tool calls · a repeated-identical-call detector · a >85% context-pressure warning | "Skills regardless of model" needs a tool loop the CLI provides only for claude; owning the loop in-process gives local runs the same assets, event shapes, and observability with zero UI change, while degrade-in-place + the truthful badge preserve the §02 routing-honesty contract (choosing ollama can never silently run claude) | Keeping the `ollama run` prompt pipe as the only engine (no tools, no MCP — parity impossible); silently falling back to claude for tool-less models (violates routing honesty); a child-process loop runner (the supervisor already owns lifecycle — in-process + AbortController is simpler and kill-safe); content-JSON tool-call salvage (strict structured `tool_calls` v1 — salvage is a documented non-goal) |
| D-073 *(DRAFT — P0 probe verdict; finalize in P6/E-21)* | 2026-07-07 | E-21 mid-run steering mechanism | **Design X — interrupt steering** *(pick iff scenario A showed control_response and/or an early result AND the post-interrupt turn answered)*: the supervisor keeps stream-json stdin open for ALL managed claude runs and exposes `interrupt(runId)` + `steer(runId, text)` — a steer during a turn = control_request interrupt then inject the operator turn. Evidence: core/probes/out/steering-{a,b}.jsonl — A: control_response=yes result_after_interrupt=yes post_interrupt_turn_answered=yes (interrupt ack subtype success; the aborted turn ends with an early error_during_execution result; the injected turn is answered STEER-ACK on the same stdin); B: results=2 queued_turn_answered=yes stream_survived=yes exit=0; measured $0.0423 | Probe-measured on the real CLI (2026-07-07, haiku) — the stdin wire behavior is a fact, not a guess; committing to the strongest mechanism the CLI actually supports keeps E-21 buildable in P6 without re-probing | Design Y — queued-turn steering (its pick-criterion required scenario A to fail, but A succeeded on all three counts; B's queued_turn_answered=yes stays recorded as evidence that a non-abort soft steer also works under Design X); Design Z — park-and-steer fallback (disqualified: both stronger mechanisms work on the wire); rewriting dispatch onto the Agent SDK (out of scope — the CLI wrapper is the product) |
| D-074 *(DRAFT — P0 probe verdict; finalize in P6/E-22)* | 2026-07-07 | E-22 follow-up-run mechanism | **Resume-based follow-ups** *(pick iff same-cwd resume retained context)*: a follow-up run spawns `claude -p --resume <runs.cli_session_id>` (column landed in P0, v8); if different-cwd resume ALSO retained context (NO), follow-ups run in a fresh worktree, else E-22 must retain the original run cwd/worktree until follow-ups close. EITHER WAY managed runs synthesize a per-run CLAUDE_CONFIG_DIR that today is CLEANED at terminal (agent-config.ts synth.cleanup()) — E-22 must retain the config dir for resumable runs, mirroring the k-secretary persistentSession `persist:true` pattern. Evidence: core/probes/out/resume-*.jsonl — same-cwd retained: YES (answered "PLUM-42" verbatim); diff-cwd retained: NO (exit 1, stderr "No conversation found with session ID" — CLI session storage is keyed by project path); measured $0.0301 | Probe-measured on the real CLI; `--resume` (a cheap cache-read continuation, the proven F-054 K-thread mechanism) is strictly better than replaying transcripts when it works — the probe tells us if it does | Transcript-reseed fallback (disqualified: same-cwd --resume retained the planted secret verbatim, so replaying stored events is unnecessary); holding warm processes per run (the rejected pre-F-054 pattern — resource-heavy, crash-fragile) |
| D-075 | 2026-07-08 | Run diffs derive from the k-checkpoint chain | **A run's reviewable diff is computed from its k-checkpoint chain — base = the first checkpoint's parent (the HEAD the run started from), head = the chain tip — durable and DETERMINISTIC: the refs live in the shared `.git` of the run's source repo and survive worktree removal, so a finished run's diff is always recomputable; mid-run the diff lags the live tree by at most ONE wave (the take-latest scheduler). PR diffs come from `gh pr diff`. BOTH producers normalize through ONE parser (`diff-parse.ts::parseUnifiedDiff`, pure, byte-capped at the last complete file boundary) into ONE `DiffPayload`, so the Review Deck renders one shape** | The run worktree is removed at terminal, so any diff computed from it dies with the run; the checkpoint chain is the only durable, replayable record of what the run changed — and one parser/one payload means the checkpoint path and the PR path cannot drift apart in rendering | Diffing the live worktree (gone at terminal; racy mid-run); separate parsers/shapes for checkpoint vs PR diffs (two renderers drift); persisting diff text in the DB (the chain already holds it in git — recompute is deterministic and cheaper than storing megabytes) |
| D-076 | 2026-07-08 | E-04 verify — one current row per run, fresh checkpoint worktree, operator-authored recipes | **`verify_results` holds ONE current row per run (PK `run_id`, upsert — the chip reflects the latest battery, not a history). The E-04 battery executes in a FRESH temp worktree materialized from the run's FINAL checkpoint: it never blocks (or races) run cleanup and is re-runnable anytime. Recipes are operator-authored shell strings — CI-config trust level: the operator writes them, K executes them verbatim (bounded timeouts + kill-tree). No recipe → NO row (the chip stays absent — never a fabricated pass)** | The run worktree is already gone when a terminal `done` emits, and the final checkpoint is guaranteed by the terminal snapshot — a fresh tree from it is always available, isolated, and disposable; operator-authored recipes carry exactly the trust an operator already places in their own CI config, so K adds no new execution authority | A verify-history table (the chip is a current-state signal; history = re-run); verifying inside the run worktree (races terminal cleanup); auto-detected or agent-authored recipes (recipes are arbitrary shell — authorship must stay with the operator); blocking terminal status on the battery (async, never in the run's critical path) |
| D-077 | 2026-07-08 | Rewind = a NEW run via `baseCommit`; the original run is immutable | **Rewind dispatches a NEW run whose worktree is created AT the chosen checkpoint via `StartRunOptions.baseCommit` — the original run, its chain, and its events are never mutated. Review fix runs (request-changes) use the SAME seam with baseCommit = the reviewed run's final checkpoint; `baseCommit` and `carryWorkingTree` are mutually exclusive by explicit guard (both dictate the worktree's starting tree). Conversation rewind is OUT of scope — this is code-state rewind only. Retention: a boot sweep deletes `refs/k-checkpoints/*` refs whose run rows are gone, under a documented SINGLE-CORE-PER-REPO assumption (ref existence is checked against this instance's DB only; cross-instance guard deferred to P7 fleet work)** | Immutability is the trust property — a review surface you can rewind FROM must never be rewritten BY the rewind; reusing the one dispatch seam means rewind and fix runs inherit every existing lifecycle guarantee (checkpointing, events, cleanup) for free | Resetting the original run's worktree/chain in place (destroys the reviewed record); conversation rewind (a transcript concern, not a code-state one — deferred); eager ref deletion at run terminal (the review surfaces read finished runs — refs must outlive them); a cross-instance ref guard now (the instance lock is per-data-dir; deferred to P7 rather than half-built) |
| D-078 | 2026-07-08 | Verify/impact GitNexus leg reads the exported graph OFFLINE | **E-04's change-scope and E-07's impact panel map changed files to indexed symbols by reading the project's exported `.gitnexus/graph.json` (written by `graph.ts::exportGraphJson`) — deliberately NO MCP and NO CLI in the hot path: the installed gitnexus CLI has no detect-changes subcommand, and the exported artifact is the already-built source of truth. Structural risk thresholds live in `riskForScope` (`gitnexus-scope.ts`); unindexed projects degrade to `indexed:false`, a missing/garbled artifact to `null` — never a blocked verify or panel** | The verify battery and the impact panel are post-run/read paths that must stay fast and non-blocking; a file read of an existing artifact is orders cheaper and failure-isolated compared to spawning a server or shelling out per call | The gitnexus CLI in the hot path (no detect-changes subcommand exists); an MCP round-trip per verify/impact call (spawns a server process — slow, blocking, failure-coupled); re-analyzing the repo at verify time (heavy; the exported graph is the indexed truth until the next analyze) |
| D-079 | 2026-07-09 | Plan gate — the `awaiting_plan` process-dead park (Phase 2) | **`awaiting_plan` is a DISTINCT process-dead park (canonical `waiting/review_needed`); resume = `--resume runs.cli_session_id` in the SAME worktree with the SAME preserved per-run config dir (D-074 composes); boot sweep flips only broken substrates to `interrupted`.** | A plan gate must survive a reboot and cost nothing while it waits — which a live-stdin `awaiting_input` park (D-014) cannot — so the process EXITS and the plan turn is replayed cheaply via the proven `--resume` continuation (D-062/D-074) in the same cwd + preserved config dir; a healthy parked run must never be swept to `interrupted` on boot | Holding a live process at the plan gate (the D-014 cost/park, not reboot-survivable); resuming in a fresh worktree or config dir (D-074 proved diff-cwd resume loses the CLI session); a blanket boot sweep that interrupts every parked run (would destroy reboot-survivable plan parks — only BROKEN substrates flip) |
| D-080 | 2026-07-09 | Plan storage — `run_plans`, one current row per run | **Plan storage = `run_plans`, ONE current row per run; last-wins operator edits (`edited` flag); `raw` always kept; unparseable plan turns park honestly with `plan = NULL`.** | The plan chip reflects the CURRENT plan the operator is approving, not a history — so one upsert row per run mirrors the `verify_results` current-row shape (D-076); keeping `raw` and honoring last-wins operator edits means what is approved is exactly what is shown, and a plan turn the CLI did not emit parseably must never fabricate a plan | A plan-history table (the gate reads current state, not a log — matches D-076); discarding `raw` after parsing (loses the honest record when parsing degrades); dropping or blocking a run whose plan turn is unparseable (parking with `plan = NULL` keeps the operator in control instead) |
| D-081 | 2026-07-09 | Approvals Inbox — a union query, never a table | **The Approvals Inbox is a UNION QUERY over five sources, never a table; dismissals: `runs.reviewed_at` stamp-once (backfilled at v10) and MCP `inbox_dismissed_hash` pinned to `config_hash` (drift re-surfaces).** | Every needs-YOU item already lives in its own authoritative source (plan approvals, `awaiting_input` parks, pending lessons, untrusted MCP, review-ready runs), so a derived union stays truthful with zero sync; a stamp-once `reviewed_at` and a `config_hash`-pinned MCP dismissal keep a dismissed card gone only until the underlying config actually changes | A materialized inbox table (duplicates the five sources, drifts, needs sync); a permanent MCP dismissal not keyed to `config_hash` (a later config change would stay silently hidden — pinning re-surfaces the card) |
| D-082 | 2026-07-09 | Notifications phase 1 — seeded rules + durable center + transient browser leg | **Notifications phase 1 = seeded `notification_rules` (event key -> in-app/browser) + a durable in-app center + a transient WS `notification` member carrying the rule's `browser` flag; engine dedupes on STATUS TRANSITIONS; the browser leg raises only when hidden + permission granted (gesture-requested).** | Operators need a durable record plus an optional attention-grab, but a browser toast on every event is noise — so a seeded rules table maps event key to channel, the in-app center is the durable log, and the browser leg fires only on a real status transition while the tab is hidden and permission has been granted | Firing a browser notification on every event (noise — dedupe on transitions instead); a browser-only scheme with no durable center (misses everything while the tab is closed); requesting notification permission unprompted (browsers reject un-gestured prompts — request on an operator gesture) |
| D-083 | 2026-07-09 | Verification is GitHub-native — the `k/verify` commit status | **Verification is GitHub-native: context `k/verify` published onto the run's final checkpoint sha; `running->pending . pass->success . fail->failure . error->error . skipped->NOT published`; publish at approve (with a fresh-PR cache upsert) + republish on verify updates for PR-linked runs (`headRefName === k-review/<runId8>`); merge = `gh pr merge --merge` guarded by an OPEN + checks-green readback; auto-merge defaults OFF and never bypasses the readback.** | The delivery path already lives on GitHub (D-003/D-004), so publishing verify as a native commit status makes it visible on the PR, in branch protection, and to any other tool with no new plumbing; a skipped recipe publishes nothing (never a fabricated pass — D-076), and a merge proceeds only after a fresh server-side OPEN + green readback so a stale UI can never merge a red PR | A K-private verify signal invisible to GitHub branch protection (D-004 is GitHub-native gating); publishing a status for a skipped recipe (fabricates a pass — D-076 forbids it); merging on cached UI state without a readback (races a red rollup); auto-merge on by default (removes the human gate) |
| D-084 | 2026-07-09 | Plan-gate tier default rides `agent_profiles.plan_gate` | **Plan-gate tier default rides `agent_profiles.plan_gate` (the org-default orchestrator row is the operator default; org dispatches inherit their profile's flag); an explicit per-dispatch `planGate` always wins.** | Whether a run stops at a plan gate is a policy that belongs on the same profile row that already carries a tier's tools/MCP/authority (D-021/D-047), so the org-default orchestrator row is the one operator-editable default and org dispatches inherit their profile's flag — while a specific dispatch can always force the gate on or off | A single global on/off flag (cannot vary the gate per tier/lead); the gate always on or always off (removes operator control over routine vs. risky work); a per-dispatch flag a profile default could override (an explicit request must win) |
| D-085 | 2026-07-10 | Org Timeline feed is a read-time union projection, not a table | **E-09 Org Timeline is a read-time UNION PROJECTION over existing tables (run heads + review-ready notifications + verify_results + open-PR github_cache), never a persisted feed table; ONE `['feed']` query key (with a `['feed',500]` timeline variant) powers K-home recent + the Timeline view; ActivityStrip is deliberately NOT re-pointed and stays the live-runs widget.** | A projection reuses the existing sources with no schema/write-path and cannot drift from them (mirrors the E-05 Inbox union); one shared key keeps the two historical surfaces consistent. ActivityStrip needs COMPLETE, uncapped coverage of every non-terminal run plus rich Run fields (prompt/agent) for its cards, which a capped lean historical feed cannot honestly provide, so forcing it onto the feed would silently drop parked runs under the item cap. | A persisted org_events table (a schema bump + a write at every transition + a second source of truth that can drift); re-pointing ActivityStrip onto the capped feed (drops active/parked runs beyond the cap, a live-monitoring regression); three separate recent-activity queries (drift). |
| D-086 | 2026-07-10 | Run Narrative = deterministic fields always + auto/degrade local-model bullets | **E-08 Run Narrative renders deterministic fields (goal/outcome/files/verification/cost) derived from a run's events on every open; <=3 Decisions / <=3 Risks bullets come from the LOCAL model, auto-attempted on open, labeled "generated", physically separated from the deterministic fields, and gracefully OMITTED (with a subtle note) when the model is unreachable/unparseable; a bounded 20s abort guarantees the endpoint never blocks.** | The card must be trustworthy and always useful: measured/derived facts can never be confused with model prose (labeling + separation), and an unavailable local model degrades to facts-only rather than blocking or erroring; the abort bounds a stuck /api/chat stream so the GET honours its own never-blocks contract. | Mixing generated bullets into the deterministic fields (confuses fact and inference); requiring the local model (blocks the card when Ollama is down); an on-demand-only button (loses the auto glance); an unbounded model call (a stuck stream hangs the request). |
| D-087 | 2026-07-10 | Cost lens is measured-only; token chips become relative weight bands | **E-13 rolls up MEASURED `cost_usd` (run->lead->project->day) and computes dispatch recent-actuals (median/p90 $/run, recency-windowed, `cost_usd>0` pool, scope agent-profile->project->global fallback at n<5); catalog capability token chips become RELATIVE light/medium/heavy weight bands (derived from token counts, never a price); a committed no-price-tables grep gate enforces the no-price-coupling rule.** | The program bans price-coupled estimation (measured actuals only): recent-actuals are real historical costs, not price*tokens, and the agent->project->global ladder gives the most relevant number with a graceful thin-history fallback; weight bands convey relative context cost without implying a dollar figure; the grep gate makes the rule mechanically enforced. | A price-per-token table or price*token estimate (confidently-wrong, banned); a single global actuals number (mixes cheap and expensive agents); dollar-denominated capability chips (implies billing the user does not incur). |
| D-088 | 2026-07-10 | Single HealthRubric + compile-time glossary | **E-12 folds the ~5 drifting health-score->color/label copies into ONE `healthRubric` (canonical thresholds >=75 healthy / >=50 warn / else critical / null unknown) consumed by every web health surface; glossary terms are extracted from the bible glossary section at `pnpm bible` COMPILE time into a committed generated module feeding a `GlossaryTerm` tooltip, guarded by a core-side drift test. The server `bible.ts liveHealth` 80-threshold is intentionally left unchanged.** | One rubric means a threshold or palette change lands in a single place instead of drifting across ProjectCard/FleetGraph/ProjectVerification/ProjectWorkspace/verify; compile-time glossary extraction keeps tooltips in lockstep with the living spec at zero runtime cost with a drift guard; the server's distinct liveHealth threshold is a separate concern not swept in, to avoid changing server behavior. | Per-site health color logic (drifts); a runtime glossary fetch (latency + another endpoint); silently changing the server 80 threshold to match web's 75 (an unrelated behavior change). |
| D-089 | 2026-07-10 | Electron over Tauri for the desktop app | **Ship the desktop app as ELECTRON (electron-builder → NSIS + electron-updater); core is spawned as a Node-20 CHILD, not Electron's runtime.** | Core is all-Node with native modules (better-sqlite3 / node-pty) and spawns CLIs, so one JS/Node toolchain fits and electron-builder gives installer + updater for free; the ABI smoke (Electron ABI 130 ≠ core's ABI 115) is handled by spawning core under a bundled Node-20 — avoiding electron-rebuild and keeping core's ABI-115 test suite intact (see §15). | Tauri (a Rust toolchain plus a standalone node.exe sidecar whose ABI match Tauri itself flagged make-or-break); running core IN Electron's runtime + electron-rebuild (breaks the shared pnpm store and core's ABI-115 natives/tests). |
| D-090 | 2026-07-10 | Core serves the web SPA same-origin | **Core serves `web/dist` via @fastify/static (gated on `K_WEB_DIST`) so the packaged app is ONE origin — relative `/api`, same-host WS — with the bundle public and the data plane token-gated.** | One origin removes CORS and the build-frozen port entirely (the client's relative `/api` and `window.location` WS just work); the bundle already bakes no token, so making it public matches the existing Vite posture while every `/api/*`/`/ws*` route stays bearer/query-token gated (only public GET/HEAD assets are auth-exempt). | Tauri hosting `web/dist` and repointing `/api` + WS (needs runtime port injection into the bundle); a cross-port core + CORS (more surface, build-frozen port). |
| D-091 | 2026-07-10 | Token handoff via IPC → auto-auth | **The shell reads core's on-disk auth token and seeds it into the renderer's `sessionStorage['k.harnessToken']` via a SYNCHRONOUS IPC from a sandboxed preload, so the same-origin SPA auto-authenticates; the token never rides argv or the URL, and core's auth is unchanged.** | The SPA already reads `sessionStorage['k.harnessToken']`, so seeding it from a sandboxed preload before the first API call gives a no-login desktop experience with zero core change; keeping the token off argv/URL (IPC only) closes the two easiest leak paths, and best-effort seeding degrades to the normal login screen on any failure. | Baking a token into the bundle (a prod build bakes none by design — it would be a shipped secret); an in-app login screen (needless friction for a local single-user app). |
| D-092 | 2026-07-10 | Detect-&-guide host prerequisites | **A system doctor (`GET /api/system/doctor` + a Settings card) detects `claude`/`git`/`node` (required) and `gh`/`ollama` (optional); the non-redistributable `claude` CLI is guided-to-install, never bundled.** | The agent engine is the host `claude` CLI and it is not redistributable, so the honest move is to detect + guide (with install URLs) and report `ok` only when every required tool is present; probes never throw (a missing tool is data), so first-run readiness is a clear checklist instead of a mystery failure. | Bundling the agent engine (not redistributable); assume-installed (a rough, opaque first run when a prerequisite is missing). |
| D-093 | 2026-07-10 | Core relocatability via K_REPO_ROOT | **`REPO_ROOT`/`ARTIFACTS_DIR`/`WORKSPACE_DIR` honor a `K_REPO_ROOT` override, so a read-only install redirects writable repo-relative paths (`.worktrees`/artifacts/workspace) to a per-user writable runtime (seeded with the bible source on first run/version-change); agent-config stays read `__dirname`-relative from the read-only bundle.** | A packaged install is read-only but core must write worktrees/artifacts, so one env override moves exactly the write paths to `<userData>/runtime` while read-only assets (agent-config, including skills) stay bundled and `__dirname`-relative — the smallest seam that makes core relocatable without copying the whole repo. | Shipping/running from a writable copy of the entire repo (heavy, and pollutes user data); leaving paths `__dirname`-relative (every write fails under a read-only install). |
| D-094 | 2026-07-10 | Windows-first, unsigned, auto-update | **Ship Windows-only and UNSIGNED with electron-updater against public GitHub Releases; trust = HTTPS-to-GitHub + the sha512 in `latest.yml` (NO signature backstop → the release pipeline must be protected); SmartScreen warnings accepted.** | Proving the Windows install/tray/notification/update pattern end-to-end is the valuable first step, and an unsigned build still auto-updates (feed pinned at build time, sha512 pins the bytes) — provided the GitHub account/Actions pipeline that can publish a Release is locked down (2FA, branch protection, no direct-push releases), since that is the actual trust anchor. | Code-signing now (a paid cert + notarization — deferred until the pattern is proven); no auto-update (manual re-download); cross-platform now (deferred until the Windows pattern is proven). |
| D-095 | 2026-07-11 | Detect-&-guide is on-demand doctor + reactive first-run dialog | **Detect-&-guide (D-092) is SURFACED as (a) the always-available System Doctor card in Settings — the on-demand, re-checkable detect+guide surface — plus (b) a reactive native dialog when core fails to become healthy on launch (which points at the Node.js/PATH prerequisite). A proactive pre-flight probe and auto-opening the doctor on first run are deferred as a tracked UX enhancement, not shipped in this cut.** | The two shipped surfaces already deliver the substance: the doctor genuinely detects every prerequisite with install links, and the missing-Node case surfaces IMMEDIATELY (not after the full health timeout) because a failed `node` spawn rejects the health race at once (see §15). Auto-surfacing on first run needs a first-run flag + web auto-navigation that is better designed as its own small change than rushed into the packaging cut — recorded here so the delivery is not an undocumented deviation from the Wave 2/3 plan wording. | Blocking the plan on a literal pre-flight `git --version`/`node --version` + first-run auto-open (real UX value but a non-trivial web/first-run change late in the packaging effort); silently shipping the substitution with no decision record (the deviation would be invisible to a future reader). |
| D-096 | 2026-07-11 | Rail flattened 14->9; legacy hashes soft-redirect (E-29, P4) | **P4 flattens the Direct/Observe rail into ONE 9-item flat rail (K · Org · Projects · Skills · Runs · Insights · Inbox + footer Help · Settings); merged/folded view strings are removed from `KNOWN_VIEWS`; a `VIEW_REDIRECTS` map + `resolveRoute` soft-redirect every legacy hash to its new home and `useHashRoute` canonicalizes the address bar via `history.replaceState` (no Back-trap). Schema-neutral — no migration, no new table, no `WsMessage` change; K-home stays the default landing.** | A 15-entry two-group rail buried the handful of surfaces an operator actually uses; folding related surfaces (Org, Insights, Runs) into one destination each with in-page segments/tabs cuts the choice overload while every old deep link keeps working via redirects, so no bookmark or test breaks and the data model is untouched. | Keeping the two-group 14-entry rail (choice overload); hard-404ing removed hashes (breaks bookmarks + e2e); adopting a router library (the hand-rolled hash router already suffices); pushing the canonical hash on redirect (leaves a Back-trap — replaceState instead). |
| D-097 | 2026-07-11 | Insights merges Metrics + Routing + Evals under 4 tabs (E-10) | **Insights = 4 tabs — Overview (DETERMINISTIC deltas + z-score anomaly threshold math over measured `timeseries(days)` + `quality(days)`, earlier-half vs later-half baseline, null-filtered, NO LLM, no price*tokens), Charts (all 9 charts preserved + a shared `days` window that cross-filters Overview/Charts/Routing), Routing (preserved), Evals (preserved run/dry-real + freeze baselines); metrics/routing/evals redirect in to the matching tab.** | The three cost/quality surfaces answered one question ("how is the org performing?") from three rail entries; one destination with a shared window unifies them, and a deterministic Overview gives a glanceable delta/anomaly summary with no model and no price coupling (measured-only, per the program rule). | Three separate rail entries (fragmented one question); an LLM-written summary (non-deterministic, unmeasured — banned); per-tab independent windows (silent divergence when switching tabs); a price*token Overview (price-coupled estimation is banned). |
| D-098 | 2026-07-11 | One Org surface — Roster/Tree/Graph SegControl (E-29) | **One Org surface merges Orchestrators + Chief + Fleet Graph behind a SegControl — Roster (default) · Tree · Graph; orchestrator detail is preserved as a drill-in (back-nav -> org/roster); chief/orchestrators/graph redirect to the matching segment; the force graph keeps the `react-force-graph-3d` subpackage import (never the aggregate).** | The roster, the whole-org delegation tree, and the structural fleet graph are three views of ONE thing (the org), so one destination with a segment switch matches the mental model; detail stays a drill-in so the roster is the stable home; keeping the `-3d` subpackage avoids the AFRAME blank-screen trap. | Three rail entries for one org (fragmented); collapsing detail into the roster (loses the per-lead editors); importing the `react-force-graph` aggregate (references a non-existent global AFRAME → blanks the route). |
| D-099 | 2026-07-11 | E-30 — contextual terminal, edit-in-place docs, one Tabs/SegControl, Workflows->Runs, Memory demotion | **E-30 bundle: the guarded terminal relocates to WHERE a shell is used — a ProjectWorkspace tab + a Settings diagnostics panel (`#/terminal` -> settings); DocsPage edits the bible IN PLACE per section via the EXISTING section-write API (`PUT /api/artifacts/:slug/sections/:sectionSlug`, `key={slug}` remount guards cross-bible draft bleed); ONE new `Tabs` (underline, role=tab/aria-selected) + ONE extended `SegControl` (pill, aria-pressed) replace every ad-hoc tab/segmented copy; Workflows fold into Runs (`#/runs/workflows[/:defId]`, in-view run-tree picker); Memory is demoted to ⌘K-only (`#/lessons`) with `#/memory`->inbox — lessons were ALREADY an Inbox union source, so no new source is added.** | Each piece removes a standalone surface whose function belongs somewhere with context — a terminal next to the project it shells into, doc editing on the doc itself, one standardized control instead of many copies, workflows beside the runs they spawn — and Memory needed no rail entry because the Inbox already surfaces pending lessons. | A standalone Terminal rail entry (no project context; default-off dead weight); a separate bible-editor surface (a second source of truth); per-page bespoke tab/segment markup (drift — one component each); a standalone Workflows rail entry (divorced from its runs); a Memory rail entry (duplicates the Inbox lessons source). |
| D-100 | 2026-07-11 | E-11 — canonical run-status adoption completes (`lib/status.ts`) | **E-11 completes for canonical run-status surfaces: they adopt `web/src/lib/status.ts` (`runStatusMeta` / `agentRunStatusMeta` / `delegationStatusMeta`, all DEFAULT-LESS), and every adoption site GUARDS membership so an unknown status degrades to muted rather than throwing (skill_run rows use agent-run vocab -> `agentRunStatusMeta`, not `runStatusMeta`). Non-canonicalized vocabularies (ProjectTask / WorkflowStep / SkillDraft / SkillEval / CI checks) get token-drift fixes only; dedicated canonicalizers for them are DEFERRED.** | A single default-less status source with membership guards makes an unknown status impossible to render as a wrong color and impossible to crash on, converging the ~5 drifting status->color/label copies; the non-run vocabularies run different state machines, so forcing them through the run canonicalizer would mislabel them — better to defer real canonicalizers than fake one. | A status helper with a silent default fallback (renders an unknown status as a plausible-but-wrong color); routing agent-run vocab (completed/failed) through `runStatusMeta` (throws on normal terminal states — a real crash the sweep fixed); canonicalizing the non-run vocabularies now (different state machines — misleading; deferred). |
| D-101 | 2026-07-11 | UI Simplification — 6-tab rail + Personal/Agents hubs | **The 9-item flat rail (D-096) FOLDS to a 6-tab primary rail — K (Home) · Personal · Agents · Runs · Insights · Projects — plus a Help/Settings footer; PERSONAL absorbs Inbox/Tasks/Chats/Memories and AGENTS absorbs Org/Skills/Pipelines (the former Workflows) as tabbed hubs, not standalone destinations. Every folded view keeps its deep link via `VIEW_REDIRECTS`.** | Nine flat items still forced a guess between near-duplicate org/insight surfaces; grouping the org-facing and self-facing surfaces under two hubs cuts the rail to what fits in one glance, while the redirect map (D-106) means no existing deep link breaks. | A further-flattened single-level rail (Runs/Insights/Projects share no natural parent, forcing an arbitrary 7th tab); renaming the 9 labels in place (does not address the underlying pick-between-duplicates problem). |
| D-102 | 2026-07-11 | Message Dock replaces the ⌘K palette + Activity strip | **A single `MessageDock` component — a persistent composer BAR on Home, a floating circular FAB everywhere else — replaces both the ⌘K CommandBar palette and the live-only Activity strip as the one front door; the Ctrl/Cmd+K chord and every `g`+letter navigation chord are UNCHANGED.** | The palette (search+dispatch) and the strip (live-status ticker) were two separate UI languages for "talk to K" and "see what's running" that both duplicated the Home composer; one Dock with a shared `<Composer/>`, K's inline route preview, and the 5 s undo toast collapses them into one idiom — live-run visibility moves to the Runs badge + Insights, per metric-uniqueness. | A dock-bar mounted on every route (a fixed bottom bar competes for vertical space on dense Runs/Insights surfaces); dropping the palette's fuzzy-navigate function (jump-to-anything is still needed; the Dock's thread-picker + K routing subsumes it). |
| D-103 | 2026-07-11 | Multi-thread K = K conversation threads only | **`k_threads`/`k_thread_turns` model K's OWN conversational history — multiple named, renamable, archivable chat threads — and are DELIBERATELY NOT a second view onto agent run execution; a dispatched run's live/replay detail stays exclusively in the Runs console, cross-linked from a thread turn via a `→ view run` chip carrying that turn's `runId`.** | Conflating "a chat with K" and "a run's event stream" into one thread model would force every run event through a chat-shaped (role+text) turn and lose the run console's structured tool-call/diff/timeline rendering; keeping the two data shapes separate and cross-linking by id lets each own what it actually needs. | Modeling a run as appended K-thread turns (loses RunConsole's structured rendering); one unified polymorphic "activity" timeline (materially more complex for no operator-visible gain over a simple cross-link chip). |
| D-104 | 2026-07-11 | Operator user-memories store + `memory_save` | **A `user_memories` table (`UserMemory`: content, optional `sourceThreadId`) holds durable operator facts/preferences, editable directly on Personal → Memories; K's `memory_save` MCP tool writes to the SAME store from inside a conversation with NO approval gate, but fires a quiet `memory_saved` in-app notification (never a modal) so a save is never silent. The tool and its "your current memories are…" prompt injection are wired into the SECRETARY (K) charter ONLY — not Chief, not the leads.** | A user memory is a fact ABOUT the operator, not an approved-lesson process change (§04's separately-gated lessons queue), so it needs no review gate — but a background write with zero signal would still be surprising, hence the quiet notification. Scoping the tool and the injection to K's own charter keeps "knows the operator" a property of the one agent that actually talks to them. | Gating `memory_save` behind the lesson accept/reject queue (wrong trust model — this is the operator's own data, not a proposed process change); injecting user memories into every agent's system prompt (unbounded prompt bloat for leads that never talk to the operator, and a scope leak of personal facts into engineering-only contexts). |
| D-105 | 2026-07-11 | Widget grid, server-persisted Zod-validated layout | **Home's Overview tab is a fixed 3×3 grid of up to 9 catalog widgets (`HomeWidgetIdSchema`); placement (`HomeLayoutSchema` — in-bounds, non-overlapping, no duplicate ids, `.strict()`) persists server-side via `GET`/`PUT /api/settings/home-layout` (Zod `safeParse`, `400` on an invalid layout) through the existing `config-store.ts`, with an optimistic client-side write (`useHomeLayout`) so customize-mode feels instant.** | Session/localStorage-only placement resets silently on a new device or a cleared browser; an unvalidated layout risks an out-of-bounds or overlapping grid the renderer can't lay out. A server-side Zod schema makes an invalid layout impossible to persist, and reusing `config-store` keeps the storage layer boring rather than inventing a new one. | A free-form (non-grid) drag-and-drop canvas (no bound on overlap/off-screen placement, no natural "empty cell" affordance for the add-picker); per-device-only local persistence (loses the layout on a new machine or browser profile). |
| D-106 | 2026-07-11 | Legacy-hash redirect posture | **Every view string removed from `KNOWN_VIEWS` by this restructure keeps a `VIEW_REDIRECTS` entry mapping it (and its param/sub-param, where the destination can consume them) to its new home; `useHashRoute` applies the redirect via `history.replaceState` (never a push), so a bookmarked or shared legacy hash still lands correctly and Back never bounces through a dead intermediate hash.** | Silently 404-ing a link that worked yesterday breaks bookmarks, shared URLs, and muscle memory for no operator benefit; `replaceState` specifically (not a normal navigate) avoids a Back-button trap where Back from the canonical page would return to the legacy hash, which immediately re-redirects forward again. | Routing every removed hash to the 404 page with a manual "did you mean" link (an unnecessary extra click for a redirect the router can resolve deterministically); a server-side redirect table (the app is a static SPA with hash routing — there is no server request to intercept). |
| D-107 | 2026-07-13 | CHIEF_WAKE → a persisted Settings choice, default OFF (the single front door) — E-14..E-18 | **Turn the env `CHIEF_WAKE` flag into a persisted operator choice: a new Settings → "Autonomous Org" section (`SettingsAutonomy.tsx`) with a master `enabled` toggle (default OFF) + sub-toggles (Generate proposals / Auto-pull backlog / Self-heal failed runs) + max-concurrency + org daily budget cap + warn %, all one JSON blob in `app_config` (`autonomy.settings`, mirrors `home_layout`). Agents→Org shows only a read-only status chip deep-linking to it. Toggling ON at runtime needs NO restart — the schedulers always wire at boot and gate on the persisted setting per-tick.** | Autonomy is global org config with the same shape as every other operator-configurable *system* behavior (Ollama, Voice, Claude default model, Notifications, org-default authority), which already lives in Settings via `config-store`; making it a persisted **default-OFF** choice means a fresh DB is inert until the operator opts in, exactly as directed. | Env-only flag defaulting ON (invisible, non-persistent, on by surprise); an Agents/Org control panel (duplicates the Settings pattern for one more surface); an always-on scheduler that reads the env each tick (still env-coupled, no operator UI). |
| D-108 | 2026-07-13 | The master gates the whole stack; the budget governor is the always-on exception *(autonomy boundary amended-by D-125 — domain supervision is core, not master-gated; the master still gates self-generated work)* | **`autonomySettings().enabled` (default false) gates ALL FOUR autonomous behaviors — chief auto-wake, proposal collectors (`&& proposals`), backlog auto-pull (`&& backlogAutoPull`), self-heal (`&& selfHeal`); each sub-toggle additionally requires `enabled`. The budget governor is the ONE exception: a safety cap that applies to manual AND autonomous dispatches even while autonomy is OFF, once a cap is set.** | A single on/off front door the operator configures once is the honest model — a fresh DB with the master OFF does nothing autonomous. The budget cap is a *safety limit*, not an autonomous behavior, so gating it behind the master would let turning autonomy off silently disable the operator's own spend cap. | Per-behavior independent enables with no master (no single front door — the exact fragmentation the directive rejected); gating the budget cap behind the master too (a safety cap that vanishes when autonomy is off is a footgun). |
| D-109 | 2026-07-13 | Env `CHIEF_WAKE` deprecated warn-only | **`chiefWakeEnabled()` now reads the persisted `autonomySettings().enabled`; the env var no longer defaults autonomy ON and NO migration writes a persisted `true` from it. A one-time boot warning fires (regardless of enabled state) whenever `CHIEF_WAKE` is set in the environment, pointing the operator at Settings → Autonomous Org.** | Removing a shipped env flag silently would strand an operator who still sets it; warn-only keeps the boot honest while the persisted default (OFF) becomes the real gate. Refusing to migrate the env `true` into a persisted `true` avoids silently keeping autonomy on for anyone who had it enabled — the operator re-opts-in deliberately. | A hard removal (breaks an existing `.env` with no notice); migrating `CHIEF_WAKE=1` → persisted `enabled:true` (silently keeps autonomy on, defeating default-OFF); keeping the env as a co-equal gate (two sources of truth). |
| D-110 | 2026-07-13 | E-16 routines reuse the Automations tab / skills table (no new table, no new rail slot) | **E-16 EXTENDS the existing `AutomationsTab.tsx` (Agents→Skills→Automations, already a node-cron registry over the `skills` table): an NL→cron helper that is RULES-ONLY (no model; an unmappable phrase → `400` at the route boundary), a next-run display, and MEASURED cost per routine (summed from its run history via a JOIN to `runs.cost_usd`).** | The Automations tab is already a full schedule registry; a routine *is* a schedule-triggered skill, so it needs only a derived next-run + measured cost, not a new entity — DRY over the master re-baseline's mapping, and no new destination in the 6-tab rail. | A new `routines` table + rail slot (duplicates the skills scheduler and the no-new-rail rule); an LLM NL→cron translator (non-deterministic; a rules table suffices and fails closed on unmappable phrases). |
| D-111 | 2026-07-13 | Proposals are blocked org work_items deduped by source_key (+ sticky-dismiss & one-shot-per-project honest limits) | **A proposal is an `org`-scoped `work_item` created `status='blocked'` with a `source`/`source_key`, deduped by a partial-unique `source_key` index, open-capped (20). Deterministic ZERO-TOKEN collectors (ci_failed / verify_finding / open_issue / stale_bible) run on a 15m cron. Personal→Inbox surfaces them as proposal cards: approve→`open` (enters the backlog); dismiss→`cancelled` (STICKY — the source_key still resolves to the cancelled row, so it never re-nags).** | Reuses the unified `work_items` store (D-026) with no new table; the sticky dismiss is the "don't nag again" signal. **HONEST LIMITATION (deliberate, default-OFF):** the project-keyed `source_key` (`<source>:<projectId>` for ci_failed/verify_finding/stale_bible; open_issue keys per issue) has no run/date discriminator, so such a proposal is effectively **ONE-SHOT per project** — once a row exists in ANY status (approved/done or dismissed) a genuine later recurrence is NOT re-surfaced until the row clears — and there is **no undo** on inbox dismiss. Documented as tracked follow-ups, not sold as continuous monitoring. | A new proposals table (the work_items store already fits); a run/date-discriminated source_key (would re-nag on every 15m tick — a worse default); shipping an undo affordance now (deferred). |
| D-112 | 2026-07-13 | Budget park = reactive refuse-with-reason, measured rolling-24h, zero forecasting (+ deferred operator-action routes) *(the `delegateToChief` gated path retired-by D-126; the interactive-K exemption is restated as the `kind='chat-turn'` exemption — D-127)* | **E-17 caps on MEASURED `runs.cost_usd` over a rolling 24h window — ZERO forecasting (no price×token, no projection). Org cap (autonomy settings) + per-project caps (`projects.budget_daily_usd`). At cap a dispatch is PARKED = refused-with-reason (`429` / `BudgetCapError`), no queue — the operator raises the cap. Gated paths: `startAgentRun` (autonomous schedule/event/delegation), manual `POST /api/runs` (→429), operator→Chief `delegateToChief` (→429), and autonomous scheduled/event skill dispatch. EXEMPT: interactive/persistent K turns.** *2026-07-14 (P5-FU-1): the park now ALSO gates the operator action routes — rewind, review-fix, deep-verify, workflow dispatch, run-skill-now — with the same 429 payload (`sendBudgetCapped`); the interactive-K exemption is unchanged and now the only one.* | Measured-only honors the program's no-price-coupling rule (the committed no-price-tables grep gate stays green); a reactive refuse-with-reason is simpler and truer than a queue or a projection; exempting interactive K keeps the operator's own conversation with their secretary unblocked. Operator-initiated action routes (rewind / review-fix / deep-verify / workflows / run-skill-now) are a **tracked follow-up**, documented honestly rather than claimed. | Forecasting / "projected" spend (price-coupled, banned); a queue that runs the parked work later (hidden deferred spend); gating interactive K (blocks the operator talking to their own secretary). |
| D-113 | 2026-07-13 | E-27 eval-derived lessons ride the D-041 lesson gate, off verify_results (eval_results deferred) | **Repeated (≥2) same-signature failures produce ONE deduped, capped (10) PENDING `agent_memory` lesson through the EXISTING D-041 lesson gate — reusing the `lesson_pending` inbox kind, no new UI. SCOPE: implemented off `verify_results` only; `eval_results` is DEFERRED (it has no free-text failure-reason column to signature on) as a documented extension point.** | This is memory layer C's first real slice; riding the existing human-gated lesson queue means a machine-derived lesson STILL needs operator approval (nothing self-modifies its brain), and no new surface is added. `verify_results` carries the free-text failure signature `eval_results` lacks, so it is the honest first source. | A new auto-lesson table + UI (duplicates the D-041 gate); ungated auto-derived lessons (a bad lesson poisons the profile); claiming eval_results coverage now (it has no failure-reason column — would be a fabricated signature). |
| D-114 | 2026-07-13 | UI Polish — liquid-glass design system: 4 tiers, gated icon set, 19-primitive kit, empty token gate | **A 26-task program re-skins the dashboard onto FOUR explicit glass tiers (`glass-chrome`/`glass-panel`/`glass-overlay`/`surface-solid`, picked by role with a hard no-nested-backdrop-filter rule and a reviewed ≤6-blurred-regions-per-viewport budget, replacing D-013/D-024's hero-only/dense-opaque binary); the full `lucide-react` icon set is closed behind `ui/Icon`'s 33-entry `ICONS` map (the only file in `web/src` importing the library directly); a 14-file, 19-named-component primitive kit ships at `web/src/ui/` (Button/IconButton, Dialog, EmptyState, ErrorState, Input/Textarea/Select, GlassPanel, Icon, KpiTile, SectionHeader, Skeleton/SkeletonRow/SkeletonTile, Spinner, StatusPill, Tag, Tooltip) alongside the pre-existing canonical Tabs/SegControl; and `web/test/ui-token-gate.test.ts`'s allowlist — seeded at the program's start and shrunk wave by wave — reaches EMPTY (`[]`, commit `fcc3284`), so every color in `web/src` now routes through a token or its Tailwind semantic class with zero exemptions.** | The prior binary (glass on hero surfaces, opaque on dense data) couldn't express nesting (a dialog floating over an already-glass chrome) or a numeric blur budget (an 11-tile KPI row blows the frame-rate budget even though KPI tiles read as a hero surface) — a graded, role-based tier system with an explicit nesting rule replaces per-component ad hoc calls with one discipline; gating icons behind one typed map keeps the icon vocabulary closed and auditable; a shrinking-to-empty allowlist makes "no raw color left in the app" a verifiable, continuously-enforced fact rather than a one-time claim. The `--chart-1..8` dataviz palette was validated, not redesigned — a CVD check against the real dark surface caught one failing adjacent pair (chart-7↔chart-8, ΔE 1.1) fixed by snapping `--chart-8` to `#6366f1` (ΔE 15.4); a separate lightness-band check fails all 8 tokens against a generic heuristic and is accepted as non-actionable since the palette is brand-locked and contrast ≥3:1 still holds. | A single opaque/glass switch per component (already broken by nesting cases the program had to resolve one-by-one); an automated blur-region-count lint (no static check reliably counts simultaneously-rendered, responsive-layout-dependent blur regions — kept as a manual per-wave and whole-app visual review instead); repainting the chart palette wholesale to satisfy the lightness-band heuristic (palette is brand-locked; the one real contrast failure needed only a single-token snap). |
| D-115 | 2026-07-15 | Liquid Glass 2.0 — living ambient, SVG refraction, specular edges, pointer sheen; tier fills re-graded .48/.64/.76 | **`Ambient.tsx` gains 4 blob divs (`ambient-blob-1..4`) animated by `@keyframes lg-drift-1..4` — GPU-only `translate3d`/`scale`, 66-114s alternating cycles, frozen under `prefers-reduced-motion` — replacing the old static radial-gradient wash. `GlassFilterDefs.tsx` (mounted once in Shell) renders one hidden `<svg>` `#lg-refract` filter (`feTurbulence` + `feDisplacementMap scale={LG_REFRACT_SCALE=14}`, mirrored by `--lg-refract-scale` in `index.css`) driving a Chromium-first, `@supports`-guarded `backdrop-filter: url(#lg-refract) ...` on each tier's `::after`. The W0.5 specular-edge technique replaces each tier's flat single-color fill with a double background (`fillColor` on `padding-box` + a `135deg` `var(--lg-edge)`→border wash on `border-box`) and an `inset 0 1px 0 var(--lg-edge)` box-shadow highlight, superseding `--glass-sheen` (now vestigial — still defined in `:root`, referenced nowhere). Tier fill opacities drop one step, `.55/.72/.82` → `.48/.64/.76`. `.glass-interactive` + `useGlassPointer` add an opt-in pointer sheen: ONE window-level, rAF-throttled `pointermove` listener writes `--lg-mx`/`--lg-my` (viewport px) onto `document.documentElement`, disabled under `prefersReducedMotion()` and `pointer: coarse` (touch), mounted once in Shell.** | An AA/WCAG contrast pass (≥4.5:1 against the brightest blob) forced the tier-fill step down — proof the new ambient layer didn't quietly erode text contrast, not an aesthetic choice. Gating refraction behind `@supports` + Chromium-first is an honest platform-capability boundary (an explicit no-op fallback) rather than a silent visual break on unsupported browsers. One shared filter definition and one shared pointer listener (not one per glass surface) keep the runtime cost O(1) regardless of how many tiered surfaces render. | Per-element pointer listeners (n listeners for n glass surfaces defeats the point of a single shared CSS-variable hook); shipping refraction unconditionally with no `@supports` guard (renders wrong on non-Chromium engines); keeping the old `.55/.72/.82` fills (would fail the AA check once the brighter ambient blobs replaced the flatter static wash). |
| D-116 | 2026-07-15 | In-app multi-page Help guide replaces the bible deep-link | **`web/src/help/HelpGuide.tsx`, a ~880px Radix Dialog, plus `web/src/help/pages/*` ship 7 bundled pages (`HELP_PAGES`): `welcome`/"Welcome to K", `messaging`/"Messaging K & dispatching", `runs`/"Runs & reviewing changes", `projects`/"Projects, bibles & artifacts", `agents`/"Agents & the org", `insights`/"Insights & budget", `settings`/"Settings & shortcuts" — a left page rail plus an article body, `ArrowLeft`/`ArrowRight` and Prev/Next navigation. The sidebar's Help entry now opens this dialog; it no longer navigates to the compiled bible. No backend involved — content ships bundled with the web app.** | Bundling help content in the app removes a runtime dependency on the bible being compiled and served, and lets help text version alongside the UI it documents. A fixed 7-page rail follows the app's own information architecture (messaging, runs, projects, agents, insights, settings) rather than mirroring the bible's own section structure, which readers of the app have never seen. | Iframe-embedding the compiled bible HTML (couples help availability to bible-build freshness, and the bible's section IA doesn't map 1:1 onto the app's); one long scrolling help page (defeats task-oriented, page-per-topic navigation). |
| D-117 | 2026-07-15 | Artifact registry: `artifacts.project_id` + `origin`, filesystem scan makes ArtifactsTab a gallery | **`core/src/artifact-scan.ts` — `scanProjectArtifacts(projectId)` sweeps `<localPath>/artifacts/*.html` (top-level only, no recursion); `scanHarnessArtifacts()` sweeps the harness's own `ARTIFACTS_DIR` (`projectId` NULL). Both are `isPathWithin`-guarded (`resolveScannedFile` rejects symlink escapes via `realpathSync` + a containment check) and idempotent: an unchanged already-registered file is skipped, a vanished scanned row's file is deleted, and compiled (non-`scanned`) rows are never touched or overwritten. A file already backed by an explicit `html_path` match, or whose basename names an existing slug's implicit `ARTIFACTS_DIR/<slug>.html` fallback, is skipped — this is what stops the compiled bible / ui-demo from double-registering as `scanned` duplicates. Triggered on every terminal run (`startArtifactScanOnRunTerminal`, boot-wired via the event bus) and on demand via `POST /api/projects/:id/artifacts/scan` (ArtifactsTab's "Refresh from disk" button).** | Agents already drop ad hoc HTML reports/demos into a project's `artifacts/` directory with no registry entry — a guarded, idempotent scan surfaces those without requiring agents to call a registration API, while the path guard and skip-on-existing-match rules preserve the compiled bible/demo as the one privileged, non-overwritable pair per project. | Recursive directory scanning (larger path-escape surface for no current use case — nested artifact directories aren't a real pattern yet); requiring agents to self-register artifacts via an API call (an extra step agents reliably forget; a passive scan reports what was actually produced, not what was declared). |
| D-118 | 2026-07-15 | First-class Changes/review surface: `ChangesLayout` + DiffViewer v2, full-screen `pr-review` route; PR side read-only | **`ChangesLayout.tsx` pairs a left file list (per-file `+`/`-` stat, a persisted `viewed` ✓ via `getViewed(identity)`) with a right `DiffViewer` v2 pane. `DiffViewer` renders `refractor`-tokenized code inside a `.code-viewer` region colored entirely by the `--code-*` custom properties in `index.css`, via `refractor`'s own `token <type>` class names — `DiffViewer` itself never tracks a token-to-color mapping. A `readOnly` prop disables comment add/delete and file-expand affordances. `#/pr-review/<projectId>/<n>` (`PrReviewPage.tsx`, registered in `route.ts`'s `KNOWN_VIEWS`) is a full-screen, read-only view of a GitHub PR's diff, reusing the same `ChangesLayout` with `readOnly` set; comments stay run-only and are never posted back to GitHub. `PrsCiTab` gains an "Open review" action linking into that route.** | Reusing one `ChangesLayout`/`DiffViewer` for both run-review and PR-review — rather than building two diff UIs — keeps the review experience (viewed marks, file list, code coloring) identical regardless of what is under review. Making the PR path explicitly read-only is an honest boundary: K has no GitHub-comment-write plumbing today, so the UI doesn't imply one. | A separate, lighter diff view built just for PRs (would drift from the run-review UI over time as one surface gets features the other doesn't); allowing PR-side comment writes now (no GitHub-write plumbing exists — would be a fabricated capability). |
| D-119 | 2026-07-16 | Executable pipeline engine (the deferred D-012 `workflow_stages`) — a multi-run DAG: agent/deterministic/gate/hook stages, per-edge handoff, declarative+dynamic gates, retry-in-place, `delegate_pipeline`, live Pipelines UI | **A `PipelineSpec` (stored in the new `workflow_definitions.spec`, Zod in `@k/shared`) is an executable DAG: stages of kind `agent` (a supervised `startAgentRun`), `deterministic` (zero-token `command`/`verify` script), `gate` (human checkpoint), or `hook` (a script returning a `HookResult`); edges carry a per-edge `handoff` (`share-tree` = fork at the upstream `result_commit` incl. fan-out, `branch` = fork at the pipeline base, `merge` = 3-way fan-in) and a `when` condition (`always`/`pass`/`fail`/`repair`). A main-process `PipelineEngine` + `pipeline-scheduler` drives it exactly like the lead-dispatch relay — a DB ledger (`pipeline_runs`/`pipeline_stages`/`pipeline_edges`) + a `draining` latch + per-stage CAS claim (`pending→dispatched`) on an unref'd interval; readiness is an AND-join over `always`/`pass` predecessors plus `fail`-activation, `markSkips` dead-branches unreachable stages to `skipped`, and a skip-aware `maybeFinalizePipeline` completes/fails on quiesce. Stage-to-stage state hands off through the existing checkpoint chain: each stage forks a worktree at a computed base commit and its output is its terminal checkpoint SHA, written to a sweep-immune `refs/k-pipelines/<pipelineRunId>/<stageKey>` (a new `sweepPipelineRefs` reaps them by `pipeline_runs` existence). Failure retries IN PLACE via the retry brain factored out of self-heal (`classifyRunFailure`+`stampRetryLineage`), bounded by `StageDef.retry.maxAttempts`, at the SAME base commit on a fallback model — and a mandatory ownership guard (`runs.pipeline_stage_id`, the v14 sentinel, stamped on every dispatch incl. retries) makes the global self-heal SKIP pipeline runs so they never double-fire. Gates park at `awaiting_gate` (reusing the plan-gate park), resolve via a single-resolver CAS (`resolveGate`, 409 on a lost race), and can be inserted dynamically (`insertGate` + `repointEdgesTo`); `rewindPipelineToStage` resets a stage + its downstream closure for an operator re-run. Execution is behind a `StageExecutor` seam (worktree/process now; Docker later). K (or the operator) delegates via `delegate_pipeline` (a kstore tool → `pipeline_dispatches` queue → `pipeline-dispatch-relay` → `startPipelineRun`), and a single `onPipelineTerminal` callback reports the outcome back onto the K thread; `POST /api/pipelines/:id/run` is the same seam without K. `GET/POST /api/pipelines/*` (8 endpoints, budget-gated `/run`) + a `pipeline_update` WS delta drive an upgraded Agents→Pipelines UI (a hand-laid live DAG, per-stage cards, gate dialog, launcher). Schema v13→v14 (5 tables + the sentinel + `spec`). Three seeded reference pipelines (`code-wave` = implementer → parallel share-tree reviews → merge-join controller → gate → verify; `investigate`; `refactor`).** | Turns the prose-delegation-in-ONE-run NamedWorkflow (D-012/D-047) into real staged, per-stage-visible/retryable/gated pipelines — the substrate every later orchestration pillar builds on — while REUSING the proven relay, checkpoint-chain, self-heal, budget-governor, and plan-gate machinery rather than inventing parallel mechanisms (the whole engine is the lead-relay shape applied to a DAG). Handoff rides checkpoints (no long-lived pipeline worktree), so a mid-pipeline reboot is fully reconcilable; the `runs.pipeline_stage_id` back-ref (not `pipeline_stages.run_id`, which a retry overwrites) is what keeps the failed original recognized as engine-owned. Forward routing + retry-in-place covers the reference loop without the bug-prone automatic repair-LOOP. | A brand-new `workflow_stages` concept (the executable form EVOLVES `workflow_definitions` with a `spec` column — legacy rows lazily compile via `namedWorkflowToPipeline`); a long-lived pipeline-owned worktree (checkpoint handoff is reboot-safe and reuses existing plumbing); keying the self-heal guard on `pipeline_stages.run_id` (a retry overwrites it → the failed original double-fires); silent fan-in conflict auto-resolution (surfaced as a stage failure). **Deferred (documented):** repair-LOOP back-edges (re-running an already-passed upstream stage — forward routing + retry instead); `commit`/`ci` deterministic actions (agents open their own PRs); hook `inject` cross-stage propagation + agent-hook structured `HookResult` (Phase-3 injection); run-internal operator hooks (PreToolUse/PreSkill generalization — a Phase-1.5 fast-follow, operator-chosen); the full ContextAssembler injection intelligence (Phase 3); auto-pipeline-selection from a vague request (Phase 2); the Docker `StageExecutor` backend (Phase 4). |
| D-120 | 2026-07-16 | Orchestration Program Phase 2 — Pipeline Library, IA & Orchestration Visibility | **Made the Phase-1 engine usable across eight fronts, built on `feat/orchestration-p2` (schema v14→v15) via a W0 shared-contract foundation → three parallel reviewed worktree lanes (Lane A engine, Lane B registry/schedules, Lane C UI/integration) → serial integration → two opus SEAMS reviews (both approved). (1) Agents IA restructured to top tabs **Org / Catalog / Automations** (`AgentsPage` `TAB_IDS`); Catalog holds 4 sub-tabs **Skills / MCP / Hooks / Sub Agents** (`CatalogPage`); Automations unifies **Library / Runs / Schedules** into one pipeline surface (`AutomationsView`), replacing D-101's Org·Skills·Pipelines 3-tab split — legacy `#/agents/skills/*` and `#/agents/pipelines/*` deep links redirect in (`route.ts` `VIEW_REDIRECTS`). (2) An **editable sub-agent worker registry** (`sub_agent_defs` table + `SubAgentDef`): K-native workers parsed live from `agent-config/agents/*.md` (`source:'k'`, read-only, "Fork to edit" clones into an operator row) alongside full-CRUD **operator workers** (`GET/POST/PATCH/DELETE /api/sub-agents`, `SubAgentsTab`/`SubAgentEditor`); a pipeline `agent` stage now names its worker via `subagentType`. (3) **Bounded loops** — `when:'loop'` edges + `StageDef.maxIterations` (≤10, Zod-capped, rejected on the done sink) with a corrected `maybeFinalizePipeline` that accounts for in-flight loop iterations so a pipeline never reports a false `COMPLETED` mid-loop. (4) A **standard pipeline library** — 6 seeded, executable defs (Implementation Cycle, Deep Research, Bug Triage & Fix, Refactor, Security Audit, Quick Task) composing sequential/parallel/loop/gate patterns with K-native workers as stage actors. (5) **Legacy migration** — `NamedWorkflow` templates and `type:'workflow'` skills convert to `PipelineSpec` defs at BOOT (idempotent, fail-safe — a conversion error degrades that one row, never blocks boot), and the legacy `WorkflowsView` UI is RETIRED. (6) A **progress ledger** (`pipeline_ledger`, per-run append-only, instrumented at every engine transition) surfaced via `PipelineLedgerPanel` + a `pipeline_update` WS `ledgerSeq` cursor, and an **orchestrator multi-pipeline view** grouping runs by owning orchestrator via `pipeline_runs.owner_profile_id` (stamped from the delegating orchestrator at dispatch, incl. retries). (7) **Pipeline cron schedules** — `skills.pipeline_def_id` lets a scheduled routine target a pipeline (manual + cron; `routines.ts` resolves it onto `RoutineView.pipelineDefId`), surfaced on Automations → Schedules. (8) UI polish — a full input-sweep of raw `<input>`/`<textarea>`/`<select>` to the `ui/Field.tsx` primitives across the touched surfaces, and the page-header **tab icon removed** from `TopBar`'s title (title/breadcrumb only now). Schema v14→v15 (ledger + sub-agents + loop/owner columns; the `SCHEMA_SENTINEL` moved to `skills.pipeline_def_id`, a `migrateSlow()`-added column, after review caught it originally targeting the unconditional-DDL `pipeline_ledger` table).** | The Phase-1 engine (D-119) was real but unreachable — no worker roster to point a stage at beyond hardcoded subagent names, no seeded pipelines to run day one, the legacy Workflows UI still the only visible entry point, and no cross-run visibility into what an orchestrator's pipelines were doing. Phase 2 closes exactly that usability gap: a registry gives stages a nameable, editable actor; a seeded library gives the operator something to run immediately; boot-time legacy migration retires the dead-end UI without breaking existing `NamedWorkflow` rows; the ledger + `owner_profile_id` view make a multi-pipeline orchestrator's work legible before the autonomous supervision loop (a later phase) needs to reason over it. Three parallel lanes map to the natural seams of the work with a cross-lane contract frozen at W0 (shared Zod schemas + the sentinel-column fix landed before any lane branched); two opus SEAMS reviews (both approved) is the heavier bar the program's size earned. | **Operator-worker EXECUTION now** (the create-time materializer + a flat-vs-nested subagent-discovery fix is its own design+security pass — deferred to **Phase 2.5**, documented rather than half-wired); an **autonomous multi-pipeline supervision loop** now (the ledger + hook seams this wave built are what it will consume — premature without them, a later phase); **event triggers** for pipelines (manual+cron cover the shipped library; an event-trigger seam is a later increment); **loop re-entry re-forking from the prior iteration's result tree** (cross-stage tree-carry is a Phase-3/injection-intelligence concern — re-forking from the loop head's PRE-loop base is the simpler, correct-for-now semantics — deliberate); **intent-driven auto-pipeline-selection from a vague request** (re-homed to **~Phase 5** — it held the "Phase 2" label in the roadmap's later-phases list before this wave claimed Phase 2 for real shipped scope). |
| D-128 | 2026-07-17 | UI Refinement — modern-frosted glass, user-settable wallpaper (supersedes the animated backgrounds), pipeline graph in the definition inspector, scroll-nav Settings sidebar | **A four-lane refinement on `feat/ui-refinement` (parallel implementer lanes → integration → opus whole-impl review [APPROVE-WITH-FOLLOWUPS] → live Playwright visual smoke), NO SCHEMA-VERSION BUMP (background settings ride `app_config` key `ui.background` as JSON; the wallpaper image is a file at `<DATA_DIR>/wallpapers/wallpaper.<ext>`; legacy `galaxy/blobs/aurora` values migrate in-read). **(A) Pipeline graph in the definition inspector** — `PipelineDefInspector` now renders the `PipelineGraph` DAG above the stage/edge lists via `lib/pipeline-preview.ts` `previewViewFromSpec(spec)` (synthesizes `pending` stages from a `PipelineSpec`; `attempt:0` → `PipelineStageNode` hides the attempt line, which is also correct for real pending stages), so analyzing a *definition* shows a graph, not just text (previously the React Flow graph rendered only for a *run*). **(B) User-settable wallpaper replaces the animated background system** — the D-121 `<Background variant>` enum (galaxy starfield / aurora / blobs / solid), `lib/starfield.ts`, and the retired `<Ambient/>` give way to `BackgroundSettings {kind: solid|gradient|image, preset, imageVersion}` (`@k/shared`): a solid `--bg-deep`, one of four static CSS gradient presets (`aurora/dusk/ocean/ember`, recombining the retained LG2 blob tokens), or an uploaded image. Because every `/api/*` route is Bearer-gated a raw `<img>`/CSS `url()` can't attach the header, so the image is fetched as an authenticated Blob → `URL.createObjectURL` object URL (`lib/useBackgroundImageUrl.ts`, revoked on change/unmount); `PUT /api/settings/background/image` validates `image/png|jpeg|webp`, ≤8 MB decoded, writes a fixed basename, and sets a route-scoped `bodyLimit`. Settings→Appearance becomes a solid/gradient/image picker with a FileReader→dataURL upload + live preview. **(C) Modern-frosted glass** — the D-114/D-115 purple liquid-glass tokens retune to neutral low-alpha frosting (`--glass-*-bg` → dark-neutral ~0.42–0.58, `--glass-tier-border`/`--lg-edge`/`--lg-sheen` → white low-alpha), blur/saturate up to 22–28px / 1.6; the D-115 SVG refraction (`GlassFilterDefs.tsx`, `#lg-refract`, `--lg-refract-scale`) and the living-ambient blob ANIMATION are removed (the `--lg-blob-*` tokens live on, now feeding the static gradient presets; `tokens.test.ts` names stay stable — values only). **(D) Settings sidebar scroll-nav** — `SettingsNav.tsx` replaces `<a href="#id">` anchors (which hijacked the hash router → 404) with `<button onClick={scrollIntoView}>` + an IntersectionObserver active-section highlight, never writing `location.hash`. Whole-impl opus review APPROVE-WITH-FOLLOWUPS (zero Critical/Important; M2 wasted-blob-fetch + M3 revoked-URL re-upload flash fixed); the previously library-default WHITE React Flow MiniMap/Controls given `colorMode="dark"` (fixes the run view too).** | The operator asked for four concrete corrections to the P2.6 wave: the animated galaxy/ambient "rendered scene" should just be a standard settable wallpaper you can load your own image into; the liquid glass looked "early-2000s" rather than the modern translucent frosting of recent iOS/Linux; the pipeline graph wasn't visible when analyzing a pipeline *definition* (only a run); and the Settings sidebar was "clunky and unworking" — its anchor `href`s navigated the hash router away to a 404. Each is fixed at the smallest surface: the wallpaper is JSON in `app_config` + a file (no schema bump, no new table — same rationale as D-121's background pref); the glass change is token *values* only (names stable, so `tokens.test.ts` and every consumer are untouched); the def-inspector reuses the existing `PipelineGraph` with a synthesized preview view (no engine change); the sidebar is a button + `scrollIntoView` (the only correct pattern under a hash router). The authenticated-Blob → object-URL image path keeps the Bearer token out of any URL and needs no route auth-exemption. This SUPERSEDES the animated-background half of D-121 (galaxy/starfield/ambient) and the refraction + living-ambient half of D-115. | Exempting the wallpaper image route from auth or putting the token in the URL (leaks the token / weakens the gate — an authenticated Blob fetch is correct); a SCHEMA_VERSION bump for the wallpaper (JSON in `app_config` + a file suffice, as with the D-121 pref); keeping the animated galaxy/ambient/refraction (the operator explicitly rejected the "animated rendered scene" and the early-2000s glass); an unauthenticated client `<img src>` for the wallpaper (401 under the Bearer gate); `<a href="#section">` sidebar anchors (the exact hash-router hijack that produced the 404); deleting the `--lg-blob-*` tokens along with the animation (they're reused by the static gradient presets); a full `ui-artifact.ts` demo-generator rewrite in this wave (the self-contained demo's refraction/ambient prose is now stale — tracked as a follow-up, not sold as current). |
| D-121 | 2026-07-17 | Usability & Access (Phase 2.6) — React Flow pipeline canvas, galaxy background system, per-agent default models, unified Access console + worker ceiling validation (D-121) | **A usability + access-control wave on `feat/usability-access-p2-6`, built W0 shared-contract foundation → three lanes (serial on one branch, per-lane spec+quality reviewed) → integration → two opus SEAMS (correctness + security), with NO SCHEMA-VERSION BUMP (the background preference rides `app_config` key `ui.background`; the Access matrix reads existing tables; the model list is a query-time aggregate). **Lane A** rewrites `PipelineGraph.tsx` onto `@xyflow/react` v12 + `@dagrejs/dagre` (a pure, sync `layoutPipeline` in `lib/pipeline-layout.ts` — `rankdir:'LR'`, drops `from:null` entry edges, keeps loop back-edges retargeted, maps `to:'done'`→a `'__done__'` sink) as an INTERACTIVE READ-ONLY VIEWER (pan/zoom/minimap/controls, ephemeral drag-reposition — not persisted), preserving the exact `PipelineRunView` data contract and live `['pipeline-run', runId]` query cache (no new WS plumbing); custom `PipelineStageNode` (status dot, gate pill, cost/attempt) is a keyboard-activable `role=button` (Enter/Space → `onSelect`, single tab-stop via `focusable:false`), and pass-edge marching-ants animation is frozen under `prefers-reduced-motion`. **Lane B** replaces the single always-on `<Ambient/>` with one route-agnostic `<Background variant>` at Shell z-0 (so glass has something to refract): `galaxy` (default) is a lightweight `<canvas>` starfield (`lib/starfield.ts`, rAF loop; a single static draw under `prefersReducedMotion()`; rAF + ResizeObserver cleaned up on unmount and variant change), plus `aurora` (CSS gradient wash), `blobs` (the retained `<Ambient/>`), `solid`; the variant is an app-wide preference (`GET/PUT /api/settings/background`, Zod-enum-guarded) with a Settings→Appearance picker; all four variants share one `data-testid="app-background"`+`data-variant` contract and are `aria-hidden`. The home chat rail + transcript are glassed (`glass-panel`, blur budget = 5, composer left solid), and the pointer-tracking `useGlassPointer` sheen (the `--lg-mx`/`--lg-my` radial) is DELETED in favor of a static `.glass-interactive:hover` sheen + `brightness(1.04)` lift (the `--lg-sheen` token is retained/reused). **Lane C** adds a unified available-models aggregate (`core/src/models.ts` `resolveAvailableModels` = Claude `KNOWN_MODELS` ∪ installed Ollama via `listInstalled()`, degrading to Claude-only + `localDegraded:true` when Ollama is unreachable — never a 500) at `GET /api/models/available`; relaxes per-agent model validation from `isKnownModel` to membership in that available set across `orchestrators`/`org-default`/`sub-agents` routes (`''`→`null` clear-override preserved, ids validated against a set and never shell-interpolated); makes the orchestrator page's previously-read-only model an editable default-model `<Select>` and re-sources the sub-agent editor's existing model Select from `KNOWN_MODELS` to the unified available-models list (the editor also swaps its skills/MCP chip fields for catalog-backed `CapabilityPicker`s); and adds a 4th Agents-hub tab **Access** (`AccessPage.tsx`) — a who-has-what matrix (orchestrator leads + sub-agent workers — K-native read-only + operator editable — × model/tools/skills/MCP) with expandable inline catalog-backed editing (patching through the same validated `api.orchestrators.update`/`api.subAgents.update` endpoints; K-native workers read-only) and a prominent **Auto-index** (capability rescan) button. The load-bearing security piece — **the D-121 ceiling gate** — closes the prior gap where sub-agent worker grants had NO validation: `POST`/`PATCH /api/sub-agents` now validate the resolved `{allowedTools,mcpServers,skills}` with `assertEffectiveGrants(WORKER_CEILING_TIER='orchestrator', …)` (`GrantError`→400), PATCH merging the patch arrays over the CURRENT row and validating the FULL merged set (a two-step smuggle can't slip a grant past an untouched field) then persisting EXACTLY those validated arrays; a K-native (read-only) worker is skipped by the gate and returns the canonical 403, not a misleading ceiling-400.** | Six real usability gaps + one security gap, addressed without a schema bump because none needs new persisted state. The hand-laid SVG pipeline diagram couldn't pan/zoom/minimap a large DAG; React Flow as a read-only VIEWER (not a persistence-backed structural editor) gives n8n-style navigation while keeping the definition the source of truth and the live-status cache untouched. Backgrounds are a single Shell-z0 layer (not per-page) so every glass surface refracts the same backdrop and there is one lifecycle to manage; a lightweight canvas starfield frozen under reduced-motion avoids a WebGL dependency and honors the motion budget. Merging Claude + installed-Ollama into one available-models list (degrading gracefully) lets any agent/worker take a local model as its default without a 500 when Ollama is down. The Access console makes the already-existing per-agent allow-lists + authority ceiling + rescan legible in one who-has-what surface — but the SERVER remains the authorization boundary (the console only calls validated endpoints). Most critically, sub-agent workers ran INSIDE an orchestrator's authority yet had NO grant validation — an operator could author a worker whose tools/skills/MCP exceeded the orchestrator ceiling; validating at the orchestrator tier (merge-then-validate, persist-what-you-validated) fail-closes that hole, and the two opus SEAMS (security verdict: SOUND — two-step-smuggle and over-ceiling defeated on create/patch/clone) is the bar the access-control surface earned. | React Flow as a full persistence-backed structural editor (out of scope — drag is ephemeral; the definition, not a saved layout, is canonical); a SCHEMA_VERSION bump for the background preference (unnecessary — `app_config` already stores UI prefs like home layout); per-page background components (n lifecycles, and glass on a page with no backdrop looks broken — one Shell-z0 layer instead); a WebGL/AFRAME galaxy (heavier + the prior react-force-graph blank-canvas gotcha class — a 2D canvas frozen under reduced-motion suffices); restricting model defaults to Claude `KNOWN_MODELS` only (the whole point is to also use installed local models); leaving sub-agent worker grants UNVALIDATED (the status quo — a real privilege-escalation gap the D-121 gate closes); returning a ceiling-400 for a read-only K-native worker patch (misleads a client into "fix your payload and retry" when the target can never be edited — the canonical 403 is correct). |
| D-122 | 2026-07-17 | Session layer — hybrid warm/resumable runtime for every durable agent *(supersedes D-062's one-shot-ONLY posture; resume kept as the cold path)* | **`agent_sessions` is a new core primitive: one row per (profile, conversation thread) with `cli_session_id`, a stable `home_dir` under `<dataDir>/sessions/<profileId>/<threadId>/` (never a worktree — `--resume` is cwd-bound, D-074), and a `live \| resumable \| stale` state machine. `live` = an interactive claude process parked `awaiting_input` between turns (the D-014 machinery, reused) — messages inject via stdin in real time; `resumable` = no process, the next message spawns `--resume <cli_session_id>` (the D-062 machinery, reused as the cold path); `stale` = resume impossible, the next message visibly reseeds from durable turns. Live sessions are capped (`K_MAX_LIVE_SESSIONS`, default 3, graceful LRU demotion); an idle timer (`K_SESSION_IDLE_MS`, default 10 min) and a boot sweep demote live→resumable; live-process death demotes to resumable, a rejected resume to stale. `cli_session_id` persists at the CLI's INIT event (not only on a clean `done`), so an errored turn keeps continuity. `askK` resolves thread → `ensureSession` → `sendToSession`; the same engine serves every durable profile's conversation.** | The product intent — K as a persistent, Claude-Code-grade engineering agent — was under-served by answer-and-exit one-shots: no instant replies, no real-time steering, a cold-resume tax on every turn of an active conversation. The hybrid buys warmth exactly while a conversation is active and demotes to the proven cheap path the moment it isn't; every mechanism (interactive park, idle timer, `--resume`, seed replay) is reused, not invented, and durable turns remain the source of truth at every state. | A single always-on process per agent (leaks, restart-fragile, unbounded cost — the D-023 rejection still holds); staying one-shot-only (cold latency + no mid-turn steering — the D-062 posture this supersedes); a bespoke chat runtime outside the supervisor (re-implements the D-014 machinery). |
| D-123 | 2026-07-17 | Conversations for every durable agent + the unified Messages surface | **`k_threads` gains `profile_id` (default `k-secretary`) and `last_read_at` — a thread is now a conversation for ANY profile over the unchanged threads/turns machinery. K keeps multi-thread; every other durable agent gets exactly ONE auto-created conversation (`getOrCreateConversation`). A new 7th rail destination, Messages (`#/messages`, chord `g m`), lists every conversation — K threads first — with unread badges (cursor-based; the operator's own delivered messages never badge), a session-state chip + context meter, per-segment sender bubbles for batched deliveries, and report-backs rendered as messages FROM that agent; transcripts render via a shared `ConversationView` extracted from `ChatView` (Home Chat, agent detail pages, and Messages are one component). Personal → Chats folds in (`#/personal/chats` redirects; thread management moves over intact).** | The Chief and leads had NO conversations — amnesiac activations seeded from goal strings — while the entire thread/turn/transcript machinery already existed for K; one added column generalizes it to the whole org for near-zero new surface. One-conversation-per-agent keeps the mental model simple (an agent is a colleague with one ongoing chat), and a single Messages surface makes report-backs legible where they land instead of scattered across bespoke views. | A new per-agent conversation table (duplicates `k_threads` wholesale); multi-thread for every agent (no use case — only the operator-facing K needs parallel topics); leaving agent transcripts embedded-only on detail pages (no unified unread/inbox view — the surface the wave exists to provide). |
| D-124 | 2026-07-17 | Mailbox + tier-gated `message_agent` + priority delivery *(urgent = interrupt NUDGE + boundary; the relay is main-process)* | **`agent_messages` (to-profile + optional to-thread, from `user\|profile`, body, priority `normal\|urgent`, `provenance_run_id`, status `queued\|delivered\|failed`) is a DB queue drained by a main-process relay (2 s tick; `MESSAGE_RELAY=0` opt-out — with it off, report-backs/briefings strand as queued rows). Delivery is by target-session state: live+parked → stdin NOW; live mid-turn → next turn boundary, where an urgent row additionally fires ONE control-protocol interrupt NUDGE (`sendInterrupt`, per-run cooldown, `K_URGENT_INTERRUPT=0` kill switch) that only ACCELERATES the boundary — the nudge never delivers, so an interrupt-less CLI degrades structurally to boundary delivery; idle → wake the session with the conversation's queued messages batched. Failure → row `failed` + an un-silenceable notification, never silent. `message_agent` (kstore, every tier) is gated by a pure matrix — K → anyone; manager → its domains' members + K; orchestrator → its manager + its running stages' actors; self-send DENIED — with the sender resolved from the calling run, never the payload. Delivered messages embed as provenance-tagged transcript blocks with relay-side escaping of lookalike tags, so a body cannot forge another sender. Report-backs (chief delegation outcomes, lead continuations, pipeline terminals) and mgmt `report` all queue through the mailbox into K's conversation; the terminal report-back de-dupes against the report tool's real-time dual-write.** | Running work could only be killed, never steered; report-backs were bespoke turn-appends invisible outside K's thread. A DB queue + state-routed delivery reuses the lead-relay shape (CAS claim, main-process drain — the session engine's in-memory attachments make delivery a main-process concern by construction); making the interrupt a nudge rather than a delivery channel means CLI interrupt support changes latency, never correctness; the tier matrix + unforgeable sender + escaping keep agent→agent speech attributable, and self-send denial is what keeps the supervisor's self-addressed briefing discriminator unforgeable (D-125). | Direct in-process delivery from the mgmt stdio child (dies with the child — the D-050 lesson); urgent-as-hard-interrupt with a fallback branch (a structural boundary degrade is honest; a kill-and-replan channel is a footgun); re-checking `mayMessage` at delivery time (would block the supervisor's self-addressed briefings — the boundary gate is the correct single enforcement point); trusting a sender field in the payload (forgeable). |
| D-125 | 2026-07-17 | Domains registry + the manager role + always-on supervision *(amends D-108's autonomy boundary)* | **`domains` is a first-class registry row (name, description, `manager_profile_id` — chief-tier-guarded on assignment); orchestrator profiles and pipeline defs carry `domain_id`, and launched work inherits it at dispatch, so a manager's oversight auto-attaches to everything running in its domain regardless of who launched it. `POST /api/domains` with a `manager` block creates the domain + a chief-tier manager profile in one call — dynamic managers are configuration, not code. `domain-supervisor.ts` (chief-wake-shaped, main-process, `DOMAIN_SUPERVISOR=0` opt-out) subscribes stage terminals / gate parks / failures / budget warnings + a `*/15` heartbeat that fires ONLY while the domain has active work, builds a briefing (progress-ledger delta since last wake, open gates, failures, budget state — capped) and queues it as a SELF-ADDRESSED message (to == from == manager; gate events urgent) delivered into the manager's conversation. Governed per-manager: min-interval debounce (urgent does NOT bypass it), rolling-hour cap (`domain_wake_max_per_hour`, default 6; `0` = hard off), suppressed wakes write NOTHING. The briefing discriminator is unforgeable because the supervisor's internal queue path is its sole author — `message_agent` denies self-send and mgmt `steer` denies self-steering. AMENDS D-108: supervision is CORE, always on — gated by the wake governor + caps only, NOT the Autonomous Org master, which still gates self-generated work (proposals, backlog auto-pull) unchanged.** | Oversight that only exists when a default-OFF master is flipped is not oversight — gates parked, stages failed, and budgets burned regardless; the operator accepted paid supervision outside the autonomy switch with three guards (active-work-only wakes, the governor, caps). Domains-as-rows make attribution automatic and managers growable by dialog (a Research manager is a row + an overlay, no deploy); the manager keyed governor + self-addressed discriminator bound the spend and make forged or DoS-ing briefings structurally impossible (SEAMS-verified triangle: tool self-send denial + steer self-ban + supervisor-as-sole-writer). | Gating supervision behind the Autonomous Org master (an unsupervised org whenever the switch is off — the boundary this amends); a manager tier (the chief tier already IS the manager ceiling — a new tier would fork every allowlist); urgent gate events bypassing the debounce (unbounded spend under a gate storm; the heartbeat re-surfaces open gates); ledger rows for suppressed wakes (noise dwarfing the real history — the chief-wake posture holds). |
| D-126 | 2026-07-17 | K = primary agent: engineering identity, read authority, in-context routing *(supersedes D-057's classifier-as-decision; retires D-046's mandatory K→Chief hop)* | **K's charter is rewritten as a top-tier engineering agent + harness expert on the new role layering (L0 base → L1 role template — the tier charters rewritten as primary/manager/orchestrator templates via `roleForTier()` — → L1.5 per-profile `identity_overlay`, operator-editable, `''` silences a seed / `NULL` reseeds, empty-overlay synthesis byte-identical → L2 project). K's authority becomes READ + ANALYZE: `Read`/`Grep`/`Glob` on the secretary allowlist + gitnexus granted PER-TOOL (the 13 read/analysis tools; `rename`/`group_sync`/`cypher` excluded at the GRANT layer on both delegating tiers — the SEAMS #2 (f) ruling, enforced by the tier ceiling, with a one-shot row swap for pre-narrowing DBs; the orchestrator/coding tier keeps server-level under run-level Trust Core checkpoints); NO Write/Edit/Bash/Task — every mutation stays a delegated run/pipeline. The keyword router (`routeForMessage`) is DEMOTED to a Dock preview chip; the `route.escalates → delegateToChief` auto-delegation branch is DELETED — K decides in-context per message (answer, read tools, `delegate_pipeline`, `message_agent`), and `forceRoute` becomes a mailbox send. Authority tiers themselves are untouched — the role drives prompts/UX only.** | K's differentiator is the harness, and a harness expert that cannot read the code it manages answers from hearsay; read tools + delegated writes keep the Trust Core audit trail intact while making answers evidence-based. A keyword classifier deciding delegation was the wrong brain in the loop — deterministic string-matching overruling an LLM that can actually read the request; demoting it to a preview keeps the UX affordance and returns the decision to K. The mandatory K→Chief hop made every engineering ask pay a Chief activation even when K could delegate a pipeline directly; managers now oversee (D-125) rather than gatekeep. | Giving K write tools (collapses the delegate-writes audit boundary); keeping the classifier as the decision (D-057's clause — deterministic keywords overruling in-context judgment); keeping the mandatory Chief hop (a paid middleman on every ask; oversight is the manager's job now, not routing); a SERVER-level gitnexus grant on the delegating tiers (the A.5 interim posture — carried to SEAMS #2, which ruled for per-tool narrowing; charter prose is not a grant boundary on the highest-injection-exposure profile). |
| D-127 | 2026-07-17 | `runs.kind` bookkeeping — chat turns stop being top-level jobs | **`runs.kind ∈ chat-turn \| job \| pipeline-stage` + `runs.session_id`, stamped by trusted in-process code at dispatch (`persistentSession` → `chat-turn`; the pipeline executor → `pipeline-stage`; default `job`) with a one-shot v16 backfill of historical rows. The Runs surface defaults to `job` + `pipeline-stage` with a "show chat turns" toggle — conversation turns belong to their conversations (§08 Messages). Policy reads `kind`: the budget posture (an OPERATOR-originated `chat-turn` is exempt; a PROFILE-originated session wake is budget-gated + relay-breaker-damped — SEAMS #2 (b)) and the wake governor's org-relevance filter switch from K-shaped special cases to `kind`/origin (identical gating for every pre-existing caller; the one deliberate change is arming the gate on agent-caused wakes); `GET /api/runs` gains a `kind` filter (enum-validated, deduped).** | Chat turns as top-level runs polluted every runs surface and forced budget/wake logic onto brittle profile-id special cases; one explicit discriminator makes "is this a conversation?" a stored fact rather than an inference, so UI filters, budget exemption, and wake relevance all read the same truth. Kind is unreachable from HTTP/MCP/queue input, so the exemption cannot be smuggled onto a paid dispatch. | Inferring kind from trigger strings at read time (trigger semantics drifted — the mailbox dispatches profile sends as `'delegation'`); a separate chat-runs table (forks the run lifecycle every consumer rides); exposing `kind` on dispatch APIs (an exemption-smuggling surface — trusted in-process stamping only). |
| D-129 | 2026-07-21 | Home chat is a messaging surface, not a run list *(UI Adjustments, Lane A)* | **Home's Chats rail reads `api.conversations.list()` filtered to `profileId === 'k-secretary'` (was the unfiltered `api.threads.list()` → `GET /api/k/threads`, which has no profile filter and leaked every orchestrator/agent conversation into Home). On the K surface, a relayed (non-user provenance) segment renders as a quiet centered muted system-note (day-separator styling) instead of a left-aligned agent bubble; direct you↔K turns and non-K (agent-detail) surfaces are unchanged. The rail gains messaging-service affordances (unread badge, search past >5 threads, `Row`-based rows). The per-message "view run" chip is removed from EVERY `ConversationView` surface — the run stays reachable from the Runs page and pipeline stage node/card (D-130). The message bar is a seamless glass pill (MessageDock composer inner box dropped; ConversationView composer restyled). The floating dock's own recent-threads picker also switched to `conversations.list(k-secretary)`, and `useAskK` now invalidates `['conversations']` so the rail + picker stay live after a send.** | The operator reported "weird system messages from other orchestrators appearing as chats" and a message bar that read as a form field. The leak's root cause was a listing endpoint with no profile filter; `conversations.list` already carries `profileId`/`unread`/`sessionState` (the exact partition the Messages page uses), so one data-source swap fixes the leak AND supplies the messaging-rail fields. Relay report-backs are real content but not chat, so they read as quiet notes. The chip cluttered every reply while the run is reachable elsewhere. The picker fix closed a functional cross-thread path (selecting a leaked non-K thread would append a turn into that agent's thread via `api.k.ask`). | Filtering the display client-side off `threads.list` (KThreadSummary carries no `profileId` to filter on); a core-side profile filter on `/api/k/threads` (would change behavior for the MessageDock selection-resolver and other consumers — deferred as a hardening follow-up, plus a `resolveAskThread` profile-ownership guard); gating the chip on `isK` only (the operator asked for no per-message chip anywhere — the run stays reachable from Runs/pipeline nodes). |
| D-130 | 2026-07-21 | Runs page consolidation — pipelines + agent runs, archive/delete via app_config *(UI Adjustments, Lane B)* | **The Runs page is the single home for active work: a segmented control "Agent Runs / Pipelines" (chats stay on Home). `PipelineRunsPane` moves off Agents→Automations onto the Runs page; AutomationsView keeps library+schedules; `route.ts` reserves `pipelines` as a `runs` param keyword (`#/runs/pipelines[/<id>]`) with a legacy `agents/automations/runs*` → `runs/pipelines*` redirect (replaceState). A pipeline stage node/card click opens the agent's live `RunConsole` via `stage.runId` (`navigate('runs', runId)`; stage-select highlight is the no-runId fallback) — this is how "see every ongoing agent's chat/context" is satisfied. A run's Artifacts panel lists artifacts whose `linkedRunId` ∈ its stage runIds (`GET /api/pipelines/runs/:id/artifacts`); ledger artifact rows click through to the DocViewer. Archive/permanent-delete/clear-finished ride `app_config` key `runs.archived` (JSON array — NO SCHEMA_VERSION bump): endpoints archive/unarchive/clear-finished/DELETE + `GET /api/runs?archived=include/only/exclude` (default exclude, every row self-tagged `archived`). Permanent DELETE is guarded (404 unknown → 409 running/queued → 409 not-archived → 204) and FK-safe in one transaction: removes the run row + its hard-FK children (events/review_comments/verify_results/run_plans), NULLs `artifacts.linked_run_id` and `runs.retry_of` (a self-FK with no ON DELETE), drops the id from the archived set, and never cascade-deletes threads or artifacts.** | The operator wanted active pipelines + agent runs on the Runs page, the ability to clear/select-delete runs, pipeline nodes that open the agent's current context, and visibility into pipeline artifacts. Archive-then-permanent-delete (recoverable by default) mirrors the wallpaper's no-schema `app_config` pattern, so no table/version change was needed. The delete guard + FK-safe transaction prevent orphaned-ref crashes; the `retry_of` self-FK was caught in review (deleting a self-heal ORIGINAL run threw `SQLITE_CONSTRAINT_FOREIGNKEY` → 500) and is now nulled in the transaction. | A SCHEMA_VERSION bump / a dedicated archived-runs table (JSON in `app_config` suffices — same rationale as the wallpaper pref); cascade-deleting threads/artifacts (destroys history — nulling dangling refs is correct); leaving chats on the Runs page (the operator kept chats on Home); ON DELETE SET NULL on `retry_of` (a migration on an existing column is riskier than nulling in the delete path); per-row app_config writes in clear-finished (batched to one read/write). |
| D-131 | 2026-07-21 | Glass everywhere — violet/graphite palette, no-blur glass-look, HD wallpaper *(UI Adjustments, Wave C / W0)* | **The palette shifts to a purple/white accent hierarchy over the retained purple background: `--text #c6cede` (pale graphite-blue body font), `--muted #8f99ad`, `--accent #a855f7` (violet), `--accent-hover #c084fc`, new `--accent-hi #e9d5ff`; `--bg`/`--surface`/`--raised` and the wallpaper system are unchanged, `--chart-1` stays frozen `#ff8fc0`, and every changed canvas token is mirrored in `lib/tokens.ts` (so `tokens.test.ts` and the graph/jsdom consumers stay in sync). Dense/repeated surfaces (fields, pills, list rows, checkboxes) adopt `.glass-control` — a no-`backdrop-filter` glass-LOOK (tinted bg + hairline border + inset sheen) — so "everything glass" respects the DEV-11 blur budget (real blur stays only on the chrome/panel/overlay tiers). The wallpaper upload cap rises 8 MB → 25 MB (route bodyLimit auto-derives ~35 MB) so HD/4K images render at native resolution, with the fixed-basename + triple-MIME (png/jpeg/webp) + path-safety guards and the authenticated Blob→objectURL render path all unchanged.** | The operator asked for "every icon/page/form to be glass, with different shades of purple/white for hierarchy accents, and a pale graphite blue font," while keeping the purple background and user wallpaper. A blur-budget-safe glass-look class lets dense surfaces read as glass without the >6-region `backdrop-filter` perf cliff; changing only token VALUES (not names) keeps every consumer and the token gate intact. The HD cap is a one-constant change (the bodyLimit derives from it) preserving the existing security guards and no-re-encode storage. | Neutralizing the background to graphite (the operator kept purple); real `backdrop-filter` on list rows/fields (blows the DEV-11 budget); renaming tokens (breaks `tokens.test.ts` and every consumer); changing `--chart-1` or making `--muted` non-6-hex (violates the test contract); re-encoding/downscaling the wallpaper server-side (loses native resolution). |
| D-132 | 2026-07-21 | Knowledge graph — fully-expanded, slow auto-rotate, neutral glass palette *(UI Adjustments, Lane D)* | **The 3D knowledge graph renders fully laid out with NO settle/expansion animation: a two-phase mount (mount `ForceGraph3D` on an empty sentinel first so `configureGraphForces` runs before real data, then `warmupTicks` settles headless + `cooldownTicks=0` freezes, then a one-shot `zoomToFit`) — never calling `d3ReheatSimulation` (which crashes the 3D engine). A `requestAnimationFrame` loop slowly orbits the camera azimuth (react-force-graph-3d ships TrackballControls with no `autoRotate`), pausing on pointerdown and resuming only on pointerup/pointercancel + wheel (so a sustained drag never fights the user) and while the inspector is open, and disabled under `prefers-reduced-motion`. Free zoom/orbit stay on. Clicking a node no longer flies the camera (the disorienting drill-in) — it opens the glass-overlay inspector (details + View-in-Runs + Dispatch) only. Colors neutralize to match the glass: a new `--graph-bg #14161c` graphite canvas token (mirrored index.css↔tokens.ts), `GRAPH_COLORS.dim` and `GRAPH_LINK_COLOR` ride `--muted`, `ok` rides `--accent`. `forcesReady` resets across a `GraphErrorBoundary` remount (onReset + mount-gen key) so the two-phase mount can't be skipped after a WebGL crash.** | The operator wanted "a rendered graph that is slowly rotating with the ability to zoom and control rotation, rather than navigating through a whole graph," fully expanded with no expansion animation, and neutral colors matching the glass. Pre-warm-then-freeze avoids the reheat crash while removing the visible spread; the rAF orbit with proper pointer-release tracking gives casual rotation that always yields to the user; removing the camera-fly keeps the inspector's value without the disorienting navigation. A canvas-bg token decoupled from the purple `--bg` reads calm behind the glass overlays. | Calling `d3ReheatSimulation` to re-settle (crashes the WebGL canvas — documented in `graph.ts`); resuming auto-rotate on a flat timer from pointerdown (fights a >1.5s drag); keeping the camera-fly on node click (the "navigating through the graph" the operator rejected); reusing the purple `--bg` for the canvas (reads busy behind glass); gating the two-phase mount on node-count alone (skips phase 1 on a boundary remount). |
| D-133 | 2026-07-21 | UI Adjustments Round 2 — glass shade/opacity system, message-bar de-form, Home=Overview, Runs rework, orchestrator-update routing, KG crash fix *(supersedes D-131's theme-recolor mechanism)* | **A second W0-then-4-lane correction pass on `feat/ui-adjustments` (commits W0 `f31bc99` · Lane A `09767f8` · Lane D `d9bd53e` · Lane B `5457b6c` · Lane C `e65faef`), full core 2853 + web 1560 + build GREEN, whole-impl opus review APPROVE-WITH-FOLLOWUPS (7/7 global constraints held). **(1) Glass shade/opacity system (W0)** — new `--glass-1..4` (graded white→purple, rising alpha = prominence) + `--glass-active` (blue interaction wash) tokens in `index.css`, mirrored in `lib/tokens.ts`; `.glass-chrome`/`.glass-panel`/`.glass-control` become OPACITY-TINT (backdrop-filter dropped) so "everything glass" keeps the wallpaper sharp within the DEV-11 blur budget, real frost confined to `.glass-overlay` + opt-in `.glass-blur`; legacy `--glass-*-bg` repoint onto the new scale. Hierarchy now comes from glass shade, not theme recolor — `--bg`/`--accent`/`--text`/`--chart-*` are UNCHANGED from pre-D-131. Blue = interaction (hover/press/selected), purple/white = resting. **(2) Glass application** — solid holdouts (Tag, ErrorState, SectionHeader/Tabs badges, Row, Button ghost, MetricCard, ProjectCard, Toast, NotificationBell, VerifyChip, SegControl, FeedRow, PlanCard, LessonCard) convert to graded glass; hover/selected → `glass-active`. **(3) Message bar de-form** — `Field variant='bare'` drops the glass-control box + focus glow on the Home/agent-conversation composers, so the bar reads as a bar, not a form. **(4) Home = Overview** — the Chat|Overview SegControl is removed; `OverviewView` sits under an "Overview" header; sending from the Home bar redirects into the Chats tab with the K conversation open (`navigate('messages', threadId)`). **(5) Orchestrator-update routing** — `continuePipelineOutcomeToK` now delivers a pipeline outcome to the RESOLVED OWNER's own conversation when the owner is a real orchestrator/manager (not unconditionally to k-secretary); the client renders it as an "Update from {pipeline}" Systems notification, never a bare profile name; `resolveAskThread` gained an ownership guard (explicit foreign `threadId` → not-found). The launch side adds an optional tier-validated "Observed by" `orchestratorId` on the pipeline-run route → `owner_profile_id` (default = the def's domain manager) — all via the pre-existing column. **(6) Runs rework** — a collapsible NarrativeCard (per-run localStorage), the "Show chat turns"/"Show archived" toggles replaced by an Active|Archived sub-segment, a floating selection popup (Archive/Archive-all/Delete), a permanent-delete ConfirmDialog mirroring Messages, run detail + runs aside wrapped in their own glass-panel. **(7) KG soft-crash fix** — the auto-rotate rAF loop now includes `mountGen` in its deps (re-arms on a GraphErrorBoundary remount), null-bails before any ref method, and try/catches its own tick so a torn-down-instance throw can't escape the boundary and blank a POPULATED graph panel. **(8) Settings** — a font-color picker (`app_config` key `ui.fontColor`, GET/PUT + Zod-validated, the Background applier writes `--text` at runtime), "Replace image…" renamed "Set background", and a non-blocking upscaling warning when an uploaded wallpaper is smaller than screen×dpr (the reported "blurry HD" wallpaper is CSS upscaling of an under-sized source, not a lossy store/serve path — confirmed byte-lossless). NO SCHEMA_VERSION bump (stays 16).** | Round 1 (D-131) reskinned the THEME — recoloring `--accent`/`--text` violet/graphite — and the operator said it missed the point: the ask was to reskin the GLASS, not the color scheme. Round 2 corrects the mechanism (graded glass shade/opacity carries hierarchy; theme stays put) while closing a batch of concrete bugs the operator hit live: a random orchestrator's pipeline update landing in K's own chat (traced to `continuePipelineOutcomeToK`'s unconditional-to-k-secretary default), a knowledge-graph panel going permanently blank after a WebGL crash (the auto-rotate rAF loop firing on a torn-down ref outside React's commit cycle, so the boundary never saw the throw), a message composer that read as a form, and a wallpaper that looked soft despite a byte-lossless store path (diagnosed as CSS upscaling of an under-sized source). | Iterating further on the D-131 theme palette (addresses the wrong layer — the ask was about glass, not color); real `backdrop-filter` on every converted surface (blows the DEV-11 ≤6-blurred-region budget — opacity-tint carries the same visual grading without the perf cost); routing every pipeline outcome to k-secretary unconditionally (the root cause of the orchestrator-leak bug); a hard "fix" for wallpaper crispness (the image genuinely is under-sized for the display — a dimension probe/warning is honest, a silent upscale or reject is not). **Deferred (documented, non-blocking):** a keyboard-only focus ring for the bare composer (`:focus-visible` matches on click for a text input too, so CSS structurally cannot give a keyboard-only affordance — the operator's explicit remove-the-highlight request is honored as asked, WCAG tradeoff flagged); reconciling the diverging selected-state glass convention (Row blue vs SegControl purple vs an un-migrated Sidebar — operator eyeball decision); finishing the everything-glass sweep (message bubbles/dropdowns still opaque); removing the now-dead `.glass-active` CSS class. |
| D-134 | 2026-07-22 | UI Adjustments Round 3 — color-by-role glass hierarchy, built-in terminal removed, knowledge-graph background worker, orchestrator-update routing hygiene *(supersedes the glass posture of D-131/D-133; branch continues `feat/ui-adjustments`)* | **(a) Color-by-role glass hierarchy, superseding D-131/D-133's glass posture (which tinted panels purple and left many surfaces opaque):** panels/forms/sidebars/navbars/cards/widgets are translucent WHITE glass that warms to a light purple-pink with nesting depth (`--glass-1..4`); main icons + chips are pinkish-purple (`--glass-icon`/`--glass-icon-strong`, glyph `--icon-glyph` `#e7a8e4`); hover/click/active/selected is sky-blue (`--glass-active` + `--glass-active-edge`, `--glass-hover` on plain hover); the message bar is a visible `.glass-bar` pill (`--glass-2` fill) with a pink-purple hover/focus ring (`--glass-icon-edge`, the one operator-specified exception to blue=interaction); code/diff legibility surfaces (DiffViewer, DocViewer, ToolCall) use a dark-neutral `--glass-code`; `.surface-solid` is repointed from opaque `--surface` onto `var(--glass-3)` so every `tier="solid"` panel upgrades to glass at once. **No theme recolor** (`--bg`/`--accent`/`--text` unchanged from pre-D-131); **NO SCHEMA_VERSION bump** (stays 16). **(b) Built-in terminal feature removed entirely** — `TerminalPage.tsx`, `lib/terminal.ts`, `core/src/terminal.ts`, the `/ws/terminal` route, the `@xterm/*` + `node-pty` dependencies, the `ENABLE_TERMINAL`/`TERMINAL_TOKEN`/`TERMINAL_ALLOW_REMOTE` env plumbing, and every dedicated test/doc/screenshot reference are deleted; run-lifecycle "terminal" run-state (`terminalRuns`, `onRunTerminal`, `TERMINAL_RUN_STATUSES`) and general WS/loopback auth (`wsTokenOk`, `isLoopbackHost`) are untouched. **(c) Knowledge graph moves its force layout off the main thread** — a Web Worker (`graph-layout.worker.ts`, plain `postMessage`, no new dependency) runs `d3-force-3d` to a bounded tick budget and posts back positions the main thread applies as fixed `fx`/`fy`/`fz` (`warmupTicks=0`, `cooldownTicks=0` — zero main-thread simulation); a top-N-by-degree LOD cap plus a "Computing layout…" overlay and a watchdog guard large graphs, and the pre-existing two-phase mount + `mountGen` rAF soft-crash guard (D-133) is preserved. **(d) Orchestrator-update routing hygiene** — a pipeline-outcome update owned by the generic seeded `default-orchestrator` profile now resolves to the pipeline's **domain manager** (falling back to the delegating thread rather than authoring into the generic conversation), and the never-authored generic `default-orchestrator` conversation is hidden from the Messages list/bucketing — closing the "stray orchestrator message" case D-133's owner-based fix didn't cover. | Round 2 (D-133) still read as "pure transparent white" with panels the operator called "still pure opaque purple" across Overview/Project/Insight/Settings, and the role→color mapping the operator actually meant — icons/interactions carry the accent color, panels stay a bare translucent wash — had never been expressed, so two rounds in a row mistuned the same opacity/theme layer instead of assigning color BY ROLE; a published swatch made the mapping unambiguous before the reskin, not after. The message bar had been over-corrected into invisible (Round 2's bare-composer fix dropped the whole surface along with the form affordances it was asked to remove). A stray `default-orchestrator` message was still reaching Messages because D-133's fix only handled a *resolved, real* owner — the generic seeded profile had no fallback. The knowledge graph froze the page because its force simulation ran on the main thread regardless of node count; moving it to a Worker fixes the freeze at its source instead of only capping node count. The terminal was flagged for outright removal, not another gating layer. | Further opacity/shade tuning without a role mapping (repeats the D-131/D-133 mistake — recoloring the same layer instead of assigning color by role); gating the terminal further (`ENABLE_TERMINAL`, a stricter token) instead of deleting it (the operator asked it gone, not hidden); capping the knowledge-graph node count alone without moving layout off the main thread (treats the symptom, not the freeze's main-thread `d3-force-3d` cause); routing every pipeline outcome unconditionally to `k-secretary` or leaving `default-orchestrator` conversations visible (re-opens the D-129/D-133 leak for the unowned-orchestrator case); a new runtime dependency (comlink or similar) for the worker (`postMessage` + the existing `d3-force-3d` suffice); a theme recolor (`--bg`/`--accent`/`--text` stay exactly as D-133 left them). |
