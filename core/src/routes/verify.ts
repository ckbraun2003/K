// core/src/routes/verify.ts (W0 stub — Lane B lands the endpoints)
import type { FastifyInstance } from 'fastify'

/** E-04 verify routes (run verify-result read / project recipe PATCH). */
export async function verifyRoutes(_app: FastifyInstance) {
  // Lane B fills this plugin. Registered at W0 so lanes never edit index.ts.
}
