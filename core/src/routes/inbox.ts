// core/src/routes/inbox.ts (W0 stub — Lane B lands the endpoints)
import type { FastifyInstance } from 'fastify'

/** E-05 Approvals Inbox routes (union list / dismissals). */
export async function inboxRoutes(_app: FastifyInstance) {
  // Lane B fills this plugin. Registered at W0 so lanes never edit index.ts.
}
