// core/src/routes/feed.ts (W0 stub — Lane B lands the union projection)
import type { FastifyInstance } from 'fastify'

/** E-09 Org Timeline feed routes (read-time union projection over runs/events/notifications). */
export async function feedRoutes(_app: FastifyInstance) {
  // Lane B fills this plugin. Registered at W0 so lanes never edit index.ts.
}
