// core/src/routes/merge.ts (W0 stub — Lane C lands the endpoints)
import type { FastifyInstance } from 'fastify'

/** E-06 one-click merge + auto-merge routes. */
export async function mergeRoutes(_app: FastifyInstance) {
  // Lane C fills this plugin. Registered at W0 so lanes never edit index.ts.
}
