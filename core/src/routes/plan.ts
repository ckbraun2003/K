// core/src/routes/plan.ts (W0 stub — Lane A lands the endpoints)
import type { FastifyInstance } from 'fastify'

/** E-02 Plan Gate routes (plan read / edit / approve / discard). */
export async function planRoutes(_app: FastifyInstance) {
  // Lane A fills this plugin. Registered at W0 so lanes never edit index.ts.
}
