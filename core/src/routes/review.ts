// core/src/routes/review.ts (W0 stub — Lane A lands the endpoints)
import type { FastifyInstance } from 'fastify'

/** E-01 Review Deck routes (diff / comments / request-changes / approve). */
export async function reviewRoutes(_app: FastifyInstance) {
  // Lane A fills this plugin. Registered at W0 so lanes never edit index.ts.
}
