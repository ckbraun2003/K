// core/src/routes/rewind.ts (W0 stub — Lane C lands the endpoints)
import type { FastifyInstance } from 'fastify'

/** E-03/E-07 routes (checkpoints list / rewind dispatch / run impact). */
export async function rewindRoutes(_app: FastifyInstance) {
  // Lane C fills this plugin. Registered at W0 so lanes never edit index.ts.
}
