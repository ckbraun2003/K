// core/src/routes/notifications.ts (W0 stub — Lane B lands the endpoints)
import type { FastifyInstance } from 'fastify'

/** E-19 notification center + rules routes. */
export async function notificationsRoutes(_app: FastifyInstance) {
  // Lane B fills this plugin. Registered at W0 so lanes never edit index.ts.
}
