node_modules/
dist/
.env
.env.local
core/data/
data/
*.db
*.db-shm
*.db-wal
# persisted harness bearer token (first-run generated secret) — never commit
auth-token
# artifacts: track the bible sources (artifacts/bible/**) — the living spec.
# the compiled *.html (project-bible.html, ui-demo.html, mobile-ui-demo.html) is
# generated/overwritten on boot, so keep build output out of the repo.
artifacts/*.html
tasks/
.superpowers/
workspace/
.gitnexus/
# e2e user-testing swarm: keep generated reports, screenshots, isolated-stack
# data dirs, and throwaway fixture repos out of the repo (specs/lib are tracked).
e2e/reports/
e2e/.data/
e2e/.fixtures/
test-results/
playwright-report/
CLAUDE.md
AGENTS.md
# local harness working dirs — kept on disk, out of version control
.claude/
docs/
