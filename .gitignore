node_modules/
dist/
.env
.env.local
core/data/
data/
*.db
*.db-shm
*.db-wal
# persisted harness bearer token (first-run generated secret) — never commit
auth-token
tasks/
.superpowers/
workspace/
.gitnexus/
# e2e user-testing swarm: keep generated reports, screenshots, isolated-stack
# data dirs, and throwaway fixture repos out of the repo (specs/lib are tracked).
e2e/reports/
e2e/.data/
e2e/.fixtures/
test-results/
playwright-report/
# Playwright MCP screenshot/output dir written into the repo root — never commit
.playwright-mcp/
CLAUDE.md
AGENTS.md
# local harness working dirs — kept on disk, out of version control
.claude/
docs/
.worktrees/
# P0 live-probe transcripts (raw CLI output incl. host-hook noise) — never commit
core/probes/out/
# Electron desktop app: packaging output + bundled runtime staging — never commit
# (compiled main/preload go to desktop/dist which the global dist/ rule already covers)
desktop/release/
desktop/out/
desktop/node_modules/
# prepackage staging (deployed core + bundled node + copied web/agent-config/bible)
desktop/staging/
# generated multi-size icon (prepackage rebuilds it from assets/icon.png each build)
desktop/assets/icon.ico
