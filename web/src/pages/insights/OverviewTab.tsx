// A1 placeholder — Task 5 (A2) replaces this with the deterministic deltas/anomaly Overview.
export default function OverviewTab(_props: { days: 14 | 30 | 60 }) {
  return <div data-testid="insights-overview" className="text-xs text-[var(--muted)]">Overview</div>
}
