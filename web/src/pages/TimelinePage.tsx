// web/src/pages/TimelinePage.tsx (W0 stub — Lane B replaces the whole body)
export default function TimelinePage() {
  return (
    <div data-testid="timeline-page" className="flex-1 flex items-center justify-center text-xs text-[var(--muted)]">
      Timeline
    </div>
  )
}
