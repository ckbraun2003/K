// web/src/pages/InsightsPage.tsx (W0 stub — Lane A replaces the body, keeping this exact prop contract)
export default function InsightsPage(_props: { tab?: string }) {
  return (
    <div data-testid="insights-page" className="flex-1 flex items-center justify-center text-xs text-[var(--muted)]">
      Insights
    </div>
  )
}
