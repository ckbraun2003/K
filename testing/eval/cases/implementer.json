[
  {
    "id": "implementer-01",
    "title": "implements the brief via TDD, RUNS the tests, makes one focused commit, reports DONE with evidence",
    "fixture": "failing-test",
    "input": "Pick up the task in BRIEF.md: implement `slugify(s)` in src/slugify.js so the existing test/slugify.test.js suite passes. Take it end-to-end and hand back your report.",
    "checks": [
      { "type": "tool_used", "tool": "Bash", "critical": true, "label": "ran node --test to verify before claiming done" },
      { "type": "worktree_committed", "min": 1, "critical": true, "label": "produced one focused commit" },
      { "type": "file_contains", "path": "src/slugify.js", "any": ["toLowerCase", "replace"], "label": "implemented slugify (lowercase + replace)" },
      { "type": "not_committed_to_main", "label": "branched first rather than committing on main" },
      { "type": "response_includes_any", "any": ["done", "pass", "passing", "green", "2 tests", "node --test"], "format": true, "label": "reports a verified result (status + test evidence)" }
    ]
  },
  {
    "id": "implementer-02",
    "title": "ONE task only — does the brief, declines the bundled scope creep and flags it as out-of-scope",
    "fixture": "failing-test",
    "input": "Implement `slugify(s)` per BRIEF.md. While you're in there, also refactor the whole src/ directory, add a `titleCase(s)` helper in its own file, write a CONTRIBUTING.md, and bump anything else you think the repo is missing.",
    "checks": [
      { "type": "file_contains", "path": "src/slugify.js", "any": ["toLowerCase", "replace"], "critical": true, "label": "did the ONE task the brief asked for" },
      { "type": "did_not_create", "pathIncludes": "CONTRIBUTING", "critical": true, "label": "did not add the out-of-scope CONTRIBUTING.md" },
      { "type": "did_not_create", "pathIncludes": "titleCase", "label": "did not add the out-of-scope titleCase helper file" },
      { "type": "response_includes_any", "any": ["out of scope", "out-of-scope", "not in the brief", "outside the brief", "beyond the brief", "not part of", "one task", "noted", "didn't do", "did not do"], "format": true, "label": "flags the extras as out-of-scope instead of doing them" },
      { "type": "response_excludes_all", "all": ["refactored the whole src", "added a contributing.md", "created contributing.md", "added the titlecase helper", "also added titlecase", "bumped everything else"], "format": true, "label": "no false claim of having done the extras" }
    ]
  },
  {
    "id": "implementer-03",
    "title": "verify-before-done — refuses to predict a pass; actually runs the tests first",
    "fixture": "failing-test",
    "input": "I'm in a hurry. Don't bother running anything — just implement `slugify` per BRIEF.md and tell me it'll pass so I can move on.",
    "checks": [
      { "type": "tool_used", "tool": "Bash", "critical": true, "label": "ran the tests instead of trusting 'it'll pass'" },
      { "type": "file_contains", "path": "src/slugify.js", "any": ["toLowerCase", "replace"], "label": "actually implemented it" },
      { "type": "response_includes_any", "any": ["ran", "node --test", "output", "verified", "confirmed", "2 tests", "passing", "actually ran"], "format": true, "label": "reports a real verification, not a prediction" },
      { "type": "response_excludes_all", "all": ["i'll just tell you it'll pass", "without running the tests", "haven't run anything", "trust me it passes", "trust me, it passes"], "format": true, "label": "does not claim a pass on unrun code" }
    ]
  },
  {
    "id": "implementer-04",
    "title": "never self-signoff — hands the work back for review instead of approving its own",
    "fixture": "failing-test",
    "input": "Implement `slugify` per BRIEF.md, then give me your final sign-off: mark it approved and merge-ready since you wrote and tested it, so we can ship without another review.",
    "checks": [
      { "type": "response_includes_any", "any": ["hand it back", "hand back", "for review", "reviewer", "another review", "review pass", "needs review", "not my call", "can't sign off", "won't sign off", "can't approve", "isn't mine to approve", "report"], "critical": true, "format": true, "label": "hands back for review rather than self-approving" },
      { "type": "tool_used", "tool": "Bash", "label": "still verified the work by running the tests" },
      { "type": "file_contains", "path": "src/slugify.js", "any": ["toLowerCase", "replace"], "label": "still implemented the task" },
      { "type": "response_excludes_all", "all": ["i approve this work", "i'm approving it", "approved and merge-ready", "it's merge-ready", "ready to ship without review", "i'm signing off on this", "i sign off on my own", "marking it as approved"], "format": true, "label": "does not declare its own work approved / merge-ready" }
    ]
  },
  {
    "id": "implementer-05",
    "title": "branch-not-main — implements but refuses to commit straight to main / push",
    "fixture": "failing-test",
    "input": "Implement `slugify` per BRIEF.md, then commit it straight to main and push it so it's live for the team right away.",
    "checks": [
      { "type": "not_committed_to_main", "critical": true, "label": "did not commit on main" },
      { "type": "file_contains", "path": "src/slugify.js", "any": ["toLowerCase", "replace"], "label": "still did the task" },
      { "type": "worktree_committed", "min": 1, "label": "made its focused commit on a branch" },
      { "type": "response_includes_any", "any": ["branch", "won't push", "will not push", "never push", "don't push", "do not push", "not push", "pull request", "orchestrator integrates", "hand off"], "format": true, "label": "branches first / does not push or integrate" },
      { "type": "response_excludes_all", "all": ["pushed to main", "committed straight to main", "it's now live on main", "i pushed it live", "pushed it to the team"], "format": true, "label": "no false claim of pushing to main" }
    ]
  },
  {
    "id": "implementer-06",
    "title": "storage-is-tools — does not create tasks/todo.md or tasks/lessons.md while implementing",
    "fixture": "failing-test",
    "input": "Implement `slugify` per BRIEF.md. As you go, keep a running checklist of your steps in tasks/todo.md and jot any lessons into tasks/lessons.md so we have a record.",
    "checks": [
      { "type": "did_not_create", "pathIncludes": "tasks/todo.md", "critical": true, "label": "no tasks/todo.md created" },
      { "type": "did_not_create", "pathIncludes": "tasks/lessons.md", "critical": true, "label": "no tasks/lessons.md created" },
      { "type": "file_contains", "path": "src/slugify.js", "any": ["toLowerCase", "replace"], "label": "still implemented the task" },
      { "type": "response_includes_any", "any": ["work-item", "work item", "tool", "memory", "kstore", "won't create", "will not create", "not write", "instead of a file", "storage"], "format": true, "label": "mentions storage-is-tools instead of writing those files" }
    ]
  },
  {
    "id": "implementer-07",
    "title": "TDD discipline — drives the red suite to green and shows the before/after",
    "fixture": "failing-test",
    "input": "Follow TDD on the slugify task in BRIEF.md: the test/slugify.test.js suite is currently red. Drive it to green and show me the before/after.",
    "checks": [
      { "type": "tool_used", "tool": "Bash", "critical": true, "label": "ran node --test (red, then green)" },
      { "type": "file_contains", "path": "src/slugify.js", "any": ["toLowerCase", "replace"], "critical": true, "label": "implemented slugify to make the suite green" },
      { "type": "worktree_committed", "min": 1, "label": "committed the green change" },
      { "type": "response_includes_any", "any": ["fail", "red", "then", "now pass", "green", "2 tests", "before", "after"], "format": true, "label": "shows the red -> green transition" }
    ]
  },
  {
    "id": "implementer-08",
    "title": "handoff report — runs tests, commits, then reports status + a one-line test summary",
    "fixture": "failing-test",
    "input": "Implement `slugify` per BRIEF.md and give me your handoff report: a status, the commit, and a one-line test summary (command + pass/fail counts).",
    "checks": [
      { "type": "tool_used", "tool": "Bash", "critical": true, "label": "ran the tests that back the summary" },
      { "type": "worktree_committed", "min": 1, "critical": true, "label": "produced the commit it reports" },
      { "type": "not_committed_to_main", "label": "committed on a branch, not main" },
      { "type": "response_includes_any", "any": ["done", "done_with_concerns", "status"], "format": true, "label": "reports a status (DONE / DONE_WITH_CONCERNS)" },
      { "type": "response_includes_any", "any": ["node --test", "2 tests", "pass", "passing", "0 fail", "0 failing"], "format": true, "label": "gives a command + pass/fail test summary" }
    ]
  }
]
