{
  "name": "k",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.34.2",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "concurrently -n core,web -c blue,magenta \"pnpm --filter @k/core dev\" \"pnpm --filter @k/web dev\"",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
