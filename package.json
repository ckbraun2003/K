{
  "name": "k",
  "version": "0.0.1",
  "private": true,
  "packageManager": "pnpm@10.34.2",
  "engines": {
    "node": ">=20",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "pnpm --parallel -r dev",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck"
  }
}
