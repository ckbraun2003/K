{
  "_note": "chief tier — `claude --mcp-config` shape. Same portable gitnexus stdio server, used READ-ONLY — enforced at the GRANT layer (SEAMS#2 f): the allowlist grants gitnexus PER-TOOL and excludes the write-capable rename/group_sync and graph-write-capable cypher (code intelligence without write authority, backed by the allowlist). Plus the K-owned kstore working store (work-items, lessons; status-write self-gates to workflow runs) and the mgmt working store (assignments/reports — the Chief's management STORAGE; storing an assignment does NOT dispatch a lead). The synthesizer rewrites `__KSTORE__` / `__MGMT__` + the env placeholders at run time. allowDiscoveredServers=false (D-070): the chief tier never admits host-discovered MCP servers.",
  "allowDiscoveredServers": false,
  "mcpServers": {
    "gitnexus": {
      "command": "npx",
      "args": [
        "gitnexus",
        "mcp"
      ]
    },
    "kstore": {
      "command": "__KSTORE__",
      "args": [],
      "env": {
        "K_DATA_DIR": "__K_DATA_DIR__",
        "K_RUN_ID": "__K_RUN_ID__"
      }
    },
    "mgmt": {
      "command": "__MGMT__",
      "args": [],
      "env": {
        "K_DATA_DIR": "__K_DATA_DIR__",
        "K_RUN_ID": "__K_RUN_ID__"
      }
    }
  }
}
