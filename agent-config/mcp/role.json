{
  "_note": "ephemeral role subagent tier (PLANNED — Phase 5) — `claude --mcp-config` shape. Portable gitnexus stdio server via npx; inherits the workflow's intelligence.",
  "mcpServers": {
    "gitnexus": {
      "command": "npx",
      "args": ["gitnexus", "mcp"]
    }
  }
}
