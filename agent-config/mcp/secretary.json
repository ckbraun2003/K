{
  "_note": "secretary tier (K, PLANNED — Phase 5) — `claude --mcp-config` shape. The K-owned kstore working store (work-items, lessons) is mounted so K can track tickets/memory; status-write self-gates to workflow runs. The synthesizer rewrites `__KSTORE__` + the env placeholders at run time (Wave 6). The logistics-mcp (calendar/notes/task-list/scheduling) and the reused Google Calendar/Gmail/Drive connectors are Phase 5.",
  "mcpServers": {
    "kstore": {
      "command": "__KSTORE__",
      "args": [],
      "env": {
        "K_DATA_DIR": "__K_DATA_DIR__",
        "K_RUN_ID": "__K_RUN_ID__"
      }
    }
  }
}
