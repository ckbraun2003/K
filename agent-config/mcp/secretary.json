{
  "_note": "secretary tier (K, PLANNED — Phase 5) — `claude --mcp-config` shape. The K-owned kstore working store (work-items, lessons) is mounted so K can track tickets/memory; status-write self-gates to workflow runs. The logistics-mcp (calendar/notes/scheduling) is now mounted so K can keep run-scoped notes, calendar events, and reminders (STORAGE, not execution — it does not act on any real calendar). The synthesizer rewrites `__KSTORE__` / `__LOGISTICS__` + the env placeholders at run time. The reused Google Calendar/Gmail/Drive connectors remain Phase 5. allowDiscoveredServers=false (D-070): the secretary tier never admits host-discovered MCP servers.",
  "allowDiscoveredServers": false,
  "mcpServers": {
    "kstore": {
      "command": "__KSTORE__",
      "args": [],
      "env": {
        "K_DATA_DIR": "__K_DATA_DIR__",
        "K_RUN_ID": "__K_RUN_ID__"
      }
    },
    "logistics": {
      "command": "__LOGISTICS__",
      "args": [],
      "env": {
        "K_DATA_DIR": "__K_DATA_DIR__",
        "K_RUN_ID": "__K_RUN_ID__"
      }
    }
  }
}
