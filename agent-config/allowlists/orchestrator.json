{
  "_note": "orchestrator tier — full coding authority within charter. Passed to `claude --allowedTools`. Coding tools (Bash, Write, Edit, Task) are present at this tier. The subagent-spawn tool is `Task` (verified against the claude CLI binary) — agents are the concept, `Task` is the tool id the CLI keys on.",
  "allowedTools": [
    "Bash",
    "Read",
    "Write",
    "Edit",
    "Grep",
    "Glob",
    "Task",
    "WebFetch",
    "WebSearch",
    "mcp__gitnexus"
  ]
}
