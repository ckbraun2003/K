{
  "_note": "orchestrator lead tier (PLANNED — Phase 5) — staff-engineer specialist with FULL coding authority within charter. Same coding set as the controller. Passed to `claude --allowedTools`. Subagent-spawn tool is `Task` (verified against the claude CLI binary).",
  "allowedTools": [
    "Bash",
    "Read",
    "Write",
    "Edit",
    "Grep",
    "Glob",
    "Task",
    "WebFetch",
    "WebSearch"
  ]
}
