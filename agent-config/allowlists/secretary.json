{
  "_note": "secretary tier (K) — logistics, Q&A, scheduling. NO code authority. Coding tools (Bash, Write, Edit, Task) and even Grep/Glob are absent; K only reads and researches. `mcp__kstore` grants the kstore working store (work-items/lessons); `mcp__logistics` grants the logistics working store (calendar/notes/scheduling) mounted in mcp/secretary.json. Mounting an MCP server ≠ granting it (D-034), so each server must be listed here or its tools are denied in headless `-p`.",
  "allowedTools": [
    "Read",
    "WebFetch",
    "WebSearch",
    "mcp__kstore",
    "mcp__logistics"
  ]
}
