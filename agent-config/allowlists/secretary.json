{
  "_note": "secretary tier (K) — the operator's PRIMARY agent. Full read/analyze authority: Read/Grep/Glob for code, WebFetch/WebSearch for research, `mcp__gitnexus` for code intelligence (mounted in mcp/secretary.json). No mutation BUILT-INS: Bash/Write/Edit/Task are denied by omission — writes happen in delegated runs. NB `mcp__gitnexus` is a SERVER-level grant (chief parity): gitnexus's write-capable tools (rename, group_sync) ride it, so read-only gitnexus use is a charter-enforced usage convention, not an allowlist boundary. `mcp__kstore` grants the kstore working store (work-items/lessons); `mcp__logistics` grants the logistics working store (calendar/notes/scheduling). Mounting an MCP server ≠ granting it (D-034), so each server must be listed here or its tools are denied in headless `-p`.",
  "allowedTools": [
    "Read",
    "Grep",
    "Glob",
    "WebFetch",
    "WebSearch",
    "mcp__gitnexus",
    "mcp__kstore",
    "mcp__logistics"
  ]
}
