{
  "_note": "secretary tier (K, PLANNED — Phase 5) — logistics, Q&A, scheduling. NO code authority. Coding tools (Bash, Write, Edit, Task) and even Grep/Glob are absent; K only reads and researches. Logistics actions come from the (Phase-5) logistics-mcp, not from coding tools.",
  "allowedTools": [
    "Read",
    "WebFetch",
    "WebSearch"
  ]
}
