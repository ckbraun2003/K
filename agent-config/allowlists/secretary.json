{
  "_note": "secretary tier (K, PLANNED — Phase 5) — logistics, Q&A, scheduling. NO code authority. Coding tools (Bash, Write, Edit, Task) and even Grep/Glob are absent; K only reads and researches. Logistics actions come from the (Phase-5) logistics-mcp, not from coding tools. `mcp__kstore` grants the kstore working store (work-items/lessons) mounted in mcp/secretary.json — must be granted or its tools are denied in headless `-p`.",
  "allowedTools": [
    "Read",
    "WebFetch",
    "WebSearch",
    "mcp__kstore"
  ]
}
