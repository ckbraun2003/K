{
  "_note": "secretary tier (K) — the operator's PRIMARY agent. Full read/analyze authority: Read/Grep/Glob for code, WebFetch/WebSearch for research, the gitnexus READ/ANALYSIS tools for code intelligence (mounted in mcp/secretary.json). No mutation BUILT-INS: Bash/Write/Edit/Task are denied by omission — writes happen in delegated runs. SEAMS#2 (f): gitnexus is granted PER-TOOL, not server-level — the write-capable tools (rename, group_sync) and graph-write-capable cypher are EXCLUDED at the grant layer, so read-only gitnexus is an allowlist boundary, not a charter-prose convention (K is the highest-injection-exposure profile: persistent, WebFetch-carrying, operator-primary). The operator can re-grant an excluded tool per-profile if a workflow ever needs it. `mcp__kstore` grants the kstore working store (work-items/lessons); `mcp__logistics` grants the logistics working store (calendar/notes/scheduling). Mounting an MCP server ≠ granting it (D-034): per-tool tokens satisfy the mount requirement.",
  "allowedTools": [
    "Read",
    "Grep",
    "Glob",
    "WebFetch",
    "WebSearch",
    "mcp__gitnexus__query",
    "mcp__gitnexus__context",
    "mcp__gitnexus__impact",
    "mcp__gitnexus__api_impact",
    "mcp__gitnexus__detect_changes",
    "mcp__gitnexus__route_map",
    "mcp__gitnexus__tool_map",
    "mcp__gitnexus__shape_check",
    "mcp__gitnexus__group_query",
    "mcp__gitnexus__group_list",
    "mcp__gitnexus__group_contracts",
    "mcp__gitnexus__group_status",
    "mcp__gitnexus__list_repos",
    "mcp__kstore",
    "mcp__logistics"
  ]
}
