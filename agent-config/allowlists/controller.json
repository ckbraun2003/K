{
  "_note": "controller / lead tier — full coding authority. Passed to `claude --allowedTools`. Coding tools (Bash, Write, Edit, Task) are present at this tier. The subagent-spawn tool is `Task` (verified against the claude CLI binary).",
  "allowedTools": [
    "Bash",
    "Read",
    "Write",
    "Edit",
    "Grep",
    "Glob",
    "Task",
    "WebFetch",
    "WebSearch"
  ]
}
